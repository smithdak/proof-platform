{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["operation", "data"],
  "properties": {
    "operation": {"type": "string", "const": "workflow.step.complete"},
    "data": {
      "type": "object",
      "required": ["run_id", "status", "completed_at", "content_digest"],
      "properties": {
        "run_id": {"type": "string", "format": "uuid"},
        "status": {"type": "string"},
        "completed_at": {"type": "string"},
        "content_digest": {"type": "string"}
      }
    }
  }
}
