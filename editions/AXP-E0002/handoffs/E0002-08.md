# E0002-08 handoff

## Status

Complete under D-E0002-019. Local auth formatting and tests, quiescent reverse-impact validation, and fresh independent final review all passed. The superseded `W3 auth retry quiescent` signal was not used; auth instead reached the authorized `W3 auth D019 quiescent` barrier.

## Attempt history

The initial implementation completed the serialized W3 manifest and lock phases, implemented the frozen auth contract, and produced passing initial scoped evidence. During that initial attempt, the first auth test compile exposed one owned `Zeroizing<[u8; 32]>` coercion mismatch. Its immediate correction and rerun were an implementation iteration inside the initial attempt; they did not consume the edition task's bounded evaluation retry.

Independent review then returned FAIL. The review blockers were strict inbound UUID/time validation, zeroizing secret ownership and public secret-carrier behavior, and acceptance-evidence accuracy at the auth/storage boundary. That FAIL triggered the one permitted task-level retry.

During the retry, auth-owned source and tests were revised for strict canonical lowercase hyphenated RFC 4122 UUIDv7 handling, strict frozen RFC3339 UTC handling, zeroizing nonce/token ownership, a redacted borrow-only session-header carrier, and materially trusted disposable-workspace fixtures. Repository-root descriptor comparison was not invented at the auth content-only boundary. Static review returned a provisional PASS, after which auth source was held quiescent until `W3 kernel retry quiescent` released the Cargo barrier.

The released retry commands produced:

- `rtk cargo fmt -p proof-operator-auth` — exit 0.
- `rtk cargo fmt --check -p proof-operator-auth` — exit 0.
- `rtk cargo test -p proof-operator-auth` — exit 101 during compilation.

The failed test compile reported six owned E0308 errors in `crates/proof-operator-auth/src/tests.rs`: the call sites at lines 716, 761, 836, 930, 1359, and 1447 passed `&Zeroizing<[u8; 32]>` to `encode_hex`, which requires `&[u8]`. It also reported two unused `serde::de::Error` imports in `crates/proof-operator-auth/src/types.rs` at lines 427 and 458.

Per the single-retry rule, no source edit and no further command was performed after that failure. In particular, `rtk scripts/test-scoped.sh proof-operator-auth` was not run.

## Changed files from the implementation attempts

- `Cargo.toml`
- `Cargo.lock`
- `crates/proof-operator-auth/Cargo.toml`
- `crates/proof-operator-auth/src/lib.rs`
- `crates/proof-operator-auth/src/types.rs`
- `crates/proof-operator-auth/src/authority.rs`
- `crates/proof-operator-auth/src/error.rs`
- `crates/proof-operator-auth/src/tests.rs`
- `editions/AXP-E0002/handoffs/E0002-08.md`

This administrative update changes only this handoff.

## Dependency and lock delta

The accepted W3 root delta added `crates/proof-operator-auth` as a workspace member and the frozen workspace dependencies `sha2 = 0.10.9`, `subtle = 2.6.1`, and `zeroize = 1.9.0`. The auth manifest uses the frozen dependency set. The serialized offline reconciliation added the auth package and reconciled kernel dependency references in `Cargo.lock`; it required no registry version changes or downloads.

The exact serialized check was `rtk cargo check -p proof-operator-auth -p proof-kernel --offline`, which passed before product-source fan-out and established `W3 lock stable`.

## Assumptions and residual risks

- The frozen kernel API and Gate-B packet remain authoritative; no E0006 authority was used.
- The retry source has only a provisional static-review PASS. It has no successful compile/test evidence because the scoped test command failed before tests ran.
- D-E0002-015 authorized one exceptional mechanical attempt after the sole task-level retry. The six test-call coercions and two unused imports were edited as authorized. D-E0002-019 subsequently released the auth validation barrier, and those edits passed the two permitted local checks recorded below.
- Quiescent reverse-impact validation passed for the one impacted package.

## Cross-owner acceptance request

Actual rejection of a repository-root `.proof` identity cannot be claimed by E0002-08: `AuthPolicy` receives content bindings and has no descriptor provenance or forbidden-directory input. That enforcement is frozen to storage/control assembly by contract sections 4.1 and 4.2 through `acquire_operator_workspace_lock(... forbidden_proof_directories ...)`, with E0002-11 acceptance lines 55–61 and integrated E0002-14/E0002-15 evidence. The owner should retain that integrated evidence as the repository-root acceptance proof; this is not a request for an auth contract or API change.

## D-E0002-015 exceptional attempt

D-E0002-015 authorized exactly eight mechanical auth edits: six recorded test call sites were changed so `encode_hex` receives an explicit byte-slice borrow from `Zeroizing<[u8; 32]>`, and the two recorded unused `serde::de::Error` imports were removed.

No Cargo command ran during the D-E0002-015 attempt. Kernel's mandated `rtk cargo fmt --check -p proof-kernel` exited 1, so the exact `W3 kernel exceptional quiescent` barrier was never released. Per the stop-on-any-failure rule, there was no further source edit, no local scoped-test evidence, no reverse-impact evidence, and no `W3 auth exceptional quiescent` signal at that point.

## D-E0002-019 validation evidence

After the exact `W3 kernel D019 quiescent` barrier release, no source, manifest, lock, or contract edit was made. The two authorized commands ran in order:

- `rtk cargo fmt --check -p proof-operator-auth` — exit 0.
- `rtk cargo test -p proof-operator-auth` — exit 0; 26 tests passed across 2 suites in 0.03 seconds.

After auth reached the D-E0002-019 quiescent barrier, `rtk scripts/test-scoped.sh proof-operator-auth` passed 26 tests across the one impacted package and 2 suites.

Fresh independent final review returned PASS. It verified the frozen hashes; strict UUIDv7 and UTC serde; zeroizing secrets and the redacted, borrow-only header carrier; consuming constant-work auth-first and race behavior; correctly bounded disposable-workspace evidence; no E0006 authority; and the exact exceptional scope of six explicit slice borrows plus two unused-import removals. It found no manifest, lock, public API, or behavior drift.

E0002-08 is complete under D-E0002-019.

## Administrative completion update

This handoff-only completion update records existing quiescent impact and independent-review evidence. It makes no product or source change.
