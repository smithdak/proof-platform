# E0002-11 Handoff

Current assignment note: D-E0002-041 shifts the unfinished task to W6 and adds
E0002-17 as a prerequisite. Every quoted W4 barrier signal below is immutable
historical evidence and is neither renamed nor rerun by the planning repair.

- Edition/task: `AXP-E0002 / E0002-11`
- Agent/model/reasoning: e0002-control / `gpt-5.6-sol` / high
- Result: `blocked — D-E0002-024 scoped test failure; pending owner direction`
- Dispatch: D-E0002-021, W4 only
- Live/provider spend and external effects: zero

## Serialized manifest and lock barrier

- Preserved the complete existing W3 root and lock deltas.
- Added only the frozen root member `crates/proof-operator-control` and exact
  workspace dependency `rustix = { version = "=1.1.4", features = ["fs",
  "process", "termios"] }`.
- Created the exact complete W4 control package manifest and the exact inert
  two-line `src/lib.rs` scaffold before any Cargo command.
- Inspected the scoped root/crate diff and signalled exactly `W4 root ready`.
- After the orchestrator relayed exact `W4 storage manifest frozen` with all
  W4 source trees quiescent, ran exactly
  `rtk cargo check -p proof-operator-control -p proof-storage --offline`.
  It exited 0 with two pre-existing warnings and reconciled only the expected
  W3 auth/kernel, W4 control, and W4 storage package dependency entries in
  `Cargo.lock`.
- Signalled exactly `W4 lock stable` before any W4 source implementation.

## Changed files

- `Cargo.toml`
- `Cargo.lock`
- `crates/proof-operator-control/Cargo.toml`
- `crates/proof-operator-control/src/lib.rs`
- `crates/proof-operator-control/src/error.rs`
- `crates/proof-operator-control/src/environment.rs`
- `crates/proof-operator-control/src/cursor.rs`
- `crates/proof-operator-control/src/static_bundle.rs`
- `crates/proof-operator-control/src/workspace.rs`
- `crates/proof-operator-control/src/ceremony.rs`
- `crates/proof-operator-control/src/lifecycle.rs`
- `crates/proof-operator-control/src/listener.rs`
- `crates/proof-operator-control/src/routing.rs`
- `crates/proof-operator-control/src/tests.rs`
- `editions/AXP-E0002/handoffs/E0002-11.md`

## Implemented source pending acceptance

- Added the control-owned OS `OperatorControlEnvironment` using SystemTime,
  Instant, OsRng, UUIDv7 construction, and the rustix effective-user identity
  check.
- Added a process-local keyed-BLAKE3 cursor codec with canonical claims,
  constant-work MAC verification, exact scope/page/expiry binding, and
  restart-local key custody.
- Added a closed in-memory static-bundle interface with raw SHA-256
  recomputation, exact `/` and `/assets/<filename>` constraints, and no
  filesystem fallback.
- Added the frozen authoritative database/store-opener interface. It supplies
  only `<workspace>/.proof/storage/storage.db`, a nonempty mandatory build-
  anchor exclusion set, and the same process environment; it exposes no trust
  boolean, alternate database, or ephemeral signer seam.
- Added the one-attempt signed-challenge TTY ceremony plus direct `/dev/tty`
  rustix termios echo guard, challenge consumption on failure, exact
  attestation handoff to `proof-operator-auth`, and no challenge-code terminal
  disclosure.
- Added the exact 15-route synthetic inventory, closed static source,
  no-authority synthetic handler, uniform security headers, and explicit
  exclusion of legacy audit/proof/domain/operation routes.
- Added exact IPv4 `127.0.0.1:0` listener binding, clean non-secret URL,
  direct serving seam, and the ordered stop/drain/release/checkpoint/auth-
  invalidate/store-close/workspace-lock-release shutdown interface.
- Added direct deterministic tests for environment, cursor tamper/restart,
  static/router inventory, frozen store-open inputs, challenge success/replay/
  mismatch, shutdown chronology, loopback serving/security headers, and
  algorithm-distinct artifact digests.
- Imported no E0006 source or authority and no W4 storage/runtime/general HTTP
  source. No secret, provider/tool call, remote bind, external effect, or
  persistent browser authority was introduced.

## Local commands and exact results

1. `rtk cargo fmt --check -p proof-operator-control` — exited 1 with only
   rustfmt layout diffs in owned new files.
2. `rtk cargo fmt -p proof-operator-control` — exited 0; scoped mechanical
   formatting only.
3. `rtk cargo fmt --check -p proof-operator-control` — exited 0.
4. `rtk cargo test -p proof-operator-control` — exited 101 before tests ran:
   one E0405 at `src/listener.rs:43`, missing `std::future::Future` import.
5. The one bounded source retry added only that import.
6. `rtk cargo fmt --check -p proof-operator-control` — exited 0.
7. `rtk cargo test -p proof-operator-control` — exited 101 before tests ran
   with four owned compile errors:
   - E0308 `src/cursor.rs:35`: keyed BLAKE3 requires `&[u8; 32]`, but
     `Zeroizing::as_ref()` produced `&[u8]`;
   - E0308 `src/cursor.rs:81`: kernel `canonicalize` accepts
     `&serde_json::Value`, not `&str`;
   - E0283 `src/cursor.rs:153`: base64 input needs an explicit byte-slice type;
   - E0308 `src/environment.rs:63`: UUID builder requires `&[u8; 10]`, but
     `Zeroizing::as_ref()` produced `&[u8]`.

Per the one-bounded-retry rule, no correction or command followed the second
test failure. No test executed, no reverse-impact command ran, no source-
quiescent success signal was emitted, and completion is not claimed.

## D-E0002-024 continuation

- After the exact `W4 kernel digest API accepted` gate signal, D-E0002-024
  authorized only the four recorded owned compile corrections.
- Applied exactly those four corrections: an exact `&[u8; 32]` keyed-BLAKE3
  borrow without exposing or duplicating the zeroizing key; strict parsing to
  `serde_json::Value` before kernel canonicalization; an explicit byte-slice
  input for base64; and an exact `&[u8; 10]` UUIDv7 random-array borrow.
- `rtk cargo fmt --check -p proof-operator-control` exited 0.
- `rtk cargo test -p proof-operator-control` ran 9 tests and exited 101: 8
  passed and 1 failed. The failing test was
  `tests::listener_and_synthetic_router_are_loopback_only_and_security_headered`,
  which panicked at `tests.rs` around line 604 by unwrapping
  `Err(ListenerUnavailable)`.
- Per D-E0002-024, no post-failure source inspection, edit, or repair occurred.
  No reverse-impact check or independent review ran, and the exact
  `W4 control D024 locally green` signal was not emitted.
- E0002-11 remains blocked pending owner direction.

## Stop state and next authority needed

E0002-11 is quiescent and blocked after the D-E0002-024 scoped test failure.
The four recorded compile blockers were corrected, but the listener test
failure requires new owner direction before inspection, editing, or rerunning
the local gates. Quiescent reverse impact and fresh independent review remain
pending.

## Assumptions and threat limits

- The Gate-B packet, frozen kernel/auth APIs, and D-E0002-021 remain the sole
  authority.
- The W4 shell deliberately exposes synthetic router/store/signer seams only;
  real SQLite/runtime/protected HTTP/static composition remains E0002-14/15.
- Root compromise, malicious same-effective-UID processes, filesystem/process
  memory compromise, a compromised browser/extension, and a compromised
  enrolled Human key remain the contract's documented v1 residual limits.

Never include credentials, private key contents, raw custody tokens, or copied
runtime databases in evidence.

## D-E0002-033 exhaustive repair stop

- Candidate control source implements the nine enumerated D-E0002-025 repair
  areas without dependency, root-manifest, lockfile, or Gate-B changes.
- The first mandated local command,
  `rtk cargo fmt --check -p proof-operator-control`, exited 1 with formatting-
  only differences across `environment.rs`, `lifecycle.rs`, `listener.rs`,
  `routing.rs`, `signer.rs`, `tests.rs`, and `workspace.rs`.
- No post-failure edit, scoped unit test, reverse-impact check, handoff rewrite
  by the lane owner, independent review, or local-green signal followed.
- E0002-11 is quiescent and blocked under D-E0002-035 pending an explicit
  product-owner formatting continuation.

## Post-D-E0002-064 checkpoint compile repair

- The orchestrator preserved the typed, non-clonable session-header custody
  boundary while correcting the remaining test helper and call sites to borrow
  `SessionHeaderValue` bytes. No raw session token was logged or persisted.
- Cursor canonicality now compares the kernel `CanonicalJson` string view with
  the decoded payload instead of comparing unlike types.
- `rtk cargo fmt --check -p proof-operator-control` — passed, exit 0.
- `rtk cargo test -p proof-operator-control` — passed, 17 tests across 2
  suites, exit 0; run outside the filesystem/network sandbox for loopback bind
  coverage.
- `rtk scripts/test-scoped.sh proof-operator-control --list` — impact set was
  exactly `proof-operator-control`.
- `rtk scripts/test-scoped.sh proof-operator-control` — passed, 17 tests across
  2 suites, exit 0 under the same host loopback constraint.
- This evidence does not change E0002-11 assignment status or substitute for
  its separately required fresh independent review.
