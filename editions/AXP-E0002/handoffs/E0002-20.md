# E0002-20 Handoff

- Edition/task: `AXP-E0002 / E0002-20`
- Agent/model/reasoning: e0002-kernel / `gpt-5.6-sol` / high
- Result: `done under D-E0002-088 — exact four-profile kernel alignment and
  every acceptance gate passed`
- Budget planned/used: one 120-minute / 80,000-token attempt; zero
  live/provider spend / completed within the authorized attempt

## Changed files

- `crates/proof-kernel/src/operator/durable.rs` — align
  `StaleFenceRejected` validation with the four accepted command/mask profiles,
  require a nonzero fence, and extend the existing frozen-profile test with
  positive and explicit negative cases.
- Pre-dispatch administrative records changed under D-E0002-087.

## Commands and results

- Read required AXP contract, edition charter/assignments/task, kernel API,
  accepted Gate-B decision, relevant operator-contract profiles, and
  D-E0002-068 through D-E0002-071.
- Pre-dispatch kernel custody: `crates/proof-kernel` has no worktree delta;
  `src/operator/durable.rs` raw SHA-256 is `e31b7112...99ce`.
- Source audit: `StaleFenceRejected` still accepts only obsolete mask
  `0x16c30`; its frozen-profile test contains that single obsolete tuple.
- `rtk git diff -- crates/proof-kernel/src/operator/durable.rs` — PASS; one
  validator branch and its existing test changed.
- `rtk git status --short -- crates/proof-kernel` — PASS; only
  `src/operator/durable.rs` is modified in the assigned crate.
- `rtk git diff --check -- crates/proof-kernel/src/operator/durable.rs` — PASS.
- `rtk cargo fmt --check -p proof-kernel` — PASS.
- `rtk cargo test -p proof-kernel` — PASS; 153 tests across 6 suites.
- `rtk scripts/test-scoped.sh proof-kernel --list` — PASS; selected
  `proof-kernel` plus 13 reverse workspace dependents.
- `rtk scripts/test-scoped.sh proof-kernel` in the restricted sandbox —
  ENVIRONMENT BLOCKED; two `proof-operator-control` loopback-listener tests
  received `EPERM` / `ListenerUnavailable`; all reported non-listener suites
  passed.
- The identical `rtk scripts/test-scoped.sh proof-kernel` command with explicit
  loopback-socket permission — PASS; 762 tests across 55 suites in 20.20s.
- Post-change kernel source custody: only
  `crates/proof-kernel/src/operator/durable.rs` is modified in the assigned
  crate; raw SHA-256 is
  `24c5245df008545b8d3aa709451ac3aee3cc493c550fc4a1b7a7db16732f972a`.
- Frozen artifact custody — PASS: reads
  `1d02cf888c00dcc024fae2ed930e227aa11fdb99b32ec1d498457f7317647542`,
  manifest
  `1ffc871f4478732ba4511e6ae413c30cdfb3cdf588a60bc03de5479dcdfd176c`,
  evaluator
  `fec4fa13f723ee47a2a28d6048ac1e8d1a80dab1f0f599040da08348d4b3830e`,
  and fixture index
  `0380cbb60d84b44765b19ee86e4559bc2b56b84494d1f23bd8b5661b2d09b991`.
- `rtk scripts/swarm.sh validate AXP-E0002` and global
  `rtk git diff --check` — PASS.
- Fresh independent read-only review — PASS: reproduced source custody from
  `e31b7112...99ce` to `24c5245d...972a`, the exact four correlated profiles,
  required negative coverage, unchanged public/serialized surface, both test
  results, edition/diff checks, all four frozen hashes, and accepted contract
  hash `83aa0eec...05f0`; reviewer made no edits and ran no Cargo.

## Assumptions and decisions

The four-profile source alignment is bounded by accepted Gate B v3, was
authorized only under D-E0002-087, and is accepted under D-E0002-088. No public
type/API or downstream change was required. The writer stopped after the local
format and scoped-test gates passed; no source repair or test retry was used.

## Risks and residual work

No E0002-20 residual work remains. The restricted-sandbox reverse run was
repeated only with explicit loopback-socket permission after its documented
environmental prohibition; no source changed between runs. Shifted W9 remains
closed unless separately authorized.

## Cross-owner requests

None recorded.

Never include credentials, private key contents, or copied runtime databases.
