# E0002-12 Handoff

- Edition/task: `AXP-E0002 / E0002-12`
- Agent/model/reasoning: e0002-fixtures / `gpt-5.6-luna` / medium
- Result: `complete — frozen corpus materialized, seed formula corrected, and structurally revalidated`
- Budget planned/used: 45 minutes and 25,000 combined model tokens per attempt;
  zero live/provider spend / bounded mechanical pass

## Changed files

- `evals/fixtures/operator-control/index-v1.json`
- `evals/fixtures/operator-control/valid/001-valid_signed_challenge_exchange.json`
  through `016-valid_runtime_dispatch_failure.json` (16 envelopes)
- `evals/fixtures/operator-control/reject/001-reject_unknown_top.json` through
  `105-reject_revoked_instance_split_brain.json` (105 envelopes)
- `evals/fixtures/operator-control/data/valid_<id>/{setup,action,expected}.json`
  (48 typed recipe documents)
- `evals/fixtures/operator-control/data/reject_<id>/{setup,mutation,action,expected}.json`
  (420 typed recipe documents)

## Commands and results

- `rtk python3` structural validation using the frozen evaluator schema and
  local schema-ID resolver — PASS: all eight schema metas valid; FixtureIndex,
  121 envelopes, and 468 recipe documents valid against their exact frozen
  definitions.
- `rtk python3` index/order validation — PASS: exactly 16 valid and 105
  rejection entries in ordinal filename order.
- `rtk python3` digest linkage validation — PASS: policy digest
  `sha256:d1a10306ece122cb771fc9aa3b51437887040649c117a4716b1b0b9795f4a339`,
  all case/envelope/document/payload digests linked and reproducible under
  compact UTF-8 JSON with recursively sorted keys and no trailing newline.
- `rtk python3` secret-sentinel scan — PASS: no private-key, JWT, or PEM
  material; only deterministic fixture seeds are present.
- `rtk git diff --check` — PASS.
- Independent audit correction: the initial seed prefix was not the frozen
  section 19.1 formula. All 121 envelope seeds were mechanically regenerated
  as `SHA-256("Proof-Operator-Fixture-Seed-v1" || 0x00 ||
  ASCII(fixture_id))`; no recipe, case digest, or policy artifact changed.
- Revalidation after correction — PASS: exact seed formula for 121/121,
  schemas/order/cardinality/blueprint correspondence for all 121 envelopes,
  digest linkage for all 468 documents, secret-sentinel scan,
  trailing-whitespace/no-final-newline scan, and `rtk git diff --check`.

## Assumptions and decisions

The corpus is copied mechanically from the Gate-B-frozen evaluator blueprints;
no policy judgment or case-ID/prose dispatch was added. Rejection setup and
baseline IDs are the frozen evaluator values. Seeds are deterministic SHA-256
derivations of fixture IDs using the exact section 19.1 domain label and NUL
separator, and are not credentials.

## Risks and residual work

No unresolved vector IDs. Executable replay and first complete backend
evaluation belong to E0002-14; this lane makes no product/conformance claim.

## Cross-owner requests

E0002-14 should consume `evals/fixtures/operator-control/index-v1.json`, the
121 envelope files, and the 468 typed recipe documents as immutable inputs and
run its first replay only after the real W4 APIs exist.

Never include credentials, private key contents, or copied runtime databases.
