# E0002-17 Handoff

- Edition/task: `AXP-E0002 / E0002-17`
- Agent/model/reasoning: e0002-fixtures / `gpt-5.6-luna` / medium
- Result: `local PASS under D-E0002-064; awaiting three fresh independent reviews; not done`
- Budget planned/used: one 45-minute / 25,000-token attempt; zero live/provider
  spend / bounded continuation consumed

## Changed files

The worker partially changed files only within
`evals/fixtures/operator-control/**`. The partial candidate remains preserved
and unaccepted; its current `index-v1.json` SHA-256 is
`a71a42ffb0ac2e11ef5f68ea65863fba6a7492eba2759faab9033a662339cb60`.

## Commands and results

- E0002-16 post-freeze decision-order, packet, edition, and diff transition —
  PASS before dispatch.
- `rtk cat assignments.tsv` — FAIL: `/usr/bin/cat: assignments.tsv: No such
  file or directory`; the worker stopped without retry before any edit.
- Under D-E0002-055, the corrected exact assignment path and required packet
  reads succeeded before fixture work.
- Mechanical fixture propagation was partially applied within the authorized
  fixture tree.
- `rtk python3 /tmp/validate_e0002_fixtures.py` — FAIL at its first reported
  mismatch: `document raw digest mismatch: valid_control_plane_restart setup`.
  The worker stopped without retry or further validation.

## Assumptions and decisions

The valid E0002-16 freeze signal remains intact under D-E0002-052. The current
failure concerns a derived fixture raw-digest link, not the frozen artifact or
packet semantics.

## Risks and residual work

The partial fixture candidate has not passed local acceptance. A read-only
diagnostic must first establish the exact changed-file and digest-link mismatch
set before any repair can be bounded. No review is authorized yet.

## Cross-owner requests

None recorded.

Never include credentials, private key contents, or copied runtime databases.

## D-E0002-057 through D-E0002-064 diagnostic and repair closure

- The bounded read-only diagnostics enumerated exactly 28 affected setup
  documents and their 28 matching envelopes. In-memory schema simulation under
  D-E0002-063 established that each setup payload must be the complete frozen
  `fixture_blueprint.setup` object, rather than its `steps` list projection.
- D-E0002-064 required every D-E0002-061 intermediate digest precondition
  before editing. All 28 setup documents then received only the certified
  whole-phase payload and canonical `payload_sha256`; compact UTF-8 form, key
  order, all other fields, and one terminal LF were preserved. Each matching
  envelope received only the certified final raw `setup_sha256`.
- `/tmp/validate_e0002_fixtures.py` was restored to direct whole-phase
  comparison and has SHA-256
  `9437ad3a615d0fc302fd0b2d3e3717ed29d46451292349aa867453ecc0b9f156`.
- `rtk python3 /tmp/validate_e0002_fixtures.py` — PASS: 16 valid scenarios,
  105 rejection envelopes, 468 recipe documents, and exact order, blueprint,
  seed, and digest closure.
- Exact preserved custody reproduced after repair:
  `evals/operator-control-v1.json` SHA-256
  `4e74345485f576a271bf05bae762bee8a11ae821267cdd7573c5d9cd9fc2e36d`;
  `evals/fixtures/operator-control/index-v1.json` SHA-256
  `a71a42ffb0ac2e11ef5f68ea65863fba6a7492eba2759faab9033a662339cb60`;
  superseding packet digest
  `sha256:c1772ffb53a13f66e796b6399f1b70994ac8e80710e6c46fc0a8e434df4ceca8`.
- No index, evaluator, schema, contract, Gate-B artifact, packet, manifest,
  lockfile, source implementation, or other fixture was modified by the
  D-E0002-064 repair. E0002-17 is returned for the separately authorized three
  fresh independent reviews and is explicitly not marked done here.
