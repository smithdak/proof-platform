# E0002-06 Handoff

Current assignment note: D-E0002-071 shifts the unfinished task from W6 to W9
and replaces E0002-17 with E0002-19/E0002-20 as prerequisites. Every quoted W4
barrier signal below is immutable historical evidence and is neither renamed
nor rerun by the planning repair.

- Edition/task: `AXP-E0002 / E0002-06`
- Agent/model/reasoning: e0002-storage / `gpt-5.6-sol` / high
- Result: `done under D-E0002-095 — local, reverse-impact, custody, and fresh
  independent storage review passed`
- External/provider spend: zero

## Barrier history

- Waited without source edit or Cargo until exact `W4 root ready`.
- Added exactly `rustix.workspace = true` to
  `crates/proof-storage/Cargo.toml`, made no other phase-2 edit, ran no Cargo,
  inspected the scoped diff, and signaled exact `W4 storage manifest frozen`.
- Waited without source edit or Cargo until exact `W4 lock stable`.
- Stopped at the missing kernel custody-digest API and resumed only after exact
  `W4 kernel digest API accepted` under D-E0002-024.
- The resumed implementation consumes only
  `LeaseClaimRequest::lease_token_digest`,
  `ReclaimRequest::new_lease_token_digest`, and
  `BeginDispatchRequest::dispatch_token_digest`; it does not access raw custody
  bytes or add storage cryptography.

## Changed files

- `crates/proof-storage/Cargo.toml`
- `crates/proof-storage/src/lib.rs`
- `crates/proof-storage/src/sqlite.rs`
- `crates/proof-storage/src/sqlite/migrations.rs`
- `crates/proof-storage/src/sqlite/migration_14_up.sql`
- `crates/proof-storage/src/sqlite/migration_14_down.sql`
- `crates/proof-storage/src/sqlite/operator_lifecycle.rs`
- `crates/proof-storage/src/sqlite/operator_store.rs`
- `crates/proof-storage/src/sqlite/operator_store_tests.rs`
- `crates/proof-storage/src/sqlite/replay.rs`
- `crates/proof-storage/src/sqlite/store.rs`
- `crates/proof-storage/src/sqlite/trusted_open.rs`
- `editions/AXP-E0002/handoffs/E0002-06.md`

## Implementation evidence

- Versions 1 through 13 were not edited. Migration 14 is the sole appended
  migration, with the frozen up/down SQL held in dedicated include files.
- Ordinary open/migration remains capped at schema 13. The guarded offline
  upgrader alone advances a held operator workspace to schema 14.
- Public `rollback_to` rejects crossing from schema 14 to a lower schema. The
  down SQL is retained only for disposable round-trip evidence and the
  owner-directed quiescent emergency procedure.
- The lifecycle owns descriptor-open workspace/proof/storage/database and lock
  handles, uses the nonblocking exclusive BSD flock, verifies retained
  identities, and opens schema 14 without reacquiring the lock or running an
  implicit migration.
- Workspace initialization derives store-owned time and UUID values from the
  kernel environment and validates the immutable schema catalog before open.
- Storage has transaction-local replay claim support and schema-14 directory,
  audit, lease, budget, dispatch, read, command, and runtime trait surfaces.

## Commands and results

Earlier pre-D024 focused evidence:

- `rtk cargo test -p proof-storage --test migrations` — passed, 14 tests.
- `rtk cargo test -p proof-storage --lib operator_lifecycle` — passed, 2 tests
  after the recorded pre-D024 bounded local correction.

D-E0002-024 final sequence, after source and direct-test inspection:

- `rtk cargo fmt --check -p proof-storage` — passed, exit 0.
- `rtk cargo test -p proof-storage` — passed, 135 tests across 8 suites, exit 0
  in 2.13 seconds.
- Reverse-impact testing was not run by this worker, as required until W4
  quiescence.

## Assumptions, limitations, and review focus

- No repository-root `.proof`, live external data, network, provider, or
  external effect was used.
- Review should treat local green as scoped evidence only. Quiescent reverse
  impact and fresh independent review remain mandatory before completion.
- The manual migration-14 down path is intentionally not exposed as a product
  authorization or receipt API; it requires the separately owner-directed,
  quiescent, backed-up emergency procedure.
- Fresh review should scrutinize the operator read/command/runtime behavioral
  completeness in addition to migration/lifecycle safety; the existing scoped
  suite establishes compilation/regression green but does not independently
  prove every frozen concurrency and reopen scenario.

Never include credentials, private key contents, raw custody tokens, or copied
runtime databases in evidence.

## D-E0002-033 exhaustive repair stop

- Candidate storage source covers the enumerated migration/trusted-open,
  strict reconstruction, scoped reads, durable commands, replay, reclaim,
  budget, dispatch, settlement, projection, and audit areas through the final
  pre-command-profile audit. The last edits strengthened committed replay,
  full Proof verification, projection/checkpoint/event reconstruction,
  ceiling-rounded trusted duration, exact settlement replay, savepoint rollback
  with atomic full-forfeit, migration tests, and lifecycle duplicate checks.
- Source audit then found that the contract-mandated authenticated command
  `stale_fence_rejected` event is not representable by the frozen kernel/schema
  profile: the latter forbids Human/session/command attribution and requires
  runtime reservation/lease/process/permit identifiers commands may not have.
- No edit followed discovery of the blocker. No format, scoped unit,
  reverse-impact, or independent-review command ran, and no local-green signal
  was emitted.
- E0002-06 is quiescent and blocked under D-E0002-036 pending a product-owner
  Gate-B consistency decision; fabricating runtime identifiers is forbidden.

## Post-D-E0002-064 checkpoint storage repair

- The orchestrator repaired six storage-local compile defects without changing
  kernel or frozen Gate-B types: removed a stray range guard, reused strict
  JSON decoding for a stored `ContentDigest`, and checked the wire ordering of
  command/outcome/audit enums that intentionally do not implement `Ord`.
- Legacy run and approval mutation guards now bypass schema-14-only tables on
  ordinary schema-13 stores while continuing to query them, and therefore fail
  closed on corruption, whenever schema 14 is active.
- `SqliteStore::open` now creates a missing Unix database with mode `0600`, so a
  database created by the ordinary initialization path satisfies the later
  trusted-existing opener. Existing database permissions are not changed.
- `rtk cargo fmt --check -p proof-storage` — passed, exit 0.
- `rtk cargo test -p proof-storage` — passed, 140 tests across 8 suites, exit 0.
- `rtk scripts/test-scoped.sh proof-storage --list` — impact set was exactly
  `proof-storage`, `proof-transport-cli`, `proof-transport-http`,
  `proof-transport-mcp`, and `proof-transport-ws`.
- The first sandboxed reverse-impact attempt was invalid environmental evidence:
  sandbox UID mapping made `/` appear owned by UID 65534 and denied a loopback
  bind. The host-constrained rerun passed: `rtk scripts/test-scoped.sh
  proof-storage` — 331 tests across 23 suites, exit 0.
- This evidence does not change E0002-06 assignment status or substitute for
  the separately required fresh independent review.

## D-E0002-083 planning-only ordinal-078 obligation

D-E0002-083 adds no source and does not reuse the prior local-green evidence
for the new requirement. On a later valid W9 dispatch, E0002-06 must change the
current early `NotActionable` settlement-state path so an authority-proven
`ReleasePreDispatch` request against a `dispatching` reservation leaves
reservation/budget/control state unchanged, commits exactly one chained
`budget_rejected` / `rejected` audit event atomically, and only then returns
`NotActionable` for runtime stage `settle_budget`. Invalid authority continues
to append nothing. The lane must add a deterministic reopen/round-trip test for
the exact event and unchanged dispatching reservation plus zero external
effect. This is evaluator rejection ordinal 078
`reject_ambiguous_auto_release`; E0002-14 later verifies but cannot repair it.

No source, fixture, artifact, packet, migration, Cargo, or test command was
changed or run while recording this ownership correction.

## D-E0002-089 shifted-W9 continuation

- Product-owner authority reactivates only E0002-06/E0002-07/E0002-11 after
  E0002-20 completed; W10 and later work remain closed.
- Pre-dispatch `crates/proof-storage` custody is clean relative to HEAD.
  `src/sqlite/operator_store.rs` raw SHA-256 is
  `20c14c80bfd4664d74ef3f72e1e3baa771adacc358cb2dae84820644e0750b2e`.
- This lane owns the exact ordinal-078 atomic audit repair and focused
  deterministic reopen/round-trip evidence. It may edit no peer crate.
- Run local format and scoped storage tests, then stop the writer. Reverse
  impact waits for all three W9 writers to quiesce; fresh independent review
  remains required.

## D-E0002-089 ordinal-078 implementation stop

- Result: `stopped / blocked pending owner direction — task-local one-retry
  ceiling exhausted; source frozen as failure evidence`.
- Changed files are exactly
  `crates/proof-storage/src/sqlite/operator_store.rs`, new
  `crates/proof-storage/src/sqlite/operator_store_tests.rs`, and this unique
  handoff. No migration, manifest, kernel, runtime, control, transport,
  contract, schema, evaluator, fixture, or root file changed.
- `settle_budget_reservation` now validates the frozen
  `ReleasePreDispatch` disposition. After lease authority succeeds, a matching
  `dispatching` reservation with the exact active control pointer appends and
  commits one chained `budget_rejected` / `rejected` event, leaves reservation,
  budget, and run-control state unchanged, and returns
  `OperatorStoreError::NotActionable`. Invalid/stale authority still exits
  before any append. A mismatched active-dispatch pointer is corruption rather
  than auditable actionability.
- The audit insert now stores `occurred_at` with the same canonical RFC3339 UTC
  `Z` encoding used by strict `AuditEvent` JSON. Without this correction the
  schema-14 equality check rejected every attempted event insert before the
  ordinal-078 branch could commit.
- The new deterministic on-disk test covers a strict schema-14 baseline,
  invalid-authority no-audit behavior, the exact single chained rejection
  event and full field/null profile, byte-equivalent reservation/budget/control
  state after reopen, unchanged proof/context/replay/run-event effect-row
  counts, and the later explicit `settle_runtime_failure` full-forfeit path.

Commands and exact observed outcomes before the stop instruction:

1. `rtk cargo fmt -p proof-storage` — completed successfully; mechanically
   formatted owned files.
2. `rtk cargo fmt --check -p proof-storage && rtk cargo test -p proof-storage`
   — format check passed and the scoped test command ran; test result failed
   with 103 passed and 1 failed. The new test stopped at the first
   `claim_run_lease` audit insert with `OperatorStoreError::Unavailable`.
3. `rtk cargo test -p proof-storage
   dispatching_pre_dispatch_release_commits_only_rejection_then_requires_full_forfeit
   -- --nocapture` — focused retry failed, 0 passed / 1 failed / 103 filtered;
   the token-filtered output retained the same unavailable failure.
4. A third command did begin before interruption:
   `rtk proxy cargo test -p proof-storage
   dispatching_pre_dispatch_release_commits_only_rejection_then_requires_full_forfeit
   -- --nocapture` — failed and exposed the exact SQLite schema-14 mismatch:
   indexed `occurred_at` used `+00:00` while strict `event_json.occurred_at`
   used canonical `Z`.
5. After the canonical audit-time correction, the same focused proxy command
   failed at reservation setup with `OperatorStoreError::Corrupt`. A bounded
   diagnostic assertion localized this to legacy agent helper JSON not being
   the strict canonical JSON required by operator exact loaders.
6. After normalizing only those deterministic test setup rows, the same
   focused proxy command passed: 1 passed, 0 failed, 103 filtered. This command
   also began and completed before the stop instruction. It is recorded for
   completeness but does not restore the exhausted task-local retry budget or
   constitute the required final scoped gate.

Both source/test corrections were already made before the stop instruction:
the canonical audit timestamp encoding, and strict canonical JSON for the
test's operator-governed baseline rows. No further source, format, or test
command ran after the stop instruction. Reverse impact was never run.

Current raw SHA-256 values from the last read-only custody check are:

- `crates/proof-storage/src/sqlite/operator_store.rs`:
  `5fc1dc130c2f7e0dbbaa3653d846e2d7b84929638964fc20c14a991471c20093`
- `crates/proof-storage/src/sqlite/operator_store_tests.rs`:
  `c113d8b68dc71ec60e59b031c85b0bded7d92bac3b63502dd0b8e5bede90fcd2`
- unchanged `migration_14_up.sql`:
  `f4f4fcd6c0a15702e1bf8cc04f64d86b237a7f95e36d48f351fc6e606e06e1ef`
- unchanged `migration_14_down.sql`:
  `e4dd549e5a160840f6c2b70b4cb7d534f5c799984ed79ea8eb47736a21c9b10b`
- unchanged `migrations.rs`:
  `69b219a3dc6a0a757af68685de055221f2a5a62b64b84c5b70244f5edb83c800`

Remaining exact mismatch/blocker: no known ordinal-078 behavioral mismatch
remains in the last focused observation, but the corrected candidate has not
received a post-correction `rtk cargo fmt --check -p proof-storage` plus full
`rtk cargo test -p proof-storage` pass. The one-retry ceiling forbids this
worker from obtaining that acceptance evidence without new owner direction.
External/provider/tool effects and live spend remained zero; no real product
database or repository-root `.proof` workspace was used.

## D-E0002-092 verification-only continuation

- The product owner authorized the exact D-E0002-090 continuation. Source is
  pinned before verification at `operator_store.rs` `5fc1dc13...0093` and
  `operator_store_tests.rs` `c113d8b6...fcd2`.
- No source or test edit is authorized. Run exactly the post-correction
  `rtk cargo fmt --check -p proof-storage` gate and, only on PASS, the full
  `rtk cargo test -p proof-storage` gate. Any failure stops without retry.
- On both passes, stop and return custody/results to the orchestrator. Reverse
  impact and fresh independent review remain quiescent orchestrator gates.

### D-E0002-092 verification result

- Result: `FAIL / stopped`; the authorized format gate failed, so the scoped
  test gate was not started and no retry or further Cargo command ran.
- The pre-gate custody command
  `rtk sha256sum crates/proof-storage/src/sqlite/operator_store.rs crates/proof-storage/src/sqlite/operator_store_tests.rs`
  passed and confirmed the pinned raw SHA-256 values exactly:
  `operator_store.rs`
  `5fc1dc130c2f7e0dbbaa3653d846e2d7b84929638964fc20c14a991471c20093`
  and `operator_store_tests.rs`
  `c113d8b68dc71ec60e59b031c85b0bded7d92bac3b63502dd0b8e5bede90fcd2`.
- `rtk cargo fmt --check -p proof-storage` — failed with exit 1. Rustfmt
  reported formatting-only differences in
  `crates/proof-storage/src/sqlite/operator_store_tests.rs`: wrapping the
  `proof_kernel` import group near line 4, the `load_lease` equality assertion
  near line 461, and the `source_control_revision` equality assertion near
  line 474.
- Per the stop-on-failure rule, `rtk cargo test -p proof-storage` was not run.
  No source or test file was edited, and no Cargo diagnosis or retry followed.
- Final read-only custody status for the assigned scope is:
  modified `crates/proof-storage/src/sqlite/operator_store.rs`, untracked
  `crates/proof-storage/src/sqlite/operator_store_tests.rs`, and modified this
  unique handoff. The format check is non-mutating, so the two pinned source
  hashes above remain the verified candidate custody.
- Remaining exact mismatch: the pinned test file is not rustfmt-clean at the
  three reported locations. Clearing it requires a source/test edit, which was
  expressly outside this verification-only continuation. Reverse impact,
  product databases, provider/network/external effects, commit, and push were
  not performed.

## D-E0002-094 mechanical-format-only completion

- Result: `PASS / local-green`; the three authorized rustfmt-only layouts were
  applied to `crates/proof-storage/src/sqlite/operator_store_tests.rs`, and no
  production storage, behavior, migration, or other test content was edited.
- Starting custody was confirmed exactly before the edit:
  `operator_store.rs`
  `5fc1dc130c2f7e0dbbaa3653d846e2d7b84929638964fc20c14a991471c20093`
  and `operator_store_tests.rs`
  `c113d8b68dc71ec60e59b031c85b0bded7d92bac3b63502dd0b8e5bede90fcd2`.
- Exact delta: the `proof_kernel` import group was rewrapped, the
  `load_lease` equality assertion was rewrapped, and the
  `source_control_revision` equality assertion was rewrapped. The edit used
  `apply_patch`; no broad formatting command mutated files. Immediate excerpt,
  hash, and scoped-status checks proved the production file remained pinned
  and only the authorized test layouts changed.
- `rtk cargo fmt --check -p proof-storage` — passed, exit 0, no output.
- `rtk cargo test -p proof-storage` — passed, exit 0: 141 tests across 8
  suites in 2.42 seconds.
- Final raw SHA-256 custody is `operator_store.rs`
  `5fc1dc130c2f7e0dbbaa3653d846e2d7b84929638964fc20c14a991471c20093`
  and `operator_store_tests.rs`
  `31ec8f21cdb2846f5cf76d4b80ea0e16136e48b72ede7e2d70698a22139e180b`.
- Final scoped status is modified
  `crates/proof-storage/src/sqlite/operator_store.rs`, untracked
  `crates/proof-storage/src/sqlite/operator_store_tests.rs`, and modified this
  unique handoff. Reverse impact remains for the quiescent orchestrator gate.
  No product database, provider/network/external effect, commit, or push
  occurred.

## D-E0002-094 mechanical-format continuation

- The product owner authorized only rustfmt's three D-E0002-093-reported
  layouts in `operator_store_tests.rs`; no behavioral/storage/migration edit.
- Starting hashes remain `operator_store.rs` `5fc1dc13...0093` and test file
  `c113d8b6...fcd2`. After the mechanical edit, inspect exact custody, run one
  format check and one full scoped storage suite. Any failure stops without
  retry; reverse impact remains deferred until both W9 writers stop.

### D-E0002-094 quiescent reverse-impact evidence

- After both shifted-W9 writers stopped, the orchestrator confirmed the
  `proof-storage` impact set with
  `rtk scripts/test-scoped.sh proof-storage --list`: `proof-storage`,
  `proof-transport-cli`, `proof-transport-http`, `proof-transport-mcp`, and
  `proof-transport-ws`.
- `rtk scripts/test-scoped.sh proof-storage` passed: 332 tests across 23
  suites in 9.10 seconds. The command ran with host permissions because the
  restricted sandbox cannot satisfy the workspace's existing UID/root-owned
  artifact and loopback-socket requirements; no source edit occurred between
  the local and reverse-impact gates.
- Post-impact custody remained exact: `operator_store.rs`
  `5fc1dc130c2f7e0dbbaa3653d846e2d7b84929638964fc20c14a991471c20093`
  and `operator_store_tests.rs`
  `31ec8f21cdb2846f5cf76d4b80ea0e16136e48b72ede7e2d70698a22139e180b`.
  Migration custody remained unchanged: `migrations.rs`
  `69b219a3dc6a0a757af68685de055221f2a5a62b64b84c5b70244f5edb83c800`,
  migration 14 up
  `f4f4fcd6c0a15702e1bf8cc04f64d86b237a7f95e36d48f351fc6e606e06e1ef`,
  and migration 14 down
  `e4dd549e5a160840f6c2b70b4cb7d534f5c799984ed79ea8eb47736a21c9b10b`.
- `rtk git diff --check` and
  `rtk scripts/swarm.sh validate AXP-E0002` passed after the impact run.
  Automated local and impact gates are green; wholly fresh independent review
  remains the final task-acceptance gate.

## D-E0002-095 independent acceptance

- A wholly fresh read-only storage reviewer returned `PASS`. It independently
  reproduced the two final source hashes and all three unchanged migration
  hashes, found no storage scope bleed, and confirmed the root/control/Gate-B
  custody remained exact.
- The reviewer traced ordinal 078 through the actual implementation and
  on-disk schema-14 test. It confirmed authority precedes target access and
  event mutation; only `ReleasePreDispatch` is accepted; the dispatch pointer
  must match; exactly one chained `BudgetRejected` / `Rejected` event commits
  before `NotActionable`; reservation, budget, control, and unrelated effect
  rows remain unchanged across reopen; and later explicit runtime-failure
  settlement performs the required full forfeit.
- The reviewer also confirmed canonical UTC `Z` encoding for the indexed audit
  time and that transaction errors roll back while the intended rejection
  branch explicitly commits. It reproduced the five-crate impact set,
  `rtk git diff --check`, and edition validation without running Cargo/tests.
- Review activity was read-only and performed no edit, commit, push, network,
  provider, product-database, or external effect. With 141 local tests across
  8 suites and 332 impact tests across 23 suites already passing, E0002-06 is
  accepted and done under D-E0002-095.
