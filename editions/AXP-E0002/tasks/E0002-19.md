# E0002-19 Task Packet

- Edition/task: `AXP-E0002 / E0002-19`
- Wave: W7
- Model/reasoning: `gpt-5.6-luna` / medium
- Budget: one authorized attempt, 45 minutes and 25,000 combined model tokens;
  zero live/provider spend and no automatic retry
- Owner: e0002-fixtures
- Status/dispatch: done under D-E0002-086 after the product owner accepted the
  exact Gate-B v3 digest; no implementation dispatch is active
- Risk and owner gate: mechanical derived-data propagation with no policy
  judgment; explicit product-owner Gate-B acceptance follows three fresh
  independent reviews
- Dependencies: historical E0002-12 and additive E0002-18 done

## Objective

Mechanically propagate E0002-18's exact evaluator semantics and policy hash
through only D-E0002-070's derived 158-file fixture closure while preserving
all IDs, ordinals, counts, seeds, baselines, unrelated recipes, and terminal
byte policy.

## Writable paths (exclusive)

- `evals/fixtures/operator-control/**`
- `editions/AXP-E0002/handoffs/E0002-19.md`

## Read-only references

- `AGENTS.md`, D-E0002-068 through D-E0002-071, E0002-18 task/handoff and exact
  freeze signal, frozen contract/schemas/manifest/evaluator, and historical
  E0002-12/E0002-17 handoffs

## Non-goals

- Contract, schema, manifest, evaluator, source, migration, Cargo, dependency,
  policy, seed, ID, ordinal, count, browser, provider, external-effect, Gate-B
  acceptance, Gate-C, commit, push, or release work.

## Acceptance

- Change exactly 36 recipe documents: the 28 existing assembly-bound setup
  documents plus ordinal 067 expected, ordinal 078 expected, and ordinals
  103/105 mutation, action, and expected documents. No other recipe changes.
- Refresh all 121 wrapper `policy_sha256` values and every resulting recipe and
  envelope link, then refresh only the index links. The total changed fixture
  inventory is exactly 158 files.
- Preserve the 28 already-authorized setup terminal LFs. The eight direct
  documents, every wrapper, and the index retain no final LF.
- Direct case hashes reproduce ordinal 067
  `sha256:cfe7cb112ec6a0dcf078e1b58f7ded2662a3e7b1a986faf6bfe0411a896cd1f7`,
  ordinal 078
  `sha256:40a43f4ba59ce201b24a61098b26af16f7b4c9b4f6c862d27a90e5dfb19b7136`,
  ordinal 103
  `sha256:0bbdb382be855c671e9d76dc50e29edf8527892e4626db8bf8fe9e0d3265cf0e`,
  and ordinal 105
  `sha256:b898ef56937c4a0733da8f81740cd524d4c1fef602ac0b2747f384757778dd95`.
- The fixture index raw SHA-256 reproduces
  `sha256:0380cbb60d84b44765b19ee86e4559bc2b56b84494d1f23bd8b5661b2d09b991`.
- Strict fixture schemas, exact order/cardinality, blueprint correspondence,
  every document/envelope/index digest link, all 121 deterministic seeds,
  secret-sentinel scan, exact file/byte custody, edition validation, and
  `rtk git diff --check` pass without Cargo.
- After local PASS, enter review. Three fresh independent read-only reviewers
  must separately pass schema/manifest/packet closure, evaluator/fixture/byte
  custody, and command/audit/kernel-source alignment. Only a later explicit
  digest-bound owner Gate-B acceptance may mark E0002-19 done.

## Stop and escalate

Stop on any scope, byte-custody, schema, order, count, blueprint, digest, seed,
secret, terminal-byte, edition, or diff-check failure. Do not retry, make a
policy choice, accept Gate B, run Cargo, or dispatch E0002-20.

## Handoff

Record exact changed fixtures, commands/results, custody evidence, hashes,
assumptions, risks, and cross-owner requests in `handoffs/E0002-19.md` without
secrets.
