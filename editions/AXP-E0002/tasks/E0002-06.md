# E0002-06 Task Packet

- Edition/task: `AXP-E0002 / E0002-06`
- Wave: W9 (shifted from historical W4 by D-E0002-041 and from W6 by
  D-E0002-071)
- Model/reasoning: `gpt-5.6-sol` / high
- Budget: 120 minutes and 80,000 combined model tokens per attempt; one retry
  maximum; zero live/provider spend
- Owner: e0002-storage
- Status/dispatch: done under D-E0002-095 after the exact D-E0002-094
  mechanical continuation, local and quiescent impact gates, exact custody,
  and fresh independent review all passed
- Risk and owner gate: critical migration/concurrency; frozen Gate B contract
- Dependencies: E0002-05, E0002-19, and E0002-20 done and quiescent; dated
  Gate-B v3 acceptance; historical W4 barrier signals remain byte-exact
  evidence

## Objective

Append Gate-B-bound SQLite migration 14 and persist filtered multi-run
projections, an atomic append-only operator command/audit ledger, durable fenced
ownership, and aggregate budget accounting.

## Writable paths (exclusive)

- `crates/proof-storage/**`
- `editions/AXP-E0002/handoffs/E0002-06.md`

## Read-only references

- `AGENTS.md`, `contracts/kernel-api.md`, frozen operator contract/schemas
- E0002-05 handoff and current immutable migration list

## Non-goals

- Editing an existing migration, kernel/runtime/auth/transport/UI/contracts,
  auto-resume, provider/tool execution, or external data.

## Acceptance

- Exactly migration 14 is appended after confirming 13 remains current; fresh,
  upgraded, and reopened paths pass. Exact down SQL round-trips only against a
  disposable database; public rollback rejects crossing below 14, and the
  owner-directed manual rollback limitations are documented without adding a
  product authorization/receipt API.
- The store opens only the Gate-B-frozen authoritative workspace database via
  the trusted-open policy; tests reject alternate/split transport paths and do
  not use the repository-root `.proof` workspace.
- The operator-capable opener requires the kernel environment trait and derives
  every store-owned time/UUID internally; fixed-clock/seeded tests cover exact
  equality boundaries without accepting caller transition times or IDs.
- Lease and dispatch proofs use the kernel-owned exact 32-byte constant-work
  comparison helper before target-state access; ordinary equality and a
  storage-local crypto dependency are forbidden.
- The opener also requires the workspace-digest-matched immutable schema
  catalog. Completed replay strict-decodes/validates result bytes and verifies
  the full Proof against the persisted actor inside the same read transaction;
  the catalog callback is read-only, non-reentrant, and performs no I/O.
- Storage provides the nonforgeable `OwnedOperatorWorkspaceLock` constructor
  and guarded provision/upgrade/initialize/reopen lifecycle. One descriptor and
  BSD flock span schema inspection, migration, policy initialization, final
  verification, database close, and release; guarded APIs borrow `&mut` and
  never reopen or reacquire the lock.
- Its exact lock constructor accepts the workspace path, optional provision
  path, a nonempty forbidden-`.proof` path slice, and lock mode. Storage
  descriptor-opens and retains every forbidden directory through comparison
  with the selected open `.proof` descriptor before database/key access; it
  accepts no caller-certified device/inode or trust boolean.
- Guarded initialization accepts only stable provision/catalog inputs plus the
  shared environment. It generates IDs/times only for an absent policy; an
  exact-existing retry reads and verifies persisted IDs/times without calling
  clock or entropy.
- Wait for E0002-11's `W4 root ready` signal, add only
  `rustix.workspace = true` to the storage package manifest, run no Cargo
  command or source edit, freeze that manifest, and signal
  `W4 storage manifest frozen`.
  The guarded offline schema-14 upgrader verifies and borrows the already-held
  lock whose constructor acquired exactly the same nonblocking BSD `flock`
  operation used by the control launcher; it never acquires a second lock. Do
  not edit the root manifest or lockfile.
- Auth-filter inputs produce redacted attention/run/approval/audit projections;
  keyset pagination under concurrent inserts has no skip/duplicate and cursors
  cannot cross filters, workspace, instance, Human, or capability scope.
- Command claim/commit/replay/conflict is atomic and append-only. Deterministic
  barriers cover cancel/complete, resume/cancel, lease/reclaim, stale fence,
  process restart, and aggregate-budget contention with one winner.
- Evaluator ordinal 078 is an explicit storage-owned acceptance obligation.
  After authority is proven, calling `settle_budget_reservation` with
  `ReleasePreDispatch` against a `dispatching` reservation must preserve the
  reservation, budget, and control state; append and commit exactly one
  chained `budget_rejected` / `rejected` audit event for that reservation in
  the same transaction; and then return `OperatorStoreError::NotActionable`,
  observed by the runtime at `settle_budget`. Invalid/unproven authority still
  appends nothing. A deterministic round-trip test must prove the exact event,
  unchanged settlement state, later explicit full-forfeit requirement, and
  zero provider/tool/external effect.
- Every new storage function has a reopen round-trip integration test.
- Guarded lifecycle tests cover exclusive creation, safe-existing retry,
  self-contention, schema-13 upgrade, schema-14-without-policy recovery,
  exact-existing idempotence, provision mismatch, and movement/sidecar failure.
- Disposable down-SQL tests populate a source reservation, recovery directive,
  and linked recovery reservation before proving the one-way FK graph drops in
  child-before-parent order; empty-only rollback evidence is insufficient.
- `load_completed_replay` round-trips an exact completed result after account
  deadline and after full five-dimensional exhaustion, and fails closed on
  malformed result, catalog substitution, actor substitution, or invalid Proof.
- Wait for E0002-11's later `W4 lock stable` signal before any source edit or
  Cargo command. Local storage format/unit/integration checks may then run, but
`rtk scripts/test-scoped.sh proof-storage` waits until every shifted W9 writer is
  quiescent so it cannot compile a peer's partial source.
- `rtk cargo fmt --check -p proof-storage`,
  `rtk cargo test -p proof-storage`, and
  `rtk scripts/test-scoped.sh proof-storage` pass.

## Stop and escalate

Stop on migration-number conflict, frozen shape drift, missing kernel API,
another crate edit, destructive cleanup, external effect, or one failed retry.
Never edit or duplicate an existing migration.

## Handoff

Record migration number, files, round-trip/race commands and results,
assumptions, rollback limits, and cross-owner requests in
`handoffs/E0002-06.md`.
