# E0002-11 Task Packet

- Edition/task: `AXP-E0002 / E0002-11`
- Wave: W9 (shifted from historical W4 by D-E0002-041 and from W6 by
  D-E0002-071)
- Model/reasoning: `gpt-5.6-sol` / high
- Budget: 120 minutes and 80,000 combined model tokens per attempt; one retry
  maximum; zero live/provider spend
- Owner: e0002-control
- Status/dispatch: done under D-E0002-091 after unchanged-candidate custody,
  scoped and quiescent impact tests, and fresh independent security review
- Risk and owner gate: critical Human authentication transport, listener,
  secret delivery, root manifests, and shutdown; frozen Gate B contract
- Dependencies: E0002-05, E0002-08, E0002-19, and E0002-20 done and
  quiescent; dated Gate-B v3 acceptance; frozen kernel store, signer, cursor,
  auth/session, and router APIs

## Objective

Implement the dedicated loopback operator-control security shell: launcher/
listener, fresh enrolled-Human signed-challenge session establishment,
same-origin API/UI assembly boundary, explicit session revoke, and fail-closed
shutdown. Runtime and SQLite composition/restart evidence belongs to E0002-14.
This task owns north-star journey step 1 and control-plane restart behavior.

## Writable paths (exclusive)

- `Cargo.toml`
- `Cargo.lock`
- `crates/proof-operator-control/**`
- `editions/AXP-E0002/handoffs/E0002-11.md`

## Read-only references

- `AGENTS.md`, frozen operator contract/schemas/evaluator
- E0002-05 kernel API/handoff, E0002-08 auth API/handoff, and frozen synthetic
  router/store-opener adapter shapes; no E0002-06/07 source or handoff
  dependency
- Existing loopback/security-header/process-test patterns as read-only input
- E0006 records only for prohibited reuse and known failure modes

## Non-goals

- Reusing E0006 source/bootstrap/session, wildcard/remote binding, TLS/proxy,
  cookies/CORS/Web Storage, durable session recovery, read/mutation handler
  implementation owned by E0002-02/E0002-09, final UI owned by E0002-03, live
  provider/tool work, auto-resume, or external effect.

## Acceptance

- The process binds only exact IPv4 loopback, emits one clean non-secret URL,
  places no credential in URL/argv/output/environment/log, and shuts down before
  serving if workspace, listener, randomness, Human identity, or auth state is
  unsafe.
- Own the final nonconfigurable OS OperatorControlEnvironment implementation
  and pass one instance through auth and synthetic store-opener composition;
  clock/entropy failure is fatal before authority publication.
- Its injected store-opener interface accepts only the Gate-B-frozen
  authoritative workspace path, exact nonempty forbidden-`.proof` path input,
  and trusted-open result; it rejects the repository-root `.proof` identity
  and never generates an ephemeral replacement signer. The released interface
  has no caller-controlled forbidden-path/trust option; final assembly supplies
  the frozen non-user-controlled build anchor without a CWD/environment/argv
  fallback. Real SQLite opening/composition is E0002-14 evidence; this shell
  does not depend on in-progress W4 storage source.
- The Gate-B-frozen signed-challenge state machine has one-use nonce issuance,
  exact Human/workspace/instance/requested-and-granted-capability/expiry binding,
  signature verification, replay resistance, generic failures, and one session
  publication winner. It imports no E0006 authority.
- Session establishment, protected routing, self-revoke, and app delivery share
  one exact origin. Mutations require exact Host, Origin, and JSON media; all
  responses have the frozen cache/content/frame/referrer/CSP boundary with no
  permissive CORS, cookie, service worker, or remote resource.
- A stable injected-router/static-source assembly interface is proven at W4
  with synthetic no-authority fixtures and without placing authority in HTML or
  static assets. W4 does not claim the downstream E0002-02/09 routes or
  E0002-03 app exist; E0002-15 owns their later real source composition before
  E0002-04 verification and E0002-10 integration.
- The synthetic router enumerates its exact mounted paths and exposes no legacy
  unauthenticated audit, proof, domain-list, or generic operation route.
  Forwarded-client headers are not trusted as identity or rate-limit authority.
- Control-plane restart/shutdown invalidates every volatile session and requires
  a fresh signed challenge. E0002-14 proves runtime-worker restart continuity
  while this process remains live.
- Deterministic process/Tower tests cover port/bind failure, concurrent/replayed/
  malformed/expired challenges, lost response, revoke/expiry, page reload,
  control-plane restart, SIGINT/SIGTERM, and zero protected callback/provider/
  tool/external effect on failure. Runtime-worker restart is E0002-14 evidence.
- Only Gate-B-approved dependencies change root manifests; the control package
  uses the already-approved `sha2.workspace = true` to recompute embedded
  static-bundle hashes before bind and directly declares base64, blake3,
  chrono, ed25519-dalek, uuid/serde, and the other exact contract dependencies
  needed by its environment, cursor, and signer implementations. Every public
  function has a unit test.
- The root rustix entry is exactly version `=1.1.4` with features
  `fs`, `process`, and `termios`; `process` supplies the effective-user
  identity check and no transitive/default feature is assumed.
- Before product source fan-out, write only the root rustix/member entries,
  complete control package manifest, and the Gate-B-exact inert `src/lib.rs`
  scaffold required to give Cargo a target; define no item or behavior and run
  no Cargo, then signal `W4 root ready`. After E0002-06 signals
  `W4 storage manifest frozen`, reconcile Cargo.lock alone with the exact
  frozen control-plus-storage offline check and signal `W4 lock stable`; only
  then may shifted W9 owners resume source edits or run Cargo only under a new
  owner continuation. Reverse-impact checks wait for all shifted W9 writers to
  quiesce.
- `rtk cargo fmt --check -p proof-operator-control`,
  `rtk cargo test -p proof-operator-control`, and
  `rtk scripts/test-scoped.sh proof-operator-control` pass.

## Stop and escalate

Stop on any E0006 code/authority reuse, unspecified credential delivery,
wildcard/remote exposure, persistence, transport/auth contract drift, another
crate edit, new unapproved dependency, secret exposure, provider/external
effect, or one failed retry.

## Handoff

Record root/crate files, exact process/Tower/scoped commands and results,
dependency delta, session/restart/shutdown evidence, assumptions, threat limits,
and cross-owner requests in `handoffs/E0002-11.md`; never record a credential.
