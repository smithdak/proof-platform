# E0002-17 Task Packet

- Edition/task: `AXP-E0002 / E0002-17`
- Wave: W5
- Model/reasoning: `gpt-5.6-luna` / medium
- Budget: one authorized attempt, 45 minutes and 25,000 combined model tokens;
  zero live/provider spend and no automatic retry
- Owner: e0002-fixtures
- Status/dispatch: blocked under D-E0002-056 after partial propagation failed
  the first fixture validation at `valid_control_plane_restart` setup; no retry
  is authorized
- Risk and owner gate: mechanical derived-data propagation; no policy judgment;
  explicit product-owner Gate-B acceptance required after three fresh reviews
- Dependencies: historical E0002-12 and additive E0002-16 done

## Objective

Mechanically propagate the E0002-16 manifest and evaluator hashes through only
the exact D-E0002-037 fixture closure, preserving all scenario semantics.

## Writable paths (exclusive)

- `evals/fixtures/operator-control/**`
- `editions/AXP-E0002/handoffs/E0002-17.md`

## Read-only references

- `AGENTS.md`, D-E0002-037..041, E0002-16 task/handoff, frozen contract,
  schemas, manifest, evaluator, and historical E0002-12 handoff

## Non-goals

- Contract, schema, manifest, evaluator, source, migration, Cargo, dependency,
  policy, seed, ID, ordinal, count, action, mutation, expected-result, browser,
  provider, or external-effect changes.

## Acceptance

- Refresh exactly the 28 assembly-bound setup documents: valid ordinals 010
  and 014; rejection ordinals 001-010, 038-045, 081, 084-088, 096, and 104.
  Refresh only their derived setup/envelope/case hash links.
- Refresh all 121 wrapper `policy_sha256` values and every resulting index
  envelope link. Preserve every fixture ID, ordinal, count, seed, and policy
  meaning.
- For runtime stale-fence rejection ordinals 067, 068, 069, 103, and 105, only
  the wrapper policy hash may change; setup, action, mutation, expected bytes,
  case semantics, and empty `new_events` remain byte-identical.
- Every non-enumerated setup and every action/mutation/expected document remains
  byte-identical. The new 28 setup blueprints exactly match E0002-16's embedded
  evaluator blueprints.
- Strict fixture schemas, exact order/cardinality, blueprint correspondence,
  every document/envelope/index digest link, all 121 deterministic seeds,
  secret-sentinel scan, edition validation, and `rtk git diff --check` pass.
- After PASS, enter review rather than done. Only three fresh independent PASS
  reviews plus explicit digest-bound product-owner acceptance may mark this task
  done or make shifted implementation eligible.

## Stop and escalate

Stop on any scope, byte-custody, schema, order, count, blueprint, digest, seed,
secret-sentinel, edition, or diff-check failure. Do not retry automatically or
make a policy choice.

## Handoff

Record exact changed fixtures, commands/results, custody evidence, hashes,
assumptions, risks, and cross-owner requests in `handoffs/E0002-17.md` without
secrets.
