# E0002-18 Task Packet

- Edition/task: `AXP-E0002 / E0002-18`
- Wave: W6
- Model/reasoning: `gpt-5.6-sol` / high
- Budget: one authorized attempt, 120 minutes and 80,000 combined model tokens;
  zero live/provider spend and no automatic retry
- Owner: orchestrator
- Status/dispatch: done under D-E0002-080 after exact artifact, semantic,
  packet, projection, custody, edition, and diff validation
- Risk and owner gate: critical public schema/evaluator repair; a reproduced
  Gate-B v3 packet requires explicit product-owner acceptance
- Dependencies: historical E0002-12 and E0002-16 done and quiescent

## Objective

Apply only D-E0002-070's exact planning-artifact correction: make the four
submitted-command audit fence fields positive, correct runtime rejection
ordinals 067/078/103/105, close every derived manifest/evaluator digest, and
stage the exact pending-owner Gate-B v3 packet without changing product source
or fixtures.

## Writable paths (exclusive)

- `schemas/operator-control/reads-v1.schema.json`
- `schemas/operator-control/manifest-v1.json`
- `evals/operator-control-v1.json`
- `editions/AXP-E0002/assignments.tsv`
- `editions/AXP-E0002/decisions.md`
- `editions/AXP-E0002/evidence.md`
- `editions/AXP-E0002/handoffs/E0002-18.md`
- `editions/AXP-E0002/status.md`
- `editions/AXP-E0002/tasks/E0002-18.md`
- `editions/AXP-E0002/tasks/E0002-19.md`
- `editions/AXP-E0002/workgraph.md`

## Read-only references

- `AGENTS.md`, `contracts/axp-experience.md`,
  `contracts/operator-control-plane.md`, D-E0002-068 through D-E0002-071,
  E0002-16/E0002-17 task packets and handoffs, and all non-owned Gate-B
  artifacts
- The complete fixture corpus, kernel/storage/runtime/control source, migration
  14, Cargo manifests and lockfile, and repository-external validators

## Non-goals

- Contract, fixture, kernel, storage, runtime, control, HTTP, UI, migration,
  dependency, Cargo, browser, provider, external-effect, Gate-B acceptance,
  Gate-C, commit, push, or release work.
- Any new event, route, capability, policy, fixture ID/count/order, seed,
  command profile, or change outside D-E0002-070's exact closure.

## Acceptance

- Exactly four reads-schema references change from `SafeInteger` to
  `PositiveSafeInteger`: `AuditEvent` oneOf branches 16 through 19 at
  `properties/fence_epoch`. Every other schema leaf remains identical.
- The manifest changes only the reads-schema raw hash to
  `sha256:1d02cf888c00dcc024fae2ed930e227aa11fdb99b32ec1d498457f7317647542`
  and reproduces raw SHA-256
  `sha256:1ffc871f4478732ba4511e6ae413c30cdfb3cdf588a60bc03de5479dcdfd176c`.
- The evaluator changes exactly 107 JSON leaves: its reads/manifest artifact
  hashes, the 28 existing assembly-bound manifest links and derived hashes,
  runtime ordinals 067/078/103/105 as specified in D-E0002-070, and the two
  affected semantic digests. It produces exactly 32 changed case hashes.
- Runtime ordinal 067 observes `claim_lease` / `not_actionable`; ordinal 078
  observes `settle_budget` / `not_actionable` and retains its required
  `budget_rejected` audit; ordinals 103/105 use `begin_dispatch` with a
  `BeginDispatchRequest`, mutate only the recorded authority token/owner path,
  observe `begin_dispatch` / `stale_fence`, and retain no new audit events.
- Valid-scenario semantic SHA-256 is
  `sha256:240d33818aaeb9fe6bf89bba0ba7393bca71cb0ec41174a95040fe73916982fd`;
  rejection-vector semantic SHA-256 is
  `sha256:388ddb7eefa13f831fbf149e77438e73715a8e00aa4f1ddf297b4486a694d55a`;
  evaluator raw SHA-256 is
  `sha256:fec4fa13f723ee47a2a28d6048ac1e8d1a80dab1f0f599040da08348d4b3830e`.
- Gate-B v3 names E0002-18, remains `pending_owner`, keeps
  `implementation_dispatch: false`, differs from v2 at exactly task, artifact
  ordinals 4/10/11, and the valid/rejection semantic digests, and reproduces
  canonical SHA-256
  `sha256:25f64ff58615d495abbe4b1058bf670b11ec6d527485658d8d5863950dc16eb2`.
- Duplicate-aware parsing, all eight Draft 2020-12 schema validations,
  manifest/reference/hash closure, evaluator semantic closure, exact unchanged
  custody, fixture projection validation, edition validation, and
  `rtk git diff --check` pass without Cargo.
- On exact PASS, record `Gate B semantic repair artifacts frozen`, mark
  E0002-18 done, and activate only E0002-19. Gate B and every source task remain
  closed.

## Stop and escalate

Stop on any scope, parse, schema, reference, semantic, digest, predicted-value,
custody, fixture-projection, edition, or diff-check mismatch. Do not retry,
guess policy, touch fixtures or source, accept Gate B, or dispatch E0002-20.

## Handoff

Record exact changed paths, JSON-leaf closure, commands/results, raw and
semantic hashes, packet digest, custody evidence, assumptions, risks, and
cross-owner requests in `handoffs/E0002-18.md` without secrets.
