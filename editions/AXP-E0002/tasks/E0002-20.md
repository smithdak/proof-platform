# E0002-20 Task Packet

- Edition/task: `AXP-E0002 / E0002-20`
- Wave: W8
- Model/reasoning: `gpt-5.6-sol` / high
- Budget: one authorized attempt, 120 minutes and 80,000 combined model tokens;
  zero live/provider spend and no automatic retry
- Owner: e0002-kernel
- Status/dispatch: done under D-E0002-088 after exact four-profile kernel
  validation, focused tests, reverse impact, custody, and independent review
- Risk and owner gate: critical command/audit validation source; bounded by the
  accepted Gate-B v3 packet
- Dependencies: historical E0002-05 and additive E0002-18/E0002-19 done

## Objective

Align `AuditEvent::validate_chain_link` and its focused tests with the four
Gate-B-bound command-attributed `stale_fence_rejected` profiles, replacing the
obsolete runtime-shaped mask without changing public types or downstream
source.

## Writable paths (exclusive)

- `crates/proof-kernel/**`
- `editions/AXP-E0002/handoffs/E0002-20.md`

## Read-only references

- `AGENTS.md`, `contracts/axp-experience.md`,
  `contracts/kernel-api.md`, `contracts/operator-control-plane.md`, the accepted
  Gate-B v3 packet, D-E0002-068 through D-E0002-071, and E0002-05/E0002-18/
  E0002-19 task packets and handoffs
- Every non-kernel source crate, fixture, schema, evaluator, migration, Cargo
  manifest, lockfile, and edition record other than the unique handoff

## Non-goals

- Any public type, event kind, error, Cargo, schema, evaluator, fixture,
  storage, runtime, control, HTTP, UI, migration, dependency, provider,
  external-effect, Gate-C, commit, push, or release change.

## Acceptance

- `StaleFenceRejected` accepts only a rejected event with nonzero
  `fence_epoch` and one correlated `(command_kind, presence_mask)` pair:
  `(approval_decide, 0x101e3)`, `(run_cancel, 0x101a3)`,
  `(run_resume, 0x2101e3)`, or `(run_resume, 0x4181a3)`.
- Validation rejects obsolete `0x16c30`, zero fence, mismatched command kind,
  and every other mask. Zero-fence rejection is scoped to
  `StaleFenceRejected`; unrelated audit profiles remain unchanged.
- The existing frozen-profile test covers all four accepted tuples plus
  explicit obsolete-mask, zero-fence, and wrong-command-kind rejection.
- `rtk cargo fmt --check -p proof-kernel`,
  `rtk cargo test -p proof-kernel`, and, after every affected writer quiesces,
  `rtk scripts/test-scoped.sh proof-kernel` pass. No bare workspace test runs.
- Exact source custody, edition validation, `rtk git diff --check`, and a fresh
  independent read-only review pass before completion.

## Stop and escalate

Stop on any cross-owner need, public-contract/API change, test or custody
failure, unexpected reverse impact, or mismatch with the accepted v3 packet.
Do not retry automatically or edit downstream crates.

## Handoff

Record changed files, exact commands/results, accepted/rejected profile
coverage, custody evidence, assumptions, risks, and cross-owner requests in
`handoffs/E0002-20.md` without secrets.
