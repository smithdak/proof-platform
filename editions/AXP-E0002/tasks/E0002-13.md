# E0002-13 Task Packet

- Edition/task: `AXP-E0002 / E0002-13`
- Wave: W2
- Model/reasoning: `gpt-5.6-sol` / high
- Owner: orchestrator
- Status/dispatch: blocked and non-dispatchable until product-owner Gate A
- Risk and owner gate: critical public contract/security; exact Gate B required
  before implementation fan-out
- Dependencies: E0002-01 done plus dated Gate A approval

## Objective

Freeze the independent operator authority, projections, commands, pagination,
audit, recovery, budgets, API shapes, and all-required evaluator. Present exact
artifact digests and material-risk choices for Gate B without implementing the
product.

## Writable paths (exclusive)

- `contracts/operator-control-plane.md`
- `schemas/operator-control/**`
- `evals/operator-control-v1.json`
- `editions/AXP-E0002/assignments.tsv`
- `editions/AXP-E0002/charter.md`
- `editions/AXP-E0002/decisions.md`
- `editions/AXP-E0002/evidence.md`
- `editions/AXP-E0002/handoffs/E0002-13.md`
- `editions/AXP-E0002/ownership.md`
- `editions/AXP-E0002/status.md`
- `editions/AXP-E0002/tasks/**`
- `editions/AXP-E0002/workgraph.md`

## Read-only references

- `AGENTS.md`, `contracts/axp-experience.md`, `contracts/kernel-api.md`
- E0002 charter, decisions, assignments, and this packet
- Existing kernel/storage/runtime/HTTP/approval contracts and migrations
- E0006 records as failure/design input only, never as released authority

## Non-goals

- Source, migration, manifest, lockfile, product fixture, browser, provider, or
  external-effect work.
- E0006 import/reuse, remote access, auto-resume, bulk approval, or decision
  withdrawal.

## Acceptance

- Strict versioned shapes reject unknown/duplicate fields and bind Human,
  workspace, server instance, capabilities, revisions, filters, cursor, command
  idempotency key, fence epoch, budgets, and audit linkage.
- The auth contract defines a fresh E0002 signed challenge and one-use issuance
  flow rooted in an enrolled Human. It binds nonce, instance, workspace,
  requested/granted capability intersection, expiry, and replay protection; it
  imports no E0006 bootstrap/session authority.
- Exact loopback launcher/listener/startup/shutdown, signed-challenge credential
  delivery, same-origin UI/API assembly, session self-revoke, expiry, and
  control-plane crash/restart behavior are normative rather than left to the
  implementation.
- Auth and capability checks precede enumeration, signing-key load, provider or
  tool dispatch, state mutation, and distinguishable not-found/conflict data.
- Exact capabilities include `run.read`, `approval.read`, `approval.decide`,
  `run.cancel`, `run.resume`, and `audit.read`; session revoke is not conflated
  with run cancel, approval denial, delegation revoke, or decision withdrawal.
- Approval never resumes automatically. Cancel/resume/expiry/recovery races,
  uncertain responses, keyset pagination, append-only events, fenced leases,
  and aggregate budgets have one normative ordering.
- Separate required vectors cover `runtime_worker_restart` while the
  control-plane instance/session remains live and `control_plane_restart`,
  which invalidates all volatile authority and requires fresh authentication.
- Approval signing requires the authenticated Human to match the sealed
  required Human unless Gate B explicitly freezes a narrower delegation policy;
  merely requesting `approval.decide` cannot select another Human's key.
- The evaluator is all-required, has frozen ordered checks and rejection
  vectors, and proves zero unauthorized effect with deterministic fixtures.
- Exact SHA-256 digests, proposed dependency/root-manifest delta, next migration
  plan, rollback, limitations, and owner Gate B accept/reject text are recorded
  in the handoff; no implementation task is activated.
- After drafting, E0002-13 remains `review` and MUST NOT become `done` unless
  the product owner records dated Gate B acceptance of the exact digests and
  material-risk choices. Revision/rejection keeps W3 pending.
- `rtk scripts/swarm.sh validate AXP-E0002`, strict JSON/schema validation, all
  contract fixture checks, digest reproduction, and `rtk git diff --check` pass.

## Stop and escalate

Stop on missing Gate A, an E0006 dependency, remote/persistent credential,
unspecified authority ordering, destructive/external effect, unapproved
dependency/migration, or any source change. Return one exact Gate B request.

## Handoff

Record changed files, commands/results, exact digests, assumptions, risks, and
cross-owner requests in `handoffs/E0002-13.md` without secrets.
