# E0002-14 Task Packet

- Edition/task: `AXP-E0002 / E0002-14`
- Wave: W7 (shifted from W5 by D-E0002-041)
- Model/reasoning: `gpt-5.6-sol` / high
- Budget: 120 minutes and 80,000 combined model tokens per attempt; one retry
  maximum; zero live/provider spend
- Owner: e0002-backend-verifier
- Status/dispatch: pending and non-dispatchable
- Risk and owner gate: critical cross-crate concurrency/recovery integration;
  frozen Gate B contract
- Dependencies: E0002-06, E0002-07, E0002-11, and E0002-12 done and
  quiescent

## Objective

Independently compose the real SQLite store, runtime command/barrier policy,
and loopback control shell. Prove that the parallel W6 implementations satisfy
one durable command, lease/fence, aggregate-budget, and restart contract before
any protected mutation API is allowed to start.

## Writable paths (exclusive)

- `Cargo.lock`
- `conformance/**`
- `editions/AXP-E0002/handoffs/E0002-14.md`

## Read-only references

- `AGENTS.md`, frozen operator contract/schemas/evaluator
- E0002-05/06/07/08/11 handoffs and public APIs
- Existing conformance fixture and deterministic barrier patterns

## Non-goals

- Repairing kernel/storage/runtime/auth/control source, changing public
  contracts or migrations, adding HTTP mutations or UI, using a real provider
  or tool, external effects, E0006 reuse, or Gate C.

## Acceptance

- A disposable trusted workspace uses the Gate-B-frozen authoritative database
  and fresh synthetic Human/Agent identities; repository-root `.proof`, split
  transport paths, and ephemeral signer substitution are rejected.
- Real SQLite restart/reopen tests prove one command claim/commit/replay result,
  one lease owner/reclaim winner, monotonic fence epochs, stale-writer rejection,
  and append-only audit chronology across process boundaries.
- Runtime barriers with deterministic fake providers/tools prove
  cancel-before-dispatch has zero effect, explicit resume has one winner, and
  no provider/tool/result write occurs after authority or fence loss.
- Concurrent runs reserve/commit/release aggregate step, token, time, cost, and
  tool budgets atomically without leak or overrun on failure and recovery.
- Compose one fixed-UTC/advanceable-monotonic/seeded-entropy environment across
  real storage/runtime/control APIs so equality and restart vectors are
  deterministic while store-owned transitions remain internally generated.
- Construct one frozen schema catalog from fixture registry/schema bytes, bind
  its digest during provisioning, and inject the same Arc into storage and
  engine; substitution and invalid persisted replay fail closed.
- With the real SQLite/runtime path, prove exact completed replay remains
  reachable with zero new authority both after the aggregate deadline and
  after all five budget dimensions are exhausted, including the one-relookup
  concurrent-completion race.
- A runtime-worker restart preserves only a still-valid scoped session in the
  unchanged control-plane instance; a separate control-plane restart
  invalidates volatile authority while leaving durable runs intact.
- The control shell composes only the synthetic protected-router interface;
  legacy unauthenticated audit/proof/domain/operation routes remain absent.
- Implement the frozen `evaluate-operator-control` conformance binary under
  `conformance/**` and strictly load the complete E0002-12 index/envelope/recipe
  corpus. Execute twice from clean workspaces the backend-owned valid scenarios
  `valid_runtime_worker_restart`, `valid_aggregate_budget_concurrency`,
  `valid_runtime_dispatch_commit`, and `valid_runtime_dispatch_failure` plus
  the exact ordered rejection-vector list in the frozen `backend_subset`,
  preserving complete failures. Do not
  emit an all-required score or claim the UI/HTTP/real-assembly cases; E0002-04
  owns the first complete scored replay after E0002-15.
- Execute the frozen kernel recording-store matrix twice: all 189 ordered
  boundary/error cells and four typed absence results must produce a complete
  `StoreErrorMatrixResult`. Include that passing result in the unscored
  `BackendSubsetResult`; no cell may be skipped or inferred from another.
- Before either owner edits source, E0002-14 and E0002-02 complete and signal
  only their disjoint package-manifest deltas without running Cargo. With both
  source trees still quiescent, this task exclusively reconciles `Cargo.lock`
  for both deltas with the Gate-B-frozen serialized command and signals the
  lock stable before either owner begins source work or another Cargo command.
  Any later dependency change stops both writers, restores source quiescence,
  and repeats the barrier. Because every conformance Cargo command compiles the
  HTTP crate, E0002-14 runs no post-fan-out Cargo command until E0002-02 signals
  source quiescence; W7 impact tests wait for both writers to quiesce.
- Backend-scoped conformance tests (not a scored evaluator mode), `rtk cargo fmt --check -p
  proof-conformance`, `rtk cargo test -p proof-conformance`,
  `rtk scripts/test-scoped.sh proof-conformance`, and
  `rtk git diff --check` pass.

## Stop and escalate

Stop on any required-vector failure, contract or migration drift, source repair
need outside `conformance/**` and the assigned `Cargo.lock`, alternate
workspace store, stale-fence effect, budget overrun, retained credential,
provider/external effect, or one failed retry. Preserve the exact deterministic
interleaving and return a bounded cross-owner request; do not patch another
crate.

## Handoff

Record conformance files, exact commands/results, restart/race evidence,
assumptions, residual risks, and cross-owner requests in
`handoffs/E0002-14.md` without credentials or copied databases.
