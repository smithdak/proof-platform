# E0002-03 Task Packet

- Edition/task: `AXP-E0002 / E0002-03`
- Wave: W12 (shifted from W7 by D-E0002-041 and from W9 by D-E0002-071)
- Model/reasoning: `gpt-5.6-terra` / high
- Budget: 90 minutes and 50,000 combined model tokens per attempt; one retry
  maximum; zero live/provider spend
- Owner: e0002-ui
- Status/dispatch: pending and non-dispatchable
- Risk and owner gate: high Human-intent/credential UX; frozen Gate B contract
  and completed protected APIs
- Dependencies: E0002-02, E0002-09, and E0002-11 done and quiescent

## Objective

Build an accessible multi-run operator console against only the frozen schemas
and independently authenticated APIs. Make attention, exact consequences,
decision, explicit resume, cancel, uncertain writes, and session loss clear.

## Writable paths (exclusive)

- `apps/operator-console/**`
- `editions/AXP-E0002/handoffs/E0002-03.md`

## Read-only references

- Frozen operator contract/schemas/evaluator
- Completed HTTP handoffs and exact API tests
- `contracts/axp-experience.md` Human-control invariants

## Non-goals

- API/auth/runtime/storage/kernel changes, E0006 UI/code/assets/session reuse,
  bulk approval, auto-resume, persistent credential, remote resources, analytics
  beacons, service workers, provider calls, or external effects.

## Acceptance

- The attention inbox exposes bounded filters, stable pagination, attention
  reason, status, budget pressure, and stale/refresh state without secret fields.
- Run/approval detail shows exact goal, consequence, operation/version,
  canonical arguments, required Human, current authority, revision, budgets,
  attempts, command receipts, evidence, and audit chronology.
- Approve, deny, cancel, explicit resume, and end-session each use distinct
  target/outcome-bound controls. Approval is danger-styled and never resumes;
  stale/actionability loss clears pending intent before a POST.
- Loading, empty, expired, revoked, forbidden, conflict, lost-response, and
  unknown-write states are accessible and recover through protected readback,
  never blind mutation replay.
- Keyboard/focus/labels/live regions/color contrast/reduced motion pass the
  Gate-B-frozen accessibility checks at narrow and wide viewports.
- No credential enters URL, DOM attribute, console, storage, cookie, referrer,
  screenshot/evidence, or remote request. Page reload/memory loss requires a
  new independent operator session.
- The exact Gate-B-approved app format/test/build commands and deterministic
  browser checks pass. The task leaves a reproducible static bundle at
  `apps/operator-console/dist/**`, records its file/digest inventory in the
  handoff, and freezes the exact command E0002-15 may use to regenerate only
  that directory; `rtk git diff --check` passes.

## Stop and escalate

Stop on any API/contract workaround, persistent/remote credential, E0006 reuse,
auto-resume, hidden destructive action, new unapproved dependency, cross-owner
edit, external effect, or one failed retry. Escalate only this task to Sol on a
security or Human-intent evaluation failure.

## Handoff

Record files, exact app/browser commands and results, accessibility evidence,
assumptions, limitations, and cross-owner requests in
`handoffs/E0002-03.md`; never retain a session credential.
