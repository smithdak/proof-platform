# E0002-12 Task Packet

- Edition/task: `AXP-E0002 / E0002-12`
- Wave: W3
- Model/reasoning: `gpt-5.6-luna` / medium
- Budget: 45 minutes and 25,000 combined model tokens per attempt; one retry
  maximum; zero live/provider spend
- Owner: e0002-fixtures
- Status/dispatch: historical task remains done under D-E0002-014; the
  D-E0002-038 derived refresh never became eligible because the E0002-13
  activation failed graph validation before dispatch
- Risk and owner gate: medium mechanical work; frozen artifacts are immutable
- Dependencies: E0002-13 done and dated Gate B approval

## Objective

Materialize the frozen valid/rejection operator-control fixture index,
envelopes, and recipe documents without making security, harness, or
product-policy choices.

## Writable paths (exclusive)

- `evals/fixtures/operator-control/**`
- `editions/AXP-E0002/handoffs/E0002-12.md`

## Read-only references

- Frozen operator contract, schemas, evaluator, digests, and E0002 assignments

## Non-goals

- Contract/schema/evaluator edits, source/migration/manifest changes, security
  judgment, browser/provider/product execution, or generation of real secrets.

## Acceptance

- Every one of the sixteen frozen valid IDs and 105 rejection-vector IDs has
  exactly one deterministic fixture envelope and structured typed
  setup/action/expected recipe documents; rejection cases also have exactly
  one structured typed mutation document.
- A rejection-specific setup reproduces its named valid semantic baseline and
  may arrange only that vector's frozen prerequisite state before the
  post-setup baseline and counter reset; its one mutation occurs afterward.
  No recipe dispatches on a case ID or prose string.
- Strict corpus cases cover DTO unknown/duplicate fields, cursor/filter binding,
  scopes, revisions, idempotency receipts, fences, budgets, and audit chronology
  without weakening or adding policy; this task records those inputs and
  expected outputs but makes no executable product/conformance claim.
- The strict fixture index, envelope, and recipe-document schemas validate;
  ordered fixture IDs, policy-case digests, document digests, and secret
  sentinel scans reproduce. Each structured payload digest uses the frozen
  canonical compact-JSON rule, and every generic typed payload validates
  against its exact manifest schema reference.
- The Gate-B-frozen structural fixture validation recipe and
  `rtk git diff --check` pass. E0002-14 owns the conformance binary and first
  complete replay once the real W4 APIs exist.

## Stop and escalate

Stop and return a contract-owner question on missing/ambiguous expected output,
new policy, frozen-artifact drift, real credential/runtime data, cross-owner
edit, or one failed retry. Do not guess.

## Handoff

Record files, schema/digest commands and results, unresolved vector IDs, and
cross-owner requests in `handoffs/E0002-12.md`.
