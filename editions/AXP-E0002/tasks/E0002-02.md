# E0002-02 Task Packet

- Edition/task: `AXP-E0002 / E0002-02`
- Wave: W10 (shifted from W5 by D-E0002-041 and from W7 by D-E0002-071)
- Model/reasoning: `gpt-5.6-terra` / high
- Budget: 90 minutes and 50,000 combined model tokens per attempt; one retry
  maximum; zero live/provider spend
- Owner: e0002-http
- Status/dispatch: pending and dependency-ready under D-E0002-095; not
  dispatched without a separate product-owner decision
- Risk and owner gate: high protected-data transport; frozen Gate B contract,
  followed by Sol security review
- Dependencies: E0002-06, E0002-08, and E0002-11 done and quiescent

## Objective

Implement authenticated read-only operator APIs for the attention inbox, run
detail, pending approvals, command receipts, and audit chronology.

## Writable paths (exclusive)

- `crates/proof-transport-http/**`
- `editions/AXP-E0002/handoffs/E0002-02.md`

## Read-only references

- `AGENTS.md`, frozen operator contract/schemas/evaluator
- Auth and storage public APIs/handoffs; existing Tower integration patterns

## Non-goals

- Any approve/deny/cancel/resume/revoke mutation, auth/storage/kernel/runtime
  edit, permissive CORS, remote binding, raw execution-context exposure, UI, or
  provider/external effect.

## Acceptance

- Every operator route authenticates and checks exact capability before lookup,
  enumeration, projection, or distinguishable error; unauthorized/not-found
  responses do not reveal existence, count, identity, goal, args, or paths.
- The operator router exports an explicit protected route set for the E0002-11
  shell. It does not mount or fall through to legacy unauthenticated audit,
  proof, domain-list, or generic operation routes. It follows the frozen
  acyclic adapter direction and does not import `proof-operator-control`.
- DTOs are strict and redacted. They expose only frozen operator fields, never
  session material, signing keys, raw workspace path, model secret, or internal
  checkpoint state.
- Keyset pages remain stable under inserts and reject cursor reuse across
  workspace, instance, Human, capability, sort, or filter.
- Exact loopback Host/security/cache headers, no-CORS behavior, request bounds,
  and generic malformed/duplicate-header errors match the frozen contract.
- Client-controlled forwarding headers are ignored for identity, authority,
  workspace scope, session, and abuse-control decisions.
- Each endpoint has Tower oneshot success/auth/scope/redaction/pagination/error
  tests; the Sol reviewer accepts the auth-first ordering.
- Before editing HTTP source, complete only the owned HTTP package-manifest
  delta and signal it frozen without running Cargo. E0002-14 is the sole W10
  `Cargo.lock` owner and reconciles the lock while both W10 source trees remain
  quiescent; this owner waits for the stable-lock signal before beginning
  source work or any Cargo build/test command. Any later
  dependency change stops the wave and repeats the barrier. Reverse-impact checks wait for
  E0002-14 and this writer to quiesce.
- `rtk cargo fmt --check -p proof-transport-http`,
  `rtk cargo test -p proof-transport-http`, and
  `rtk scripts/test-scoped.sh proof-transport-http` pass.

## Stop and escalate

Stop on a mutation need, contract/DTO ambiguity, auth/storage API gap, remote
access, secret exposure, another crate edit, or one failed retry. Escalate only
this task to Sol if its declared tests or security review fail.

## Handoff

Record routes, files, scoped/Tower commands and results, redaction assumptions,
risks, and cross-owner requests in `handoffs/E0002-02.md`.
