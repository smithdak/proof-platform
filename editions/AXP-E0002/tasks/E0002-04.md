# E0002-04 Task Packet

- Edition/task: `AXP-E0002 / E0002-04`
- Wave: W10
- Model/reasoning: `gpt-5.6-sol` / high
- Owner: e0002-verifier
- Status/dispatch: pending and non-dispatchable
- Risk and owner gate: critical independent security/concurrency verification;
  verifier must not have authored product behavior
- Dependencies: E0002-03, E0002-07, E0002-08, E0002-09, E0002-11, and
  E0002-12 done and every writer quiescent

## Objective

Independently execute the frozen all-required API/browser/race evaluation for
the complete four-run operator journey and produce redacted evidence without a
provider call or external effect.

## Writable paths (exclusive)

- `docs/dogfood/operator-control.md`
- `editions/AXP-E0002/handoffs/E0002-04.md`

## Read-only references

- All frozen E0002 artifacts, completed task handoffs, candidate source/tests,
  and `contracts/axp-experience.md`

## Non-goals

- Source/contract/evaluator repair, retry beyond the frozen policy, Gate C,
  E0006 evidence combination, live provider/tool/external effect, credential or
  private workspace capture.

## Acceptance

- A fresh deterministic fixture contains at least four runs in awaiting,
  running, recoverable, and terminal states with synthetic identities/args.
- All required auth-first, scope, redaction, pagination, exact decision,
  explicit resume, zero-effect cancel, idempotency, uncertain-response, fence,
  restart/recovery, aggregate-budget, audit, browser-hygiene, accessibility,
  and sentinel vectors pass in one evaluation packet.
- Concurrency barriers establish one winner and zero unauthorized key/provider/
  tool/write/external effect for every frozen race.
- `runtime_worker_restart` preserves only a still-valid session in the unchanged
  control-plane instance while fenced recovery has one owner; separately,
  `control_plane_restart` invalidates the session, denies reads/actions, retains
  durable runs, and requires a fresh signed challenge before the journey
  continues to explicit End Session.
- Browser evidence uses one controlled session, contains no credential or raw
  private workspace data, and proves reload/end-session memory loss.
- The real E0002-03 app and E0002-02/09 routes are composed through the
  E0002-11 same-origin control-plane interface; route/static fallbacks, security
  headers, session propagation, revoke, and shutdown pass end to end.
- Ordered check/vector digests match exactly; no historical E0006 or separate
  failed run is combined into the result.
- The task's frozen evaluator command, scoped changed-crate checks, evidence
  sentinel scan, cleanup checks, and `rtk git diff --check` pass.

## Stop and escalate

Stop on any required-check failure, digest drift, sensitive artifact, source
repair need, provider/external effect, missing author independence, or one
failed retry. Preserve redacted failure evidence and return a bounded request;
do not manufacture a pass.

## Handoff

Record exact evaluator/scoped/browser commands and results, check/vector
totals, cleanup, limitations, and cross-owner requests in
`handoffs/E0002-04.md` without credentials or copied databases.
