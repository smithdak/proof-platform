# E0002-10 Task Packet

- Edition/task: `AXP-E0002 / E0002-10`
- Wave: W12 (shifted from W10 by D-E0002-041)
- Model/reasoning: `gpt-5.6-sol` / xhigh
- Budget: 180 minutes and 120,000 combined model tokens per attempt; one retry
  maximum; zero live/provider spend
- Owner: orchestrator
- Status/dispatch: pending and non-dispatchable
- Risk and owner gate: critical integration/release; product-owner Gate C
- Dependencies: E0002-01,02,03,04,05,06,07,08,09,11,12,13,14,15,16,17 done
  and all writers quiescent

## Objective

Integrate the accepted E0002 candidate, reconcile root/public/release records,
reproduce every scoped/reverse-impact/evaluator gate, document rollback and
limitations, and present Gate C without self-approval or E0006 release claims.

## Writable paths (exclusive)

- `Cargo.toml`
- `Cargo.lock`
- `README.md`
- `docs/dogfood/operator-control.md`
- `editions/BACKLOG.md`
- `editions/AXP-E0002/**`

## Read-only references

- `AGENTS.md`, all frozen E0002 artifacts, assignments, handoffs, candidate
  diff, and scoped-test impact map
- E0006 D-E0006-025 disposition only; no E0006 evidence inheritance

## Non-goals

- Agent self-approval, failed-check waiver, contract/security/migration scope
  expansion, provider/external effect, E0006 release/repair, destructive cleanup,
  commit, or push without separate owner instruction.

## Acceptance

- Every assignment is done/quiescent with accepted evidence and no path or
  migration conflict; final source/contracts/schemas/evaluator are consistent.
- The non-author E0002-04 evidence record and digest are preserved; any W12 edit
  that changes its substantive verdict requires verifier re-review.
- Formatting and tests pass for every changed crate plus reverse workspace
  dependents using the scoped commands required by `AGENTS.md`; non-Cargo app,
  schema, fixture, browser, security, and documentation checks also pass.
- The independent evaluator passes 100% of frozen required checks/vectors with
  exact digests, zero unauthorized/external effect, and no retained secret.
- Final reproduction confirms the real UI and protected read/mutation routers
  compose through the E0002-15 runnable loopback process; E0002-11 synthetic
  shell evidence is not substituted for this integrated result.
- `rtk scripts/swarm.sh validate AXP-E0002`, the full scoped impact set, and
  `rtk scripts/swarm.sh verify AXP-E0002 --quiescent` pass only after all writers
  stop.
- Evidence records known limitations, zero live spend, rollback, migration and
  credential recovery, independent verdict, and an explicit statement that
  E0006 remains unreleased and contributed no release evidence/authority.
- Product owner records a dated Gate C accept, defer, or reject decision. Only
  explicit acceptance may mark E0002 released.

## Stop and escalate

Stop on any non-quiescent writer, failed required check, hash/contract/schema/
migration drift, secret exposure, external effect, or need to broaden scope.
Never self-approve Gate C or commit/push without separate authority.

## Handoff

Record the final diff, every scoped/quiescent command and result, evaluator
totals/digests, limitations, rollback, spend, owner decision, and cross-owner
requests in `handoffs/E0002-10.md`.
