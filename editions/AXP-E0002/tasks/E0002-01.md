# E0002-01 Task Packet

- Edition/task: `AXP-E0002 / E0002-01`
- Wave: W1
- Model/reasoning: `gpt-5.6-sol` / high
- Budget: 120 minutes and 80,000 combined model tokens for this writing
  attempt; one retry maximum; zero live/provider spend
- Owner: orchestrator
- Status/dispatch: done by product-owner Gate A acceptance in D-E0002-011
- Risk and owner gate: high direction/governance; product-owner Gate A required
  before E0002-13 and Gate B required before every implementation task
- Dependencies: D-E0006-025 planning exception; E0006 quiescent and unreleased

## Objective

Create an owner-ready Gate A packet for one Human overseeing many governed
runs. Make the operator authentication boundary independent of E0006 and make
future work cheap to dispatch through bounded, dependency-safe assignments.

## Writable paths (exclusive)

- `editions/AXP-E0002/**`
- E0002 rows and narrative in `editions/BACKLOG.md`

## Read-only references

- `AGENTS.md`
- `contracts/axp-experience.md`
- `contracts/kernel-api.md`
- `editions/MODEL_POLICY.md`
- `editions/AXP-E0006/{status.md,decisions.md,evidence.md,retro.md}`
- Existing kernel/storage/runtime/HTTP source paths used by the read-only audits

## Non-goals

- Gate A or Gate B self-approval.
- Any contract, schema, evaluator, source, migration, root manifest, provider,
  browser/product runtime, external effect, commit, or push.
- Any claim or reuse of E0006 as released.

## Acceptance

- Charter fixes the eight-step journey, 100%-required outcome policy, budget,
  non-goals, material-risk triggers, and independent scoped auth direction.
- Charter requires a disposable trusted workspace with fresh enrolled Human
  and Agent identities and explicitly prohibits the compromised repository-root
  `.proof` identity from every E0002 product or evaluation path.
- Every future task has one owner, exact writable paths, earlier-wave
  dependencies, model/effort, bounded acceptance, stop gates, and one handoff.
- At most three tasks share a wave, their paths are disjoint, and the two HTTP
  tasks are sequential under one named owner.
- The accelerated W4 storage/runtime/control fan-out is separated from a W5
  non-author backend integration task; manifest deltas stabilize before
  same-wave Cargo commands and reverse-impact checks run only after quiescence.
- E0002-13 is blocked behind Gate A; all implementation is pending behind Gate
  B; no assignment is `ready` or `active`.
- E0002-01 remains `review` and MUST NOT become `done` unless the product owner
  records a dated Gate A acceptance in `decisions.md`. Revision or rejection
  leaves it non-done and keeps E0002-13 blocked.
- `rtk scripts/swarm.sh validate AXP-E0002` passes.
- `rtk git diff --check` passes and a path audit confirms no forbidden E0002
  product/public-contract artifact was created.
- Three distinct read-only reviewers accept security/gates, workgraph/path
  ownership/model routing, and journey/evaluation completeness.

All commands must be prefixed with `rtk` and scoped to owned paths/packages.

## Stop and escalate

Stop on cross-owner edits, shared-contract needs, destructive/external effects,
missing authority, or a failed retry limit. Return a bounded decision request.
Also stop if any wording could be read as Gate A approval, E0006 release, or
implementation dispatch authority.

## Handoff

Record the result in the matching file under `handoffs/`.
