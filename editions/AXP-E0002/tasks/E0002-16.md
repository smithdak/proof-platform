# E0002-16 Task Packet

- Edition/task: `AXP-E0002 / E0002-16`
- Wave: W4
- Model/reasoning: `gpt-5.6-sol` / high
- Budget: one authorized attempt, 120 minutes and 80,000 combined model tokens;
  zero live/provider spend and no automatic retry
- Owner: orchestrator
- Status/dispatch: done as a drafting barrier under D-E0002-052 after the valid
  exact freeze signal; superseding Gate B remains pending owner acceptance
- Risk and owner gate: critical public contract/security; a new exact
  superseding Gate-B packet requires explicit product-owner acceptance
- Dependencies: historical E0002-13 and E0002-12 done

## Objective

Repair only the D-E0002-037 stale-fence consistency defect: freeze four exact
command-attributed `stale_fence_rejected` profiles (`0x101e3`, `0x101a3`,
`0x2101e3`, and `0x4181a3`), reject obsolete runtime-shaped `0x16c30`, clarify
that runtime/store stale-fence observations append no audit event, add the event
to the proposed-command-ID exception, and close the exact affected artifact,
semantic, evaluator, and packet digests.

## Writable paths (exclusive)

- `contracts/operator-control-plane.md`
- `schemas/operator-control/reads-v1.schema.json`
- `schemas/operator-control/manifest-v1.json`
- `evals/operator-control-v1.json`
- `editions/AXP-E0002/assignments.tsv`
- `editions/AXP-E0002/decisions.md`
- `editions/AXP-E0002/evidence.md`
- `editions/AXP-E0002/handoffs/E0002-16.md`
- `editions/AXP-E0002/ownership.md`
- `editions/AXP-E0002/status.md`
- `editions/AXP-E0002/tasks/E0002-16.md`
- `editions/AXP-E0002/tasks/E0002-17.md`
- `editions/AXP-E0002/workgraph.md`

## Read-only references

- `AGENTS.md`, `contracts/axp-experience.md`, D-E0002-012/013 and
  D-E0002-036..041, historical E0002-13/E0002-12 handoffs
- Every non-owned operator schema, migration 14 up/down, kernel source/tests,
  fixture corpus, Cargo manifest, lockfile, and implementation source

## Non-goals

- Kernel, storage, runtime, auth, control, HTTP, UI, migration, dependency,
  Cargo manifest/lockfile, fixture, browser, provider, or external-effect work.
- Any new event kind, route, capability, policy choice, fixture ID/count/order,
  or change outside the exact D-E0002-037 closure.

## Acceptance

- The four branches retain the existing approval-decision, cancel, approval-
  resume, and recovery-resume command attribution plus submitted expected
  fence, with every reservation, lease, process, permit, budget, proof, and
  unrelated optional field null. Session revoke gains no profile.
- The contract explicitly distinguishes the typed runtime/store observation
  from durable authenticated-command audit and includes
  `stale_fence_rejected` in the proposed-command-ID existence exception.
- Only Gate-B artifact ordinals 1, 4, 10, and 11 change. The seven other raw
  artifact bytes and hashes, all schemas other than reads/manifest data,
  migration 14, Cargo files, fixture bytes, and implementation source remain
  byte-identical.
- Only the `valid_scenarios` and `rejection_vectors` semantic digests change.
  `checks`, `backend_subset`, and `store_error_matrix` remain byte-identical.
  The evaluator refreshes only the 28 assembly-bound setup blueprints and their
  derived case hashes plus the raw policy hash; its five runtime stale-fence
  cases retain their semantics and empty expected `new_events`.
- The canonical superseding packet names E0002-16, remains `pending_owner`, has
  `implementation_dispatch: false`, preserves evaluation counts, migration 14,
  and all material choices, and records every raw/semantic digest exactly.
- Duplicate-aware JSON parsing, eight Draft 2020-12 meta-schema validations,
  reference/manifest/hash closure, 206 logical shapes, 15 routes, four schema
  self-tests, evaluator semantic closure, raw/semantic/packet digest
  reproduction, unchanged-artifact custody, edition validation, and
  `rtk git diff --check` all pass without Cargo.
- On PASS, record exactly `Gate B stale-fence repair artifacts frozen`, mark
  E0002-16 done as a drafting barrier only, and leave Gate B unaccepted.

## Stop and escalate

Stop on any scope, byte-custody, parse, schema, reference, manifest, semantic,
digest, edition, or diff-check failure. Do not retry automatically, guess a
policy, touch fixtures, or dispatch E0002-17 before the exact freeze signal.

## Handoff

Record changed files, exact commands/results, old/new artifact and semantic
digests, packet digest, byte-custody results, assumptions, risks, and
cross-owner requests in `handoffs/E0002-16.md` without secrets.
