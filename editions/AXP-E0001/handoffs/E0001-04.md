# E0001-04 Handoff

Result: ready at the live credential boundary; blocked only because
`OPENAI_API_KEY` is absent from both sandbox and host process environments.

Product-owner disposition: defer the live journey until the morning of
2026-08-31. This is a pause only; it does not authorize a changed model,
endpoint, budget, preparation, or additional attempt. No provider request or
charge occurred before the pause.

All implementation dependencies and conditional B5 evidence prerequisites
pass. E0001-14 repaired the CLI delegation principal-kind mutation; E0001-15
closed workspace-import identity replacement, delegation unrevocation, and
partial proof/principal persistence; E0001-16 closed archive traversal,
substitution, target-link following, and unbound proof-file persistence;
E0001-17 bound the config actor to the signing key and hardened identity
lifecycle writes; and E0001-18 synchronized the canonical contracts with the
active implementation. E0001-19 then repaired live-v2 approval visibility,
separating event-independent crash diagnostics from exact event/call/request/
step/Human-bound actionability. The retained packet already has the correct
Agent requester and distinct Human approver, and none of E0001-14 through
E0001-19 modified it. E0001-20 then added the one-shot start claim, exact
emitted recovery, and secure operator procedure without starting a live run.
Host context passed the complete locked CLI suite 93/93 and the final-source
readiness replay after all seven tasks. E0001-09 produced
preparation
`01a053eb-a460-7f21-8b2b-5cebcd872dd0`, exact 10/10 evaluation
`01a053ec-51a1-7b13-af4e-2535a8ea551f`, readiness binding digest
`7efebe8d98ad23f9a122ae64f76887d371804f719bae7fb4a69b434e511b6b71`,
and an independently recomputed exact `next_argv` for live agent
`01a053ec-082a-7a53-b023-0ac263cbb460` in the retained fresh private
workspace. Zero provider attempts, v2 checkpoints, live runs, preview
artifacts, or failures exist.

Both host entry checks are satisfied for the current source tree. Any later
source, identity, digest, readiness, or test drift requires both checks again
before provider construction.

The retained resume root is
`/tmp/proof-e0001-readiness.GRXKx3`. Its mode was verified as private and its
readiness record remains immutable. The successful host replay ran `agent
live-prepare finish` for that preparation with agent
`01a053ec-082a-7a53-b023-0ac263cbb460`, delegation
`01a053ec-2d67-72e1-8c5d-1f22c7e53180`, and
`evals/release-manager-live-v1.json`, while both provider variables are
explicitly unset. Require binding digest
`7efebe8d98ad23f9a122ae64f76887d371804f719bae7fb4a69b434e511b6b71`
and the exact persisted `next_argv`; never reconstruct a replacement run.

The retained readiness workspace is under `/tmp` and must not be cleaned or
modified before resumption. If host cleanup or reboot removes it, stop and
regenerate plus independently verify a fresh credential-free 10/10 packet
before reading a credential; never infer that the missing packet passed.

When the credential is securely available, execute only the persisted exact
`next_argv`: direct Responses endpoint, exact `gpt-5.6-sol`, one primary run,
USD 0.15 owner ceiling / 120000 micro-USD runtime ceiling, synthetic data,
`store=true`, and no base-URL override. Stop at the signed Human approval
boundary, approve the exact emitted request, restart/resume that same run, and
assign a new non-author verifier for the exact 17/17 live evaluation. Do not
create a replacement preparation or live run.

Never include credentials, private key contents, complete `.proof` workspaces,
or copied runtime databases.

## 2026-08-31 pre-E0001-19 credential-free record reconciliation

The W14/W15 task packets, both pending handoffs, and the ranked backlog now
agree with the authoritative eighteen-task assignments/workgraph through
E0001-18. The backlog marks E0001 activated and points dispatch to the tracked
edition records instead of its original four-row seed.

- `rtk scripts/swarm.sh validate AXP-E0001` — PASS.
- Managed-sandbox `rtk cargo test -p proof-transport-cli --locked` — 55 passed
  and 15 expected environment-only secure-ancestry/loopback failures; no guard
  was weakened.
- Host `rtk cargo test -p proof-transport-cli --locked` — PASS, 81/81 across
  two suites.
- Exact host credential-free `live-prepare finish` replay with
  `OPENAI_API_KEY` and `OPENAI_BASE_URL` unset — PASS; output SHA-256
  `758847ef1a6f7be775b1fb90a86e962cf9eb93ffa9f03d9c754dd58d2b348bd7`.
- Readiness binding remains
  `7efebe8d98ad23f9a122ae64f76887d371804f719bae7fb4a69b434e511b6b71`;
  zero provider attempts or live effects exist.

## 2026-08-31 E0001-19 final-source entry gate

- E0001-19 Runtime/CLI implementation and both independent security/recovery
  audits — PASS.
- Runtime — PASS, 116/116; complete host CLI — PASS, 88/88 across two suites.
- Exact credential-free `live-prepare finish` replay with both provider
  variables removed — PASS; binding digest remains
  `7efebe8d98ad23f9a122ae64f76887d371804f719bae7fb4a69b434e511b6b71`.
- Ready-record SHA-256 remains
  `8bc2784f8ae20cc108fe50715ef31ec46289ed4bcf81da6a32b66554785cc40b`,
  mode `0600` below a `0700` workspace.
- `rtk scripts/swarm.sh validate AXP-E0001` and full diff checks — PASS for
  the nineteen-task W1-W16 graph.

At the E0001-19 checkpoint, the then-W15 live gate was ready but stopped before
provider construction: `OPENAI_API_KEY` was absent and provider-attempt count
remained zero.

## 2026-08-31 E0001-20 final-source entry gate

- E0001-20 atomic Kernel/Storage claim, Runtime one-shot replay/pristine
  recovery, exact CLI follow-up argv, and secure live-v2 runbook — PASS.
- Kernel 98/98; Storage 128/128; Runtime 119/119; complete host CLI 93/93;
  Runtime/CLI impact 212/212; full reverse impact 597/597 across 49 suites.
- Independent runtime/recovery and CLI/security audits — PASS. Independent
  runbook audit — PASS after its required post-claim recovery wording was
  corrected and re-audited.
- Exact credential-free `live-prepare finish` replay with both provider
  variables removed — PASS; preparation/evaluation IDs, exact 10/10,
  persisted `next_argv`, and binding digest remain unchanged.
- Ready-record SHA-256 remains
  `8bc2784f8ae20cc108fe50715ef31ec46289ed4bcf81da6a32b66554785cc40b`,
  mode `0600` below a `0700` workspace.
- The twenty-task W1-W17 edition graph validates; E0001-04 is now W16 and
  integration/Gate C is W17.

The W16 live gate remains stopped before provider construction:
`OPENAI_API_KEY` is absent, and provider-attempt count remains zero. Execute
only the replay-verified persisted start argv and its emitted workspace-bound,
policy-bearing, exact-Human follow-ups.

## 2026-08-31 E0001-20 concurrency-repair entry gate

- The post-commit runtime audit initially failed because concurrent stale
  resumers could append incompatible immutable checkpoints. Final source now
  combines a crash-released workspace execution lease, exact prior-state and
  process-epoch fences, prospective-history validation, and atomic
  expected-tail checkpoint append in Recording and SQLite storage.
- Direct suites pass Kernel 106/106, Storage 133/133, Runtime 122/122, and host
  CLI 93/93. Host reverse impact passes Runtime/CLI 215/215,
  Storage/transports 293/293, and Kernel/all dependents 613/613 across 50
  suites. Formatting and diff checks pass.
- Deterministic contention tests prove a blocked provider winner retains the
  sole execution lease, a resume contender returns `LiveRunBusy` without
  changing durable or workspace evidence, and an acquired-start contender
  retains only its atomic initial claim bundle before same-run recovery.
  Shared Prepared recovery reuses the same attempt and exactly one provider
  send occurs. Stale epoch and prior-epoch saves append nothing and leave
  replayable history.
- The independent final concurrency audit reports no remaining material race.
  `e0001-live-operator` owns the eventual procedure; the separate
  `e0001-live-verifier` must remain a non-author and must not sign the Human
  decision.

The source-sensitive credential-free readiness barrier was replayed after this
repair with both provider variables explicitly removed. It passed exact
ordered 10/10 with the same preparation, run, and evaluation IDs; the durable
validator accepted the unchanged persisted `next_argv`; binding digest remains
`7efebe8d98ad23f9a122ae64f76887d371804f719bae7fb4a69b434e511b6b71`;
ready-record SHA-256 remains
`8bc2784f8ae20cc108fe50715ef31ec46289ed4bcf81da6a32b66554785cc40b`;
and modes remain workspace `0700` / record `0600`. Provider attempts remain
zero, so the live gate is still stopped only at credential availability.
