# E0001-18 — Canonical E0001 contract synchronization

- Wave: W13
- Model/reasoning: `gpt-5.6-sol` / high
- Risk and owner gate: high credential-free contract correction; no normative API or live-use expansion
- Dependencies: E0001-17

## Objective

Synchronize the canonical kernel, SQLite, Content-domain, and live-contract
status with the already approved, implemented, and tested E0001 B1-B4/B6
surface so later editions do not design against pre-implementation facts.

## Writable paths (exclusive)

- `contracts/kernel-api.md`
- `contracts/domain-definitions.md`
- `contracts/release-manager-live-v1.md`
- `editions/AXP-E0001/handoffs/E0001-18.md`

## Read-only references

- `crates/proof-kernel/src/executor/{engine,store}.rs`
- `crates/proof-storage/src/sqlite/{migrations,delegation}.rs`
- `crates/proof-content/src/handlers.rs`
- `registry/content/release-publish-v2.json`
- E0001 Gate B decisions, W2/W3 handoffs, and D-E0001-010

## Required repair

1. Make the canonical kernel contract describe the active version-aware
   handler hooks and current SQLite schema version 12.
2. Add migration 12 to the canonical schema inventory and describe its active
   strict delegation-scope/load behavior without changing the frozen contract.
3. Make the Content domain contract identify `release.publish::v2` as an active
   implemented row while preserving unchanged v1 and pending edition Gate C.
4. Update the live contract's status to implemented/activated prerequisites,
   stopped at credential availability with zero provider attempts; preserve
   its pre-implementation rationale as historical decision evidence.
5. Preserve the original proposal decision row and add a dated activation row
   rather than rewriting decision history.

## Non-goals

No Rust, registry, schema, migration, policy, runtime, provider, credential,
retained-workspace, historical workspace, API behavior, or Gate C change. This
task does not declare the live journey complete.

## Acceptance

- Canonical docs agree with code on active handler hooks, migration 12,
  `scope_json`, `SqliteStore::load_delegation`, and active
  `release.publish::v2`.
- Pending/proposed labels remain only where they describe historical decisions
  or the still-pending live journey/Gate C, not shipped prerequisites.
- Scoped doc diff, whitespace, edition validation, complete host CLI suite, and
  exact credential-free readiness replay pass on the final tree.

## Stop and escalate

Stop on a discovered code/contract semantic mismatch, need to alter a frozen
API/schema/policy, credential boundary, retained packet mutation, provider use,
or any claim that E0001 Gate C has passed.

## Handoff

Record each stale statement, the exact current implementation evidence,
historical-status preservation, validation, and final host/readiness results.
Never record a credential, private key, or copied database.
