# E0000-15 Handoff

Result: complete

## Mappings

- HTTP `MissingKey`/`InvalidUuidV7`: 400.
- HTTP `Conflict`/`InProgress`/`Indeterminate`: 409.
- HTTP `StorageRequired`: 503; `StorageFailed`: 500.
- WS invalid idempotency input: `-32602`; stable conflict class: `-32006`;
  durable storage required and storage/internal: `-32005`.

## Files

- `crates/proof-transport-http/src/handlers/errors.rs`
- `crates/proof-transport-ws/src/lib.rs`
- This handoff.

## Tests

HTTP formatting and tests passed (26 tests). WS formatting passed. The initial
filesystem-sandbox run could not create its temporary listeners; the
orchestrator reran the scoped suite with loopback permission and all 9 tests
across 3 suites passed.

## Interface assumptions

Used the frozen `ExecutionError::Idempotency(IdempotencyError)` interface and
all six idempotency variants. No routing changes.

## Residual requests

None for this mapping packet. Governed WebSocket execution and exact proof
replay remain assigned to E0000-17.
