# E0000-07 Handoff

Result: complete

## Changed files

- `conformance/Cargo.toml`
- `conformance/cases/content_governance.json` (new)
- `conformance/tests/content_governance.rs` (new)
- `editions/AXP-E0000/handoffs/E0000-07.md`

No contract, root manifest, lockfile, registry, kernel, content, transport,
runtime, or repository `.proof` path was edited by this worker. The
orchestrator reconciled the root-owned `Cargo.lock` dependency edges after the
first locked test correctly refused to update it. No commit or push was made.

## Executable vectors

`content_governance.json` is consumed by
`content_v1_json_vectors_execute_governed_engine_and_exact_replay`; it is not a
declarative-only registry check. The eight vector IDs are:

1. `content-schema-create-v1`
2. `content-object-create-v1`
3. `content-object-edit-v1`
4. `content-approve-v1`
5. `content-changeset-commit-v1`
6. `content-edition-create-v1`
7. `content-release-v1`
8. `content-release-publish-v1`

The runner loads the distributable root `registry/content`, asserts there are
exactly eight entries, that every entry is active `v1`, that the vector and
registry sets equal the frozen set, and that `changeset.create::v1` is absent
from both. It also registers the real `content_handlers()` and asserts all
eight handler policies match the vector contract.

## Authority, approval, and proof evidence

- Every vector supplies an exact one-operation `DelegationScope` and content
  domain scope. The runner inserts that grant into kernel `RecordingStore`,
  executes with both `delegation_id` and its valid chain, and asserts the
  original engine proof binds the same delegation.
- The engine, request context, and signed approval requester share one agent
  keypair. Contexts remain `PrincipalKind::Agent` for all eight calls.
- `content.approve`, `content.release`, and `release.publish` first reject the
  agent with `ExecutionError::HumanOnly` and no proof or domain mutation. Each
  then executes only through an exact-input/version signed agent request,
  signed human approval decision, trusted human principal, and
  `execute_with_approval_evidenced`.
- All other calls use `execute_evidenced`; no handler is called directly and no
  proof is reconstructed outside the kernel.
- Each outcome asserts `operation::v1`, actor, delegation ID, context timestamp,
  UUIDv7 proof ID, Ed25519 verification, freshly computed canonical BLAKE3
  input/output digests, and exact equality with its stored proof.
- The passing runner ends with exactly 8 proofs, 8 execution contexts, and 8
  operation-scoped delegations. The three rejected unapproved attempts and all
  replay/conflict attempts add none.

## Lifecycle fixture and replay evidence

The runner uses a `TempDir`, copies only the root input schemas into that
temporary registry, and writes only temporary content state. Because
`schema.create` and `object.create` return but do not persist their domain
objects, the fixture materializes those evidenced outputs for downstream
operations. It submits the edited object before governed approval, then
persists an approved ChangeSet whose create edit becomes the committed object
used by the Edition and signed release calls. No product behavior was added.

Only `changeset.commit::v1` and `edition.create::v1` declare or exercise
`exact-original`; the other six assert `IdempotencyPolicy::None`, have no retry
vectors, and receive no synthetic idempotency key. For each exact target the
runner proves:

- the separately encoded retry has identical canonical input;
- output, proof ID, full proof body, signature, and complete proof equal the
  original stored outcome;
- proof/context counts and the complete temporary content-state snapshot do
  not change on retry;
- same-key changed canonical input returns
  `ExecutionError::Idempotency(IdempotencyError::Conflict)`; and
- the conflict also leaves proof/context counts and content state unchanged,
  demonstrating rejection before a second mutation.

## Commands and results

- Initial `rtk cargo test --locked -p proof-conformance` — expected clean stop:
  Cargo refused to update root-owned `Cargo.lock`; no compilation and no
  lockfile edit by this worker.
- Orchestrator lock reconciliation and
  `rtk cargo test -p proof-conformance --no-run` — reported compile pass.
- `rtk cargo test --locked -p proof-conformance` — pass: 6 tests across 7
  suites.
- `rtk cargo test --locked -p proof-conformance --test content_governance` —
  pass: 1 test in 1 suite.
- `rtk cargo fmt --check -p proof-conformance` — pass.
- `rtk scripts/test-scoped.sh proof-conformance --list` — changed and impacted
  package set is only `proof-conformance`.
- Required eight-operation `rtk rg` acceptance query — pass.
- `rtk git diff --check -- conformance editions/AXP-E0000/handoffs/E0000-07.md`
  — pass before this final handoff update and rerun afterward.

## Risks, skipped cases, and requests

- No operation vector was skipped.
- All eight manifests retain `required-uuidv7` metadata, but the frozen handler
  replay policy is intentionally `None` for six. This packet asserts metadata
  separately and does not invent replay semantics for those six.
- `RecordingStore` is the kernel-owned conformance helper that supports both
  exact replay and injected delegation grants. Accepted transport packets
  separately cover adapter-specific durable SQLite paths.
- Content filesystem mutation remains outside the replay completion
  transaction; the previously accepted fail-closed recovery limitation is
  unchanged.
- No remaining cross-owner implementation request exists.
