# E0000 Handoffs

Each task writes only its matching handoff file. The orchestrator reviews these
records and incorporates accepted evidence into `../evidence.md`.

Never include credentials, private key contents, or copied runtime databases.
