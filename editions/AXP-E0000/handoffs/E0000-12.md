# E0000-12 Handoff

Result: complete

## Changed files

- `crates/proof-transport-mcp/src/server.rs`
- `crates/proof-transport-mcp/src/lib.rs`
- `crates/proof-transport-mcp/src/main.rs`
- `crates/proof-transport-mcp/tests/mcp_server.rs`
- `crates/proof-transport-mcp/tests/mcp_governance_lifecycle.rs`

No content, registry, kernel, storage, root manifest, lockfile, `.proof`, or
other transport files were edited. No commit or push was made.

## Implementation

- Added `McpServer::new_with_storage`, retaining the existing `McpServer::new`
  constructor. One `Arc<S>` now backs `ExecutionStore`, `ApprovalStore`, and
  `AgentRunStore`; the MCP binary passes its existing
  `.proof/storage/storage.db` store through this constructor.
- Normal calls use `ExecutionEngine::execute_evidenced`; approved human-only
  calls use `execute_with_approval_evidenced`. The transport no longer mints a
  proof after execution and continues to expose the engine proof through MCP
  evidence metadata and the existing proof-file compatibility path.
- Final content handlers remain registered in `src/main.rs`, including the
  exact-replay `changeset.commit::v1` and `edition.create::v1` handlers.
- Updated the full-platform registry fixture from 20 to the canonical 21
  operations. Existing helper tests now construct engines with the same
  keypair supplied to the transport helper, matching engine signer/context
  identity.

## Evidence

`mcp_server.rs::governed_content_tools_replay_original_proof_and_preserve_run_metadata`
executes both governed content operations through `McpServer` and a shared
SQLite store. It verifies registry-derived tool names/schemas, first-call
output, signed version-bound proof, run/step success metadata, exact retries
with reordered JSON keys returning identical output and proof ID/body/signature,
stable proof counts and domain mutation counts, and changed-input conflicts
before a second mutation.

Commands:

- `rtk cargo fmt --check -p proof-transport-mcp` — pass.
- `rtk cargo test -p proof-transport-mcp` — pass, 25 tests across 5 suites.
- `rtk git diff --check -- crates/proof-transport-mcp editions/AXP-E0000/handoffs/E0000-12.md` — pass.

## Protocol risk and requests

MCP execution failures continue to use the existing JSON-RPC success envelope
with tool-level `isError: true` and stable kernel error text; no numeric MCP
error codes were invented. Numeric mapping, if later required, is a Gate B
public-contract decision. No cross-owner implementation request remains.
