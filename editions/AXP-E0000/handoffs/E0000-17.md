# E0000-17 Handoff

Result: complete

## Changed files

- `crates/proof-transport-ws/Cargo.toml`
- `crates/proof-transport-ws/src/lib.rs`
- `crates/proof-transport-ws/tests/ws_integration.rs`

No Cargo.lock, root manifest, process, content, kernel, storage, registry, or
other transport files were edited. No commit or push was made. The existing
worktree has unrelated concurrent changes, including a pre-existing modified
`Cargo.lock`; this task did not modify it.

## Implementation

- `WsAppState::with_registry_and_store` now creates one keypair, uses it for
  both the state request context and `ExecutionEngine::new_with_keypair`, and
  attaches the same `Arc<SqliteStore>` exposed by the state. `WsAppState::new`
  retains the existing `.proof/data/proofs/proofs.sqlite3` path.
- Final `proof-content` handlers are registered automatically through the
  adapter's public `register_handler` boundary. The explicit
  `register_content_handlers` helper remains available to callers.
- `execute` now calls `execute_evidenced` and serializes its original proof;
  transport-local proof reminting was removed. The response shape remains
  `{id,result:{operation,version,status,result,proof}}`.
- Idempotency mappings preserve the existing protocol shape and classify
  missing/invalid keys as `-32602`, conflict/in-progress/indeterminate as
  `-32006`, and storage-required/storage failures as `-32005`. No new numeric
  MCP-style codes were introduced.

## Evidence added

`ws_integration.rs::governed_content_operations_discover_and_replay_original_proof`
uses a real loopback WebSocket connection to discover `changeset.commit::v1`
and `edition.create::v1`, execute each operation, verify the returned proof
against the state's signer, compare reordered retries' complete output and
proof (including ID/body/signature), assert one domain mutation and one
stored proof, and assert changed-input conflicts before a second mutation.

`ws_integration.rs::idempotency_validation_and_storage_codes_survive_network_boundary`
uses the same network boundary to assert `-32602` for a missing key and
`-32005` when exact replay is requested without storage. Unit coverage in
`src/lib.rs` covers the complete idempotency mapping.

## Commands and results

- `rtk cargo fmt -p proof-transport-ws` — pass.
- `rtk cargo fmt --check -p proof-transport-ws` — pass.
- `rtk git diff --check -- crates/proof-transport-ws editions/AXP-E0000/handoffs/E0000-17.md` — pass.
- `rtk cargo test -p proof-transport-ws --lib` — pass, 2 tests.
- Root reconciled the permitted `proof-content` and `tempfile` dependency edges
  in `Cargo.lock`; `rtk cargo check -p proof-transport-ws` passed.
- `rtk cargo test -p proof-transport-ws` — pass, 11 tests across 3 suites when
  run with temporary loopback permission.

## Compatibility risk and cross-owner request

The WebSocket store path remains `.proof/data/proofs/proofs.sqlite3` exactly as
required. Cross-transport path unification is deferred to a separately
approved compatibility/migration gate. Root reconciled the lockfile and ran
the network suite; no cross-owner request remains.
