# E0000-10 Handoff

Result: complete

## Paths changed

- `crates/proof-transport-cli/src/main.rs`
- `crates/proof-transport-cli/src/commands/content.rs`
- `crates/proof-transport-cli/src/commands/benchmark.rs`
- `editions/AXP-E0000/handoffs/E0000-10.md`

No root, lockfile, kernel, storage, content, registry, other transport, or
workspace `.proof` path was edited. No commit or push was made.

## Governed CLI path

- `build_engine` now uses the workspace keypair plus an `Arc<SqliteStore>`,
  saves that workspace principal idempotently, and registers the final
  `proof_content::content_handlers()` set.
- Generic `proof execute` calls `execute_evidenced`, then writes exactly the
  engine-returned proof to the established CLI proof-file view. It no longer
  reconstructs or re-signs evidence.
- Added direct `changeset-commit --changeset-id <uuid> --idempotency-key
  <uuidv7> [--notes <text>]` and required
  `edition-create --changeset-id <uuid> --idempotency-key <uuidv7>` commands.
  Both use the same evidenced engine path.
- The local `changeset-create` helper remains local and no longer emits a
  `changeset.create` proof.
- Updated the benchmark engine construction to use the durable, workspace-key
  engine. Its existing fixture now supplies the handler's schema-complete
  input and registry schema.

## Acceptance evidence

- Added CLI tests for caller-provided direct-command UUIDv7 keys and for both
  governed mutations. The mutation test proves first-call output and
  version-bound signed proof, reordered canonical retry equality for output,
  proof ID/body/signature, no second mutation, changed-input replay conflict
  before mutation, persisted original proof-file evidence, and proof-free
  `changeset.create`.
- `rtk cargo fmt --check -p proof-transport-cli` — pass.
- `rtk cargo test -p proof-transport-cli` — pass (25 tests). The suite's
  loopback-server test required the approved scoped test invocation outside
  the filesystem sandbox.
- `rtk git diff --check -- crates/proof-transport-cli editions/AXP-E0000/handoffs/E0000-10.md` — pass.

## Legacy path disposition and compatibility

The old CLI-local `edition.create` mutation/proof path and generic local
proof minting are removed. The existing `.proof/data/proofs/<proof-id>.json`
view is retained for `status` and `verify`, but receives the durable engine's
original proof idempotently. Direct governed-command output now uses the
standard `status: executed` envelope with the governed output in `result`;
this is the necessary fail-closed replacement for the legacy output that
claimed locally minted evidence.

## Risks and cross-owner requests

The content handler's documented fail-closed recovery behavior still applies:
a process interruption after a filesystem mutation and before replay
completion requires reconciliation with a new idempotency key. No cross-owner
request is required.
