# E0000-08 Handoff

Result: complete

## Changed paths

- `crates/proof-agent-runtime/Cargo.toml`
- `crates/proof-agent-runtime/src/trace_eval.rs`
- `docs/dogfood/release-manager-preview.md`
- `editions/AXP-E0000/handoffs/E0000-08.md`

`evals/release-manager-preview-v1.json` was deliberately not changed: its
`2026.08.29-rc1` arguments and `/data/release/{id,edition_id}` pointers already
match the root release-publish contract. The runtime test now consumes that
checked-in policy directly, eliminating the prior duplicated in-code policy.
No root, lockfile, contract, registry, transport, conformance, or repository
`.proof` path was changed or inspected. No credential, provider, network, live
run, commit, or push was used.

## Deterministic fixture and evaluation evidence

- `trace_eval::tests::two_fixed_release_manager_runs_match_checked_in_policy_and_trace_digest`
  constructs two independent release-manager fixture graphs with the same fixed
  `2026-08-29T20:00:00Z` clock, UUIDv7 values, Ed25519 signing-key bytes,
  response/call IDs, terminal event evaluation ID, and evaluation ID.
- Both fixtures use `release.publish::v1`, exact
  `{environment:"preview",version_label:"2026.08.29-rc1"}` arguments, the
  real `{operation,data:{release,...}}` output envelope, a signed human
  approval, `approval_resumed`, and a signed proof.
- The test parses `evals/release-manager-preview-v1.json`, evaluates both
  traces, asserts 10 passed checks, score `10_000`, no failed checks/events,
  and equal bindings. It pins trace digest
  `b14a94ed884b758f9503fdb95f85a65bd9d384d56aa13130ffe181fb69deae34` and
  policy digest
  `1e33747b44100727056c00407103deedf2b0c852349fd6489aa71d4246569f33`.
- Existing mutation coverage remains: `evaluation_digests_bind_the_exact_policy_and_trace_snapshot`
  proves a policy mutation changes policy digest and a trace timestamp mutation
  changes trace digest; `trace_digest_ignores_non_durable_principal_read_timestamps`
  preserves the approved durable-identity normalization rule.

## Fixture command and verification

```bash
rtk cargo test -p proof-agent-runtime two_fixed_release_manager -- --nocapture
```

- `rtk cargo test -p proof-agent-runtime` — pass (38 tests).
- `rtk cargo fmt --check -p proof-agent-runtime` — pass.
- `rtk rg -n '10 checks|trace digest|approval_resumed|release.publish::v1' docs/dogfood/release-manager-preview.md evals/release-manager-preview-v1.json` — pass.
- `rtk git diff --check -- docs/dogfood evals crates/proof-agent-runtime editions/AXP-E0000/handoffs/E0000-08.md` — pass.

## Documentation and limitations

The dogfood record now gives the exact deterministic fixture command and
states that it is scripted fixture evidence only. It demonstrates runtime and
evaluator determinism, not live-provider availability or model quality.
The existing separately documented live gate remains a future owner release
decision and was not run or expanded here.

## Risks and cross-owner requests

Production trace binding was not weakened: durable run, proof, event, and
identity fields remain in the canonical trace snapshot. Determinism is achieved
solely through fixed test-fixture inputs. No Gate B decision or cross-owner
change is required; runtime-owner/orchestrator review of this evaluation-policy
binding remains required by the task packet.
