# E0000-18 Handoff

Result: complete

## Changed sections

- Added the approved exact-replay public policy, key, claim, claim-result, and
  `ExecutionStore` method contracts, with source locations and root re-exports.
- Documented the single shared `ExecutionEngine::execute_operation` lifecycle,
  canonical-input digesting, claim states, exact completed replay, atomic
  completion, and fail-closed recovery policy.
- Added all six nested idempotency errors and their HTTP/WebSocket mappings,
  while retaining the existing canonical `ExecutionError` mappings.
- Corrected `load_delegation` to the implemented `Delegation` type and updated
  SQLite current schema version to 11.
- Documented migration 11's ledger shape, constraints, atomicity, empty
  upgrade, and rollback caveat.

## Source checks

Verified against the implementation and prior handoffs:

- Kernel API/types: `crates/proof-kernel/src/executor/store.rs`, `error.rs`,
  `engine.rs`, `executor/mod.rs`, and `lib.rs`.
- SQLite replay methods and migration 11:
  `crates/proof-storage/src/sqlite/replay.rs`, `store.rs`, and
  `migrations.rs`.
- HTTP mapping: `crates/proof-transport-http/src/handlers/errors.rs`.
- WebSocket mapping: `crates/proof-transport-ws/src/lib.rs`.
- Approved semantics and lifecycle: D-E0000-006 and E0000-13; verification
  evidence: E0000-14, E0000-15, and E0000-16.

## Unresolved deviations and downstream requests

- The canonical contract maps `Delegation` to HTTP 403, but the current
  implementation groups it with HTTP 500 in
  `crates/proof-transport-http/src/handlers/errors.rs`. This pre-existing
  mapping deviation is a downstream transport integration request owned by
  E0000-11.
- The canonical contract maps `BenchmarkExpired` to HTTP 409, but the current
  implementation groups it with HTTP 500 in
  `crates/proof-transport-http/src/handlers/errors.rs`. This pre-existing
  mapping deviation is a downstream transport integration request owned by
  E0000-11.
- Replay contract/implementation semantic drift was not found. E0000-06
  remains the downstream owner request to opt in only `edition.create::v1`
  and `changeset.commit::v1`.

## Validation

`rtk cargo test -p proof-transport-ws` — passed; all 9 tests passed after the
orchestrator reran the scoped suite with loopback listener permission.

`rtk git diff --check -- contracts/kernel-api.md
editions/AXP-E0000/handoffs/E0000-18.md` passed after the review repairs.
