# E0000-06 Handoff

Result: complete

## Paths changed

- `crates/proof-content/src/changeset.rs`
- `crates/proof-content/src/handlers.rs`
- `crates/proof-content/tests/content_governance.rs`
- `crates/proof-content/tests/governed_mutations.rs` (new)
- `registry/content/approve.json`
- `registry/content/changeset-commit.input.json`
- `registry/content/changeset-commit.output.json`
- `registry/content/edition-create.json` (new)
- `registry/content/edition-create.input.json` (new)
- `registry/content/edition-create.output.json` (new)
- `editions/AXP-E0000/handoffs/E0000-06.md`

No kernel, storage, transport, contract, root manifest, `.proof`, Git-index, or
other non-owned path was edited. No commit or push was made.

## APIs and registry entries

- Preserved `ChangeSet::commit` unchanged for existing callers and added
  `ChangeSet::commit_with_result`, which returns the actual committed
  `ChangeSet` plus next object state.
- `changeset.commit::v1` now requires `idempotency_key` UUIDv7 and
  `changeset_id`, accepts optional `notes`, returns the frozen
  `{operation,data:{changeset,objects_count}}` output, persists the committed
  ChangeSet, writes surviving objects, and removes deleted object files before
  a later state load can observe them.
- Added the root `edition.create::v1` manifest and strict input/output schemas.
  Its handler requires an existing committed ChangeSet, snapshots the sorted
  current object state through `Edition::new` (v1 SHA-256 digest), persists the
  immutable Edition, and returns `{operation,data:{edition}}`.
- Corrected the root `content.approve` consequence to `content-approval`.
- Fixed JSON-schema `additionalProperties` parsing and made content handlers
  resolve root `registry/` paths first, then initialized-workspace
  `.proof/registry/` paths. `content_handlers()` now exposes exactly eight
  handlers; only `changeset.commit` and `edition.create` return
  `RequiredUuidV7ExactReplay`, while the remaining six return `None`.

## Tests and evidence

- Added content tests for the additive committed-ChangeSet API, exactly eight
  root active v1 operations, UUIDv7/strict target schemas, policy selection,
  unknown-field rejection, persisted commit/delete state, immutable Edition
  persistence, and committed-ChangeSet enforcement.
- Review repair: a TempDir test proves a handler resolves its input schema when
  the only available registry copy is
  `.proof/registry/content/schema-create.input.json`; an explicit root
  `registry/` checkout remains the first candidate.
- `governed_mutations` uses kernel `RecordingStore` end-to-end: each target
  first call mutates/persists once; canonical retries return byte-equivalent
  outputs and the original proof ID/body/signature; changed same-key input
  conflicts before mutation. It also covers missing and unsupported storage,
  non-v7 keys, and a handler failure becoming fail-closed `Indeterminate` on
  retry.
- `rtk cargo fmt --check -p proof-content` — pass.
- `rtk cargo test -p proof-content` — pass (29 tests across 5 suites).
- `rtk scripts/test-scoped.sh proof-content --list` — pass; the final impacted
  packages are `proof-conformance`, `proof-content`, `proof-storage`,
  `proof-transport-cli`, `proof-transport-http`, `proof-transport-mcp`, and
  `proof-transport-ws`.
- `rtk rg -n 'edition.create|changeset.commit' registry/content crates/proof-content schemas/content` — pass; confirms versioned manifests, schemas, handlers, and tests.
- `rtk git diff --check -- crates/proof-content registry/content schemas/content editions/AXP-E0000/handoffs/E0000-06.md` — pass.
- Final orchestrator impact run: `rtk scripts/test-scoped.sh proof-content`
  passed 231 tests across 31 suites and all seven impacted packages. This was
  run after E0000-12 reconciled the canonical registry count from 20 to 21 and
  E0000-07 added the conformance dependency edge.

## Transport requests and integration order

- E0000-10/11/12/17 should register the completed handler set and route these
  two operations exclusively through the shared engine; no transport may mint
  a direct/bypass proof. The CLI owner still owns the local,
  proof-free `changeset.create` helper disposition.
- Integrate after E0000-14/16 exact-replay kernel/storage work (already present)
  and before the dependent transport packets. Run their scoped suites from the
  impact list above after integration.

## Risks and assumptions

- Domain filesystem mutation remains outside the shared replay-completion
  transaction, as approved by D-E0000-006. A process interruption after domain
  mutation and before ledger completion remains deliberately fail-closed and
  requires reconciliation plus a new UUIDv7 key.
- The current content filesystem format has no shared transactional primitive;
  the handler validates before writing, persists the exact committed ChangeSet,
  writes resulting objects, then removes deleted files. This satisfies the v1
  next-load state requirement without widening the frozen handler boundary.
