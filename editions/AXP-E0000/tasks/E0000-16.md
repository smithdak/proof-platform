# E0000-16 — SQLite exact-replay ledger

Model: `gpt-5.6-sol` · reasoning: `high`

## Objective

Implement approved migration 11 and the SQLite `ExecutionStore` replay methods
with atomic claim, completion, conflict, failure, concurrency, and reopen
behavior.

## Writable paths (exclusive)

- `crates/proof-storage/**`
- `editions/AXP-E0000/handoffs/E0000-16.md`

## Read-only references

D-E0000-006, E0000-13, frozen proof-kernel replay API, existing approval
execution persistence, and migration tests.

## Dependencies

E0000-13 and explicit Gate B approval of D-E0000-006. It may run concurrently
with E0000-14 and E0000-15 against the frozen interface.

## Risk and owner gate

Critical; one owner controls migration 11 and no existing migration is edited.

## Non-goals

No kernel, content, registry, transport, root manifest, or unrelated storage
changes. Do not implement automatic claim expiry, stealing, or deletion.

## Acceptance

1. Migration 11 is appended once with the approved table and constraints.
2. New storage functions have round-trip tests for claim, complete, replay,
   conflict, failed, in-progress, reopen, and atomic rollback behavior.
3. Two connections racing on the same tuple produce one acquired claim.
4. Fresh, v10 upgrade, v11 rollback/reapply, and concurrent opener tests pass.
5. `rtk cargo fmt --check -p proof-storage` and
   `rtk cargo test -p proof-storage` pass.

## Stop/escalate

Stop on any migration-shape/API change, kernel edit, or recovery-policy change.

## Handoff schema

Report: files; migration; SQL invariants; trait implementation; tests;
concurrency; rollback and residual risks.
