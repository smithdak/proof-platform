# E0000-12 — Governed MCP content path

Model: `gpt-5.6-terra` · reasoning: `medium`

## Objective

Verify and complete registry-derived MCP tools for `edition.create` and
`changeset.commit`, including schema discovery, governed execution, durable run
metadata, and signed evidence.

## Writable paths (exclusive)

- `crates/proof-transport-mcp/**`
- `editions/AXP-E0000/handoffs/E0000-12.md`

## Read-only references

`AGENTS.md`, `contracts/**`, `registry/content/**`,
`crates/proof-content/**`, and E0000-06's accepted handoff.

## Dependencies

E0000-06 integrated and its writer quiesced.

## Risk and owner gate

High governance/protocol risk. Gate B is required for a public MCP contract or
governance exception.

## Non-goals

Do not edit content handlers, registry, storage, kernel, or other transports.

## Acceptance

1. `rtk cargo fmt --check -p proof-transport-mcp` passes.
2. `rtk cargo test -p proof-transport-mcp` passes.
3. MCP integration tests cover derived schemas, annotations, execution, durable
   run metadata, and version-bound signed proof for both operations. The
   full-platform registry fixture recognizes the new canonical total of 21
   operations instead of retaining its pre-`edition.create` count of 20.
4. The MCP engine is attached to its durable store and returns
   `execute_evidenced`'s outcome instead of minting a transport-local proof.
   The server wires the same SQLite instance into execution replay, approvals,
   and run metadata while retaining its existing public constructor behavior.
5. An exact retry returns identical output and the original proof ID, body, and
   signature without a second mutation; changed input under the same key fails
   before mutation.
6. `rtk git diff --check -- crates/proof-transport-mcp editions/AXP-E0000/handoffs/E0000-12.md` passes.

## Stop/escalate

Stop on a registry, handler, shared run type, or storage need and return the
exact cross-owner request.

## Handoff schema

Report changed files; generated tools; commands/results; run/proof evidence;
protocol risk; cross-owner requests.
