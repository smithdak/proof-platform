# E0000-18 — Canonical kernel contract reconciliation

Model: `gpt-5.6-luna` · reasoning: `medium`

## Objective

Reconcile `contracts/kernel-api.md` with approved D-E0000-006 and the verified
W4 implementation so downstream workers receive accurate public types, replay
lifecycle, error mappings, source locations, and migration version.

## Writable paths (exclusive)

- `contracts/kernel-api.md`
- `editions/AXP-E0000/handoffs/E0000-18.md`

## Read-only references

`AGENTS.md`, D-E0000-006, E0000-13/14/15/16 handoffs, and the implemented
kernel, storage, HTTP, and WebSocket source.

## Dependencies

E0000-14, E0000-15, and E0000-16 integrated and verified.

## Risk and owner gate

Medium. This packet records the already-approved interface; it must stop rather
than invent or alter public behavior.

## Non-goals

No Rust, migration, registry, domain, architecture, or transport edits. Do not
resolve an implementation/contract conflict by silently choosing new semantics.

## Acceptance

1. Source locations and public type/method names match the current code.
2. Exact replay policy, tuple, claim lifecycle, errors, mappings, and fail-closed
   recovery match D-E0000-006.
3. SQLite current version is 11 and migration 11 is described accurately.
4. Existing canonical HTTP mappings remain explicit; implementation deviations
   are called out as downstream integration requests rather than hidden.
5. `rtk git diff --check -- contracts/kernel-api.md editions/AXP-E0000/handoffs/E0000-18.md` passes.

## Stop/escalate

Stop on any new public choice or conflict not resolved by an approved decision.

## Handoff schema

Report changed sections; source checks; unresolved deviations; validation; and
downstream requests.
