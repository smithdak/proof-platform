# E0000-05 — Contract drift reconciliation

Model: `gpt-5.6-sol` · reasoning: `high`

## Objective

Reconcile architecture, kernel API, domain definitions, registry manifests, and
implementation names/statuses so downstream work targets one product truth.

## Writable paths (exclusive)

- `contracts/**`
- `ARCHITECTURE.md`
- `editions/AXP-E0000/handoffs/E0000-05.md`

## Read-only references

`AGENTS.md`, `contracts/kernel-api.md`, `contracts/domain-definitions.md`,
`registry/**`, `crates/**`, `docs/dogfood/release-manager-preview.md`.

## Dependencies

E0000-01. Must finish before E0000-06.

## Risk and owner gate

High; product-owner approval is required for any operation rename, scope change,
or compatibility exception.

## Non-goals

Do not modify Rust handlers, registry JSON, migrations, or transport code.

## Acceptance

1. `rtk rg -n 'edition.create|changeset.commit|operation::version' contracts ARCHITECTURE.md` supports the reconciled contract.
2. `rtk git diff --check -- contracts ARCHITECTURE.md editions/AXP-E0000` passes.
3. `rtk git diff -- contracts ARCHITECTURE.md` has a decision entry for every
   scope/name change.

## Stop/escalate

Stop on conflicting canonical sources, missing owner decision, or required
crate edits; escalate a minimal decision proposal.

## Handoff schema

Report: mismatch matrix; chosen canonical forms; decision IDs; compatibility
notes; verification commands; unresolved cross-crate requests.
