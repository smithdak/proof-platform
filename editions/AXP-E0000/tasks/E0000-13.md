# E0000-13 — Shared exact-replay design

Model: `gpt-5.6-sol` · reasoning: `high`

## Objective

Design the smallest shared execution-layer mechanism that enforces the frozen
idempotency tuple, prevents conflicting reuse before mutation, and returns the
original persisted output and signed proof for an exact replay.

## Writable paths (exclusive)

- `editions/AXP-E0000/handoffs/E0000-13.md`

Kernel, storage, contracts, migrations, and transports are read-only in this
design task.

## Read-only references

`contracts/kernel-api.md`, `contracts/domain-definitions.md`, `ARCHITECTURE.md`,
the executor/store APIs, SQLite migrations and tests, approval replay patterns,
error mappings, and E0000-05 evidence.

## Dependencies

E0000-05 and owner-approved D-E0000-005.

## Risk and owner gate

Critical. Any new migration, shared public type, error variant, or recovery
policy must be proposed for Gate B before E0000-14 writes code.

## Non-goals

Do not edit code, contracts, manifests, migrations, transports, or `.proof`.
Do not implement content handlers or weaken exact original-proof replay.

## Acceptance

1. Handoff specifies exact API/type additions and whether they are public.
2. It specifies the next sequential migration, unique constraints, and
   round-trip/upgrade/rollback behavior if storage changes are needed.
3. It defines claim/complete/replay/conflict/failure and concurrent-call
   semantics without allowing a mutation before conflict detection.
4. It lists scoped tests and every downstream error-mapping/integration request.
5. `rtk git diff --check -- editions/AXP-E0000/handoffs/E0000-13.md` passes.

## Stop/escalate

Return a minimal Gate B decision packet. Do not begin E0000-14 automatically.

## Handoff schema

Report: current gap; proposed types/APIs/table; lifecycle and atomicity;
migration; errors; tests; compatibility; exact owner decision requested.
