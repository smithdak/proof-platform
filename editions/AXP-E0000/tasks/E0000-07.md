# E0000-07 — Executable conformance

Model: `gpt-5.6-terra` · reasoning: `medium`

## Objective

Turn the eight canonical content operations into executable vectors proving
authority, operation-version naming, evidence, idempotency, and replay behavior.

## Writable paths (exclusive)

- `conformance/**`
- `editions/AXP-E0000/handoffs/E0000-07.md`

## Read-only references

`contracts/**`, `registry/content/**`, `crates/proof-content/**`, transport tests,
`editions/AXP-E0000/tasks/E0000-06.md`.

## Dependencies

E0000-10, E0000-11, E0000-12, and E0000-17 integrated and writers quiesced.

## Risk and owner gate

High; conformance owner and orchestrator review expected evidence, not merely
exit codes.

## Non-goals

Do not add product behavior, modify handlers, or run broad workspace tests.

## Acceptance

1. `rtk rg -n 'schema.create|object.create|object.edit|content.approve|content.release|changeset.commit|release.publish|edition.create' conformance` finds all eight.
2. `rtk cargo test -p proof-conformance` passes.
3. `rtk git diff --check -- conformance editions/AXP-E0000/handoffs/E0000-07.md` passes.
4. A JSON case file is consumed by an executable test; no new declarative-only
   fixture closes this packet. The runner asserts the root registry has exactly
   the frozen eight `v1` operations and that `changeset.create` is absent.
5. Every operation vector asserts governance/authority and version-bound signed
   evidence from the engine. Human-only operations execute only through the
   approval path; the runner may not bypass governance by invoking handlers
   directly.
6. `changeset.commit` and `edition.create` additionally assert exact canonical
   retry returns the original output and proof ID/body/signature, while changed
   same-key input conflicts before a second mutation. Do not impose exact-replay
   semantics on the other six operations, whose frozen handler policy is
   `None` in E0000.

## Stop/escalate

Stop when a vector contradicts a canonical contract or requires handler edits;
escalate the mismatch to the orchestrator.

## Handoff schema

Report: vector IDs; command/output; operation coverage; negative cases; proof
and replay evidence; skipped cases and reasons; risks.
