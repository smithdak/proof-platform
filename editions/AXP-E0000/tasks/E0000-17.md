# E0000-17 — Governed WebSocket content path

Model: `gpt-5.6-terra` · reasoning: `medium`

## Objective

Verify and complete WebSocket peer behavior for `edition.create` and
`changeset.commit`, including discovery, governed execution, errors, and exact
original-proof replay.

## Writable paths (exclusive)

- `crates/proof-transport-ws/**`
- `editions/AXP-E0000/handoffs/E0000-17.md`

## Read-only references

`AGENTS.md`, `contracts/**`, `registry/content/**`,
`crates/proof-content/**`, and E0000-06's accepted handoff.

## Dependencies

E0000-06 integrated and its writer quiesced.

## Risk and owner gate

High governance/protocol risk. Gate B is required for new public protocol
semantics or an exception to transport-peer behavior.

## Non-goals

Do not edit content handlers, registry, storage, kernel, or other transports.

## Acceptance

1. `rtk cargo fmt --check -p proof-transport-ws` passes.
2. `rtk cargo test -p proof-transport-ws` passes in an environment that permits
   temporary loopback listeners.
3. Integration tests cover both operations' capability discovery, governed
   execution, stable error codes, and version-bound proof response.
4. The WebSocket engine is attached to its durable store and returns
   `execute_evidenced`'s outcome instead of minting a transport-local proof.
   It is built with the same keypair used by request contexts; content handlers
   are registered through the adapter's existing handler boundary.
5. An exact retry returns identical output and the original proof ID, body, and
   signature without a second mutation; changed input under the same key fails
   with the stable conflict code before mutation.
6. Retain the existing WebSocket workspace database path in E0000. Unifying
   HTTP/WS and CLI/MCP persistence paths requires a separately approved
   compatibility and migration plan; exact replay is required within this
   adapter's existing store boundary.
7. `rtk git diff --check -- crates/proof-transport-ws editions/AXP-E0000/handoffs/E0000-17.md` passes.

## Stop/escalate

Stop on a shared error variant, handler, registry, or storage need and return a
minimal cross-owner request.

## Handoff schema

Report changed files; discovery/execution behavior; commands/results; exact
proof/error evidence; compatibility risk; cross-owner requests.
