# E0006-08 — Launcher-first exact product ceremony

- Edition/task: `AXP-E0006 / E0006-08`
- Wave: W7
- Model/reasoning: `gpt-5.6-sol` / high
- Owner: e0006-verifier, distinct non-author
- Status/dispatch: blocked and quiescent; D-E0006-018 consumed by the failed
  denial-only ceremony recorded in D-E0006-019
- Risk and owner gate: critical product/security ceremony; D-E0006-018 binds
  one launcher-first provider-free run with no retry
- Dependencies: E0006-07 done plus D-E0006-018

## Objective

Run the one authorized launcher-first provider-free synthetic journey and
produce one same-visible-tab, same-session exact 14/14 result without retaining
any credential-bearing artifact. The completed run failed because it persisted
an unexpected approval instead of the required denial; no retry is authorized.

## Writable paths (exclusive after dispatch)

- `docs/dogfood/approval-console-secure.md`
- `editions/AXP-E0006/handoffs/E0006-08.md`

## Required order

1. Create one isolated headed named browser session and directly verify its one
   visible tab on a credential-free loopback launcher. Do this before creating
   the product fixture or request lifetime.
2. Independently attach the verifier to that same launcher through direct URL,
   title, body, console, overlay, and tab-count reads. A stale inventory label
   is allowed; a second tab or failed direct read stops the run.
3. Create exactly one fresh provider-environment-unset synthetic product
   fixture, request, listener, and controlling non-echoing TTY.
4. Asynchronously navigate the already-visible launcher document to the clean
   product URL. The Human uses that headed window only and does not open the URL
   in a regular browser.
5. Establish the product document through direct reads before the Human enters
   the displayed code. Never extract, copy, print, message, or capture the code
   or session.
6. Complete the frozen source-ordered app checks, one exact synthetic denial,
   zero execution/resume, explicit End Session, controlled shutdown, same-TTY
   echo restoration, listener absence, and exact fixture/runtime disposal.

## Acceptance

- D-E0006-018 binds exactly one run with zero provider use, zero external effect,
  one synthetic denial, no retry, and no source repair.
- All frozen 14 checks and 26 rejection vectors retain their exact digests.
- One visible headed tab supplies the launcher, product, decision, revoke, and
  post-revoke evidence; no regular-browser or prior-run evidence is combined.
- PASS requires exact 14/14 and 10,000 basis points. Any failed boundary consumes
  that future authority and stops.

## Non-goals

No source/contract/evaluation change, lifetime extension, reusable credential,
second tab/session, browser export, secret-bearing screenshot, provider,
execution/resume, external effect, E0002 work, Gate C self-approval, commit, or
push.

## Handoff

Recorded redacted commands/observations, exact digests, the unexpected approved
decision, zero-execution/effect containment, cleanup, limitations, and exact
FAIL/BLOCKED disposition without any credential or private fixture.
