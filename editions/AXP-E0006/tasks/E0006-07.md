# E0006-07 — Credential-free launcher-first attachment diagnostic

- Edition/task: `AXP-E0006 / E0006-07`
- Wave: W6
- Model/reasoning: `gpt-5.6-sol` / high
- Owner: orchestrator
- Risk and owner gate: browser-evidence tooling only; bounded by D-E0006-017
- Dependencies: E0006-05 done; D-E0006-016 and E0006-06 are read-only failure
  context, not executable dependencies

## Objective

Determine why the isolated verifier target remained Chrome New Tab during the
D-E0006-015 ceremony and prove a deterministic one-tab launch pattern without
creating a product fixture, bootstrap, session, approval, or provider boundary.

## Writable paths (exclusive)

- E0006 edition planning, decision, evidence, status, retrospective, backlog,
  task, and handoff records listed in `assignments.tsv`

## Read-only references

- E0006-03/E0006-06 browser evidence and D-E0006-012/D-E0006-016
- Installed `agent-browser` help/version and the browser-skill instructions
- Frozen contract/evaluation; no edits

## Non-goals

No product workspace, approval server, bootstrap/session credential, Human TTY
handoff, decision, execution, provider, external effect, product source/test
change, screenshot containing a credential, E0002 work, ceremony authorization,
commit, or push.

## Acceptance

- Reproduce the initialization failure class with the installed browser CLI.
- Establish one isolated headed session and one visible tab on a credential-free
  loopback launcher before any asynchronous navigation.
- Navigate that exact existing document to a credential-free target holding a
  pending request, then prove the target URL, title, marker, and pending state
  through direct reads while inventory remains exactly one tab.
- Distinguish stale inventory metadata from authoritative live-document reads.
- Check visible content, console, error overlay, and screenshot only while the
  probe is credential-free.
- Close the browser, stop the listener, prove listener absence, and remove the
  exact transient server/browser directories and captures.

## Stop and escalate

Stop if a product fixture or credential would be needed, a second tab/session
appears, loopback binding fails, direct reads do not converge, the frozen
contract would change, or any product ceremony is requested without a fresh
explicit owner decision.

## Handoff

Record the installed CLI behavior, direct/inventory observations, visual and
console result, exact cleanup, operational recommendation, and remaining owner
gate. Retain no browser profile, capture, private workspace, or credential.
