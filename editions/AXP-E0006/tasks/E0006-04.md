# E0006-04 — Integration, public guidance, and owner release gate

- Edition/task: `AXP-E0006 / E0006-04`
- Wave: W4
- Model/reasoning: `gpt-5.6-sol` / high
- Owner: orchestrator
- Risk and owner gate: critical integration; product-owner Gate C required
- Dependencies: E0006-01, E0006-02, E0006-03

## Objective

Quiesce all writers, reproduce implementation and independent evidence,
reconcile the frozen contract/public guidance/backlog, run scoped impact and
the explicit edition final verifier, and present an owner-ready Gate C packet.

## Writable paths (exclusive)

- `README.md`
- `docs/dogfood/release-manager-preview.md`
- `contracts/approval-console-session.md`
- `editions/BACKLOG.md`
- E0006 status, decisions, evidence, retro, and `handoffs/E0006-04.md` exactly
  as listed in `assignments.tsv`
- `Cargo.toml`
- `Cargo.lock`

## Read-only references

All E0006 packets/handoffs, approved decisions, CLI diff/tests, browser evidence,
AXP contract/model policy, and E0001 residual-risk disposition.

## Non-goals

No security repair under integration ownership, provider call, private
credential/workspace capture, E0002 activation before Gate C, silent evidence
waiver, destructive cleanup, commit, or push.

## Acceptance

- Every task is quiescent with accepted evidence and no ownership conflict.
- Contract and implementation match exact routes, states, limits, expiry,
  bindings, failure order, actionability, and rollback.
- Public guidance removes the prohibition only if the new path passed; it
  never documents a secret-bearing URL or restores historical fragment advice.
- E0006-03 is a distinct non-author and passes exact 14/14 with no sensitive
  artifact retained.
- `rtk cargo fmt --check -p proof-transport-cli` and
  `rtk cargo test -p proof-transport-cli` pass in host context.
- `rtk scripts/test-scoped.sh proof-transport-cli` passes for the final graph.
- `rtk scripts/swarm.sh validate AXP-E0006` and `rtk git diff --check` pass.
- `rtk scripts/swarm.sh verify AXP-E0006 --quiescent` passes only after all
  writers stop.
- Rollback, residual risks, zero provider spend, and a dated product-owner
  Gate C accept/defer/reject decision are recorded without self-approval.

## Stop and escalate

Stop on a failed required check, source/contract drift, author-verifier overlap,
secret exposure, unrelated dirty change, missing rollback/cost fact, failed
final gate, or any requested E0002 implementation. Return a bounded repair or
defer recommendation.

## Handoff

Record integrated revision, changed files, exact commands/results, policy and
evidence digests, security/browser verdict, zero provider spend, limitations,
rollback, and owner decision. Never include credentials or private material.
