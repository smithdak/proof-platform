# E0006-01 — Threat model, session contract, and owner gates

- Edition/task: `AXP-E0006 / E0006-01`
- Wave: W1
- Model/reasoning: `gpt-5.6-sol` / high
- Owner: orchestrator
- Risk and owner gate: critical security/public contract; Gate A and Gate B
  required before W2
- Dependencies: E0001 Gate C defer D-E0001-020 and quiescent prior writers

## Objective

Convert the read-only P0 discovery into an exact, owner-ready charter,
bootstrap/session security contract, deterministic evaluation, conflict-safe
workgraph, and dated Gate A/B proposal without modifying the current CLI or
creating any credential/session.

## Writable paths (exclusive)

- `contracts/approval-console-session.md`
- `evals/approval-console-security-v1.json`
- `editions/BACKLOG.md`
- E0006 control records, task packets, and the four required initial handoff
  placeholders exactly as listed in `assignments.tsv`; after W1, each later
  handoff becomes exclusive to its task owner

## Read-only references

- `AGENTS.md`, `contracts/axp-experience.md`, `editions/MODEL_POLICY.md`
- E0001 Gate C records and retrospective
- Current CLI approval UI/server/HTML/signing paths and tests
- Existing public warnings and dogfood records

## Non-goals

No CLI/source/manifests/lockfile change, provider call, server/browser start,
credential creation, private workspace access, owner-gate inference, E0002
activation, commit, or push.

## Acceptance

- Threat actors, leakage channels, retained defenses, state machines, exact
  route/body/header rules, limits, failure ordering, rollback, residual risks,
  and compatibility are explicit and internally consistent.
- Evaluation contains exactly 14 unique ordered required checks with normative
  assertions and 26 unique ordered rejection vectors with no-side-effect
  results. Their expected set digests are frozen; passing requires 14/14 and
  10,000 basis points with zero provider calls/external network.
- Every task has one owner, exclusive paths, dependencies, model/effort,
  bounded scope, acceptance evidence, stop conditions, and unique handoff.
- `rtk scripts/swarm.sh validate AXP-E0006` passes.
- `rtk git diff --check -- editions/AXP-E0006 contracts/approval-console-session.md evals/approval-console-security-v1.json editions/BACKLOG.md` passes.
- Gate A and Gate B remain visibly pending; E0006-02 remains unassigned.

## Stop and escalate

Stop on a second active edition writer, missing E0001 disposition, any need to
touch product code, an unresolved bootstrap authority ambiguity, credential or
private-workspace exposure, or a proposal that requires persistent/remote
authority. Return a bounded owner decision request.

## Handoff

Record the exact scaffold files, audit sources, validation commands/results,
assumptions, residual risks, and the owner decisions still required. Never
include credential examples that could be mistaken for live secrets.
