# E0006-05 — Current-document bootstrap/session delivery remediation

- Edition/task: `AXP-E0006 / E0006-05`
- Wave: W4
- Model/reasoning: `gpt-5.6-sol` / high
- Owner: e0006-security
- Risk and owner gate: critical authentication/session delivery boundary;
  explicitly authorized by D-E0006-015
- Dependencies: E0006-02 `done`; E0006-03 and D-E0006-014 failure evidence

## Objective

Reproduce, classify, and correct the D-E0006-014 current-document failure in
which terminal verification succeeded but the attached page received a
generic rejection before session/app evidence. Preserve the frozen
`proof-approval-console-session/v1` state machines and every fail-closed and
credential-secrecy invariant.

## Writable paths (exclusive)

- `crates/proof-transport-cli/**`
- `editions/AXP-E0006/handoffs/E0006-05.md`

## Read-only references

- Approved E0006 charter, D-E0006-003/D-E0006-014/D-E0006-015, frozen session
  contract/evaluation, assignments, and this packet
- E0006-02 implementation/handoff and E0006-03 browser evidence
- Canonical approval/kernel contracts; no edit permission

## Non-goals

No contract/evaluation digest change, longer credential lifetime, reusable or
persistent credential, page-reload recovery, second exchange/session, detailed
browser authentication oracle, kernel/runtime/storage/schema/migration edit,
new dependency, remote bind, provider call, external effect, E0002 behavior,
public-guidance edit, commit, or push.

## Acceptance

- Add a deterministic regression that reproduces the observed sequence or its
  exact reachable state class: one registered page, one claimed exchange,
  terminal verification, and a current document that receives generic failure
  before app/session evidence.
- Establish the root cause from source plus deterministic evidence. Do not
  infer compromise or a competing browser without evidence.
- Correct the legitimate single-document path so one accepted bootstrap and
  its one claimed exchange receive exactly one distinct session after terminal
  verification. No session may be released before verification.
- Reload, navigation/page-memory loss, response loss, duplicate registration,
  replay, concurrent exchange loser, expiry, and wrong scope remain terminal
  and require server restart; none can obtain a second session.
- Browser-visible authentication failures remain generic and contain no state,
  timing, prefix, credential, or approval-data oracle. Any new diagnostic fact
  is deterministic, non-secret, operator-local, and absent from ordinary
  launch output unless the approved contract already permits it.
- Existing Host/Origin/content-type, loopback, expiry, revocation, signing
  lease, exact actionability, zero-auto-resume, and secret-channel checks remain
  unchanged.
- Focused regression, `rtk cargo fmt --check -p proof-transport-cli`, and
  `rtk scripts/test-scoped.sh proof-transport-cli` pass in host context. Record
  the `--list` impact set and a redacted secret-sentinel scan.

## Stop and escalate

Stop on any need to alter the frozen protocol/state machines/TTLs, expose a
failure oracle, recover authority after reload or response loss, add a second
session, change another crate or lockfile, touch a credential in evidence,
start a browser ceremony, use a provider, or cause an external effect.

## Handoff

Record the reproduced cause, changed files, exact tests/results, proof that all
failure paths remain closed, secret-sentinel method, assumptions, residual
risks, and cross-owner requests. Include no bootstrap/session value, private
key, complete workspace, browser export, screenshot, or copied database.
