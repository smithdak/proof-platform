# E0006-03 — Independent browser and security verification

- Edition/task: `AXP-E0006 / E0006-03`
- Wave: W3
- Model/reasoning: `gpt-5.6-sol` / high
- Owner: e0006-verifier, distinct non-author
- Risk and owner gate: critical independent verification; no product authority
- Dependencies: E0006-02 `done` and quiescent

## Objective

Independently verify the implemented contract end to end in one fresh synthetic
workspace and produce redacted, reproducible 14/14 evidence without authoring
the security behavior or retaining any credential-bearing artifact.

## Writable paths (exclusive)

- `docs/dogfood/approval-console-secure.md`
- `editions/AXP-E0006/handoffs/E0006-03.md`

## Read-only references

- Approved E0006 contract/evaluation/decisions and E0006-02 handoff/diff
- CLI approval sources/tests and current terminal approval behavior
- `README.md` and dogfood warnings; do not edit

## Non-goals

No source/test/contract fix, private historical workspace, real customer data,
provider call, network beyond loopback, decision beyond the synthetic fixture,
credential-bearing screenshot/log/export, owner-gate decision, commit, or push.

## Acceptance

- Reproduce all 14 required checks and 26 rejection vectors from the frozen
  policy; record the policy/check-set/rejection-set digests and exact result.
- Use a disposable synthetic workspace and a synthetic pending approval. After
  starting the local server, use the available `vercel:agent-browser` and
  `vercel:agent-browser-verify` skills for browser inspection.
- Prove the address bar/history is the clean loopback URL; Web Storage,
  IndexedDB, cookies, service workers, referrer, and console contain no
  credential; capture no screen while the one-use code is visible.
- Prove required no-store, CSP, no-referrer, frame denial, and nosniff headers;
  no remote requests or CORS permission occur.
- Verify one exact signed approve or deny decision, unchanged required Human
  and canonical arguments, and no tool execution/resume. Use a separate
  fixture or deterministic router harness for destructive rejection vectors.
- Independently inspect the process output/argv and worktree/test artifacts
  with a synthetic sentinel without printing the sentinel itself.
- Reproduce CLI scoped tests, format check, and reverse-impact selection; record
  host/sandbox distinctions rather than weakening secure ancestry checks.
- Record a dated PASS only if every required check passes. On any failure,
  stop without fixing it and return the exact bounded blocker to E0006-02.

## Stop and escalate

Stop if assigned to an E0006-02 author, any credential would enter evidence,
the server binds beyond loopback, a browser/plugin capability is unavailable,
the synthetic decision could cause a governed effect, or any required check
fails. Do not retry a credential-bearing capture.

## Handoff

Record commands, redacted observations/digests, browser storage/cookie/referrer/
console results, headers, exact decision/zero-execution evidence, test results,
limitations, and PASS/FAIL. Never include the bootstrap/session, private key,
complete `.proof` workspace, HAR/network export, or copied database.
