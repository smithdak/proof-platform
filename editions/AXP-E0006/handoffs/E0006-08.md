# E0006-08 Handoff

Status: complete/quiescent — **FAIL / BLOCKED**; D-E0006-018 authority
consumed; no retry; cleanup complete

## Changed files

- `docs/dogfood/approval-console-secure.md`
- `editions/AXP-E0006/handoffs/E0006-08.md`

No source, test, contract, evaluation, decision, assignment, root record,
fixture, credential, or runtime file was edited. No commit or push occurred.

## Frozen inputs

- Contract file SHA-256:
  `e040b4a0913490b42a0af6143e35da2d25261ccaf8e67a169b06259e26b67463`.
- Evaluation file SHA-256:
  `7b91911ad524f5ea303ae939bf5f852e8a83b852814feba834134d298a230634`.
- Ordered 14-check set SHA-256:
  `b5ddad83dc550468dd062ca814313e64fd04cb63ffad00a8a2dc7e5391c01889`.
- Ordered 26-vector set SHA-256:
  `43ad301d7a86e91760961b3d309ff193515029d6c688ba19d816936f1ef10fe8`.

Canonical recomputation confirmed cardinalities 14 and 26. The contract and
evaluation file hashes also remained exact.

## Credential-free launcher preflight

Before product state existed, the distinct verifier attached read-only to the
single isolated headed session `e0006-e08`. Session inventory contained exactly
one active session and tab inventory exactly one tab; the inventory's stale
`New Tab` / `chrome://newtab/` label was permitted and non-authoritative.

The authoritative direct document read returned exact URL
`http://127.0.0.1:43125/launcher`, title `E0006 verified launcher`, body marker
`Proof approval launcher ready`, and state `verified launcher`. The body was
non-empty, framework-overlay count was zero, captured console-error count was
zero, the console command had no messages, and the page-error command was
empty. No navigation, reload, click, screenshot, tab mutation, product state,
credential read, or file edit occurred during the preflight. This preflight
passed, but it is not product or decision evidence by itself.

## Authorized ceremony result

The single D-E0006-018-authorized launcher-first provider-free ceremony failed
the denial-only contract. Durable state contains exactly one request,
`01a05c11-6b76-7e32-9c00-b3fd952b468a`, for
`release.publish::v1`, with exact arguments `environment=preview` and
`version_label=2026.08.29-rc1`. Instead of the required denial, it contains one
unexpected signed **approved** decision at
`2026-09-01T08:28:42.006835034Z`, with reason `approve`.

No credential, private key, signature bytes, bootstrap value, or session value
was read, printed, captured, retained, or added to evidence. The read-only
validation established only that the durable signed-record field is present;
it did not extract its contents.

Because the exact contract requires one denial, the approved outcome is a
terminal acceptance failure. The ceremony cannot count as 14/14 or 10,000
basis points, regardless of the credential-free launcher preflight. Historical
or preflight evidence was not combined into a PASS.

## Independent durable containment evidence

The read-only `approval list` and `run inspect` JSON views were sanitized in
memory to emit only non-secret summary fields. They confirmed:

- one request, one decision, zero approval executions, and zero evaluations;
- request `01a05c11-6b76-7e32-9c00-b3fd952b468a`, operation
  `release.publish`, version `v1`, outcome `approved`, the exact timestamp and
  reason above, signed-record presence, and `execution_proof_id: null`;
- run `01a05c11-6b17-7ed3-b340-f2a333cba9b6` remains
  `waiting_for_input`;
- step `01a05c11-6b36-7f00-adb4-75c34a4c20b5` remains
  `waiting_for_approval`, is bound to that run/request and
  `release.publish::v1`, and has no output;
- event sequences 0–4 are exactly `started`, `model_requested`,
  `model_responded`, `tool_requested`, and `approval_required`, with zero
  `approval_resumed` and zero `tool_succeeded`; and
- latest checkpoint usage is zero input, output, total tokens, and cost, and
  retains only the expected synthetic `preview` /
  `2026.08.29-rc1` arguments.

Thus there was no runtime resume, execution proof, tool success, evaluation,
provider call, paid usage, or external/governed effect. The approved authority
was not exercised.

## Shutdown and cleanup

The controlled browser session `e0006-e08` is closed. After exact product PID
termination, independent host listener checks found no listener on either
43125 or 43126. The Human confirmed exact same-TTY `echo restored` after the
product UI process fully exited.

Root permanently disposed of the exact fixture
`/tmp/proof-e0006-e08.OmPnh0`, browser runtime
`/tmp/e0006-e08-browser.SnZSPF`, and launcher workspace
`/tmp/e0006-e08-launcher.9eODNX`. Independent checks confirmed all three paths
absent; they are not recoverable. This verifier did not copy or inspect private
fixture material. D-E0006-018 authority is consumed; no server/browser
restart, retry, execution/resume, or source repair is permitted.

## Commands and results

- `rtk target/debug/proof --workspace <disposable> approval list`, sanitized
  before output — count 1; exact request/operation/version; status and outcome
  `approved`; reason `approve`; exact decision time; execution proof null.
- `rtk target/debug/proof --workspace <disposable> run inspect <run-id>`,
  sanitized before output — run `waiting_for_input`; sole step
  `waiting_for_approval`; execution absent; input/output/total tokens and cost
  all zero.
- `rtk sha256sum` on the frozen contract/evaluation — exact hashes above.
- Canonical ordered-set hash recomputation — 14 and 26 entries with exact
  hashes above.
- Host-context `rtk ss -ltnp` scoped separately to ports 43125 and 43126 — no
  listeners.

## Final verdict

**E0006-08 FAIL / BLOCKED: denial-only contract violated by an unexpected
approved decision. No 14/14 result can be claimed. D-E0006-018 is consumed,
no retry is authorized, and Gate C remains unopened.**
