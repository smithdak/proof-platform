# E0006-03 Handoff

- Edition/task: `AXP-E0006 / E0006-03`
- Agent/model/reasoning: `e0006-verifier` (distinct non-author) /
  `gpt-5.6-sol` / high
- Evaluation date: 2026-08-31
- Result: **FAIL / blocked — 14/14 not established; Gate C must not proceed**
- Provider/external spend: zero; no provider credential or external network was
  read or used

## Changed files

- `docs/dogfood/approval-console-secure.md`
- `editions/AXP-E0006/handoffs/E0006-03.md`

No source, test, contract, policy, control record, manifest, lockfile, database,
or other documentation file was edited.

## Frozen inputs

- Contract file:
  `sha256:e040b4a0913490b42a0af6143e35da2d25261ccaf8e67a169b06259e26b67463`
- Evaluation file:
  `sha256:7b91911ad524f5ea303ae939bf5f852e8a83b852814feba834134d298a230634`
- Ordered 14-check set:
  `sha256:b5ddad83dc550468dd062ca814313e64fd04cb63ffad00a8a2dc7e5391c01889`
- Ordered 26-vector set:
  `sha256:43ad301d7a86e91760961b3d309ff193515029d6c688ba19d816936f1ef10fe8`

The cardinalities, order, and all four digests matched D-E0006-003. The
verifier did not author E0006-02.

## Commands and results

- `rtk cargo test -p proof-transport-cli` with required host escalation —
  **117 passed across three suites** in 5.07 seconds.
- `rtk cargo fmt --check -p proof-transport-cli` — passed.
- `rtk scripts/test-scoped.sh proof-transport-cli --list` — changed and impacted
  package set both contained only `proof-transport-cli`.
- `rtk git diff --check -- crates/proof-transport-cli Cargo.lock
  editions/AXP-E0006/handoffs/E0006-02.md` — passed.
- `rtk cargo tree -p proof-transport-cli -i rustix` — resolved the direct CLI
  edge to the already-locked `rustix v1.1.4`.
- Frozen file hashes used `rtk sha256sum`; canonical ordered-set hashes were
  independently recomputed from lexicographically key-sorted compact JSON.
- A fresh private `/tmp/proof-e0006-verifier.*` workspace was initialized,
  provisioned with the checked-in registry, enrolled one synthetic Human, and
  used provider-free `agent live-prepare start` to create exactly one pending
  signed `release.publish::v1` request.
- The console was started with `rtk ./target/debug/proof -w
  <DISPOSABLE_WORKSPACE> approval ui --port 0` in a real host controlling PTY.
  Its ordinary output was one clean `http://127.0.0.1:<PORT>/` URL plus
  non-secret instructions. `lsof` showed one IPv4 listener at exactly
  `127.0.0.1:<PORT>`.
- The required `agent-browser` and `agent-browser-verify` workflows were used
  after server start. The isolated session opened the clean loopback URL and
  returned a meaningful page title. Credential-redacted evaluations checked
  page content, error-overlay absence, URL/query/fragment/history length,
  referrer, storage counts, cookie text, IndexedDB count, service-worker count,
  resource origins, bootstrap element shape, and hidden approval app state.
  `agent-browser errors` was empty and `agent-browser console` contained no
  page output.
- Real header probes against the index, an unknown API route, and a
  foreign-origin OPTIONS request showed the required no-store/nosniff baseline,
  restrictive CSP/no-referrer/frame denial on the index, and no permissive CORS
  header.
- No screenshot, trace, video, PDF, browser state file, HAR, network export, or
  copied database was created. In particular, no screenshot was taken while
  the one-use code was visible.
- Before shutdown the PTY was observed in the expected non-echoing prompt
  state. The bootstrap reached its 120-second expiry and the process published
  only the redacted expiry result. The browser was closed, SIGINT stopped the
  server, and `lsof` confirmed the listener was absent. The PTY device was
  destroyed with the process, preventing a post-exit `stty` comparison; the
  117/117 deterministic suite included the explicit restoration tests, but
  this is not counted as live success evidence.
- Final read-only workspace inspection showed one pending request with
  `decision: null` and `execution_proof_id: null`; the run was
  `waiting_for_input`, the step was `waiting_for_approval`, no execution record
  existed, and events ended at `approval_required` with no resume or tool
  success. The browser session was closed and the exact disposable workspace
  was removed.

### Follow-up normal-browser Human ceremony

A new fixture at `/tmp/proof-e0006-manual.jVQYxU` completed a successful
credential-safe Human journey on fixed loopback port `43118`. The Human
visually transferred the one-use code directly from the normal browser to the
controlling Terminal A; no bootstrap or session value was communicated to the
verifier. The code cleared, the inbox became visible, and the Human signed
exactly one denial.

Durable binding record:

- request: `01a058a3-b3f4-76f3-8ebb-2ee04adb8d75`;
- run: `01a058a3-b3a9-76c1-8f16-82dc1822f3f9`;
- step: `01a058a3-b3c5-7e92-9ad0-5d76d8897b14`;
- operation/version: `release.publish::v1`;
- exact arguments: `{"environment":"preview","version_label":"2026.08.29-rc1"}`;
- request/input digest:
  `8dbccce517d9d4db2349b31681da8125a918c4e0740f1ff9d868618155cd835c`;
- enrolled Human: `01a058a3-9a87-7472-bb5d-39c665f2f1f9`;
- decision: `01a058a7-6907-7c90-8a8b-fc6a51c4645e`, outcome `denied`;
- reason: `E0006 synthetic verification — deny; no execution authorized.`

Independent checks after the decision:

- `approval list` returned exactly one approval, the exact denial above, and
  `execution_proof_id: null`.
- `agent watch 01a058a3-b3a9-76c1-8f16-82dc1822f3f9` showed the request digest
  equals the step input digest, the runtime checkpoint retains exactly the two
  stated arguments, the run is still `waiting_for_input`, the step is still
  `waiting_for_approval`, `execution` is null, and events end at sequence 4
  `approval_required`. There is no `approval_resumed`, `tool_succeeded`, proof,
  or evaluation.
- A provider-free `agent live-prepare finish` validation attempt loaded the
  exact awaiting record, verified the requester signature, loaded the enrolled
  Human principal, verified the decision signature, and checked decision
  bindings. It then returned `preparation requires the exact trusted Human
  approval` because the valid outcome was intentionally `denied`. Source order
  places this failure before registry validation, runtime construction, or
  `resume`, so it produced no execution or continuation.
- `lsof` showed PID 2005380 listening only on IPv4
  `127.0.0.1:43118`. Read-only `ps` showed exact argv
  `/home/dakota/projects/proof-platform/target/debug/proof -w
  /tmp/proof-e0006-manual.jVQYxU approval ui --port 43118`; it contains no
  credential. `pgrep -P 2005380` found no child process.
- The live index returned 200 with no-store, nosniff, the restrictive CSP,
  no-referrer, and frame denial. A live unknown API route returned 404 with
  no-store/nosniff; a foreign-origin OPTIONS request returned 405 with
  no-store/nosniff and no CORS allowance.
- After independent durable inspection, the Human clicked End Session and
  reported the exact non-secret ended-session UI state. The UI reaches that
  state only after the authenticated revoke response succeeds and the closure
  credential is cleared. A post-revoke `approval list` read returned the same
  one denial with `execution_proof_id: null`; `lsof` still showed only the
  expected IPv4 loopback listener pending controlled Terminal A shutdown. The
  session credential itself was never requested or inspected.
- The Human then stopped Terminal A with Ctrl+C and immediately ran the
  same-TTY restoration check, which returned exactly `echo restored`.
  Independent `lsof` inspection found no listener on port 43118. One final
  read-only durable check returned the unchanged single valid denial with
  `execution_proof_id: null`; only then was the exact disposable fixture
  `/tmp/proof-e0006-manual.jVQYxU` removed. Removal was confirmed, and no other
  workspace or artifact was deleted.
- Path/mode-only inspection showed the expected private workspace surfaces;
  no private file was read or copied. No current bootstrap/session sentinel can
  be scanned without extracting the browser/process-memory credential, so that
  unsafe step was not attempted. The Human reported taking one permitted
  post-code-clear screenshot in the normal browser; it was not transmitted to,
  independently inspected by, or retained by the verifier and therefore does
  not count as agent-browser evidence. No credential-bearing screenshot, HAR,
  browser state, private key, database copy, or credential-bearing artifact was
  created or retained by the verifier.

The Human journey is therefore verified through a valid signed denial and
zero execution/resume. The overall result remains **FAIL / blocked**, because
the successful page is in a normal browser that is not attachable to the named
agent-browser session. The task packet requires current-session independent
agent-browser visual, URL/history, storage, cookie, referrer, console, service
worker, resource, and screenshot evidence. Prior agent-browser evidence came
from another disposable server instance and cannot be combined with this
memory-only session into a 14/14 claim.

### Authorized headed current-session attempt

D-E0006-008 authorized a final fresh provider-free fixture at
`/tmp/proof-e0006-final.bwKhgc`, fixed port `43120`, preparation
`00000000-0000-7000-8000-00000000000a`, request
`01a058bb-8e12-7332-8ca9-2eff41f1e83f`, run
`01a058bb-8dc6-7f51-9555-ed0a64ec3de5`, step
`01a058bb-8de2-7e50-8792-e2544cf936ab`, and enrolled Human
`01a058bb-706c-7d80-bbcc-e19200052211`. Exactly one request existed with no
decision or execution before launch.

The Human visually entered the bootstrap from the real headed named
`e0006-manual` browser into Terminal A. After Terminal A reported local
confirmation verified and `#app:not([hidden])` succeeded, the verifier attached
to that same browser with `DISPLAY=:0` and the isolated
`XDG_RUNTIME_DIR=/tmp/e0006-agent-runtime`.

Credential-redacted current-session evidence:

- exact current URL `http://127.0.0.1:43120/`; empty query, fragment, and
  referrer; browser history length 3; Navigation API count 1 with every exposed
  current entry exactly the clean loopback URL;
- localStorage 0, sessionStorage 0, cookies empty, IndexedDB 0, service workers
  0, Cache Storage 0;
- 13 resource entries, zero remote-origin, and every resource URL same-origin
  with empty query and fragment;
- bootstrap text empty, bootstrap hidden, app visible, no global
  `window.session`, no error overlay, no browser errors, and no console output;
- exactly one approval and exact operation, arguments, request, run/step,
  requester, and enrolled Human; both decision controls initially enabled;
- interactive snapshot exposed only the expected approval UI controls; and
- full post-bootstrap capture `/tmp/e0006-43120-postbootstrap.png`, 1893×1402
  RGB PNG, visually inspected credential-free, SHA-256
  `7478922b399e3da38ed138285823ed07d34c7404693580e7cb49cef7df12a559`.

The Human attempted exactly one denial with reason `E0006 synthetic
verification — deny; no execution authorized.` The mutation did not persist.
The same browser showed `request rejected`, empty receipt, and pending detail;
durable `approval list` returned `decision: null` and
`execution_proof_id: null`. `agent watch` showed the exact two arguments and
matching input digest, run `waiting_for_input`, step `waiting_for_approval`, and
events ending at sequence 4 `approval_required`, with no `approval_resumed`,
`tool_succeeded`, proof, or evaluation. No second decision was attempted.

The post-rejection browser secrecy aggregates remained clean: exact URL, empty
query/fragment/referrer/Web Storage/cookies/IndexedDB/service workers/Cache
Storage, zero remote resources, code cleared, app visible, and no overlay or
generic resume text. Full rejection capture
`/tmp/e0006-43120-decision-rejected.png` was visually credential-free and had
SHA-256
`78061d1f0394dd2e1f29666b6ee46988395d8210ca40c0c01102fe8deb9de81a`.

The request subsequently expired with null decision/execution. End Session
returned a generic rejection, and the UI disabled authority actions and
remained fail-closed rather than claiming revocation. The Human stopped
Terminal A; the same-TTY check returned exactly `echo restored`; no listener
remained on 43120. The named browser was closed and `agent-browser session
list` returned no active sessions.

The earlier headed timeout fixture `/tmp/proof-e0006-final.BSUOXY` was also
independently confirmed expired with `decision: null`,
`execution_proof_id: null`, and no listener on 43119. After those final reads,
only the two exact fixture directories and the two credential-free PNGs named
above were removed, and removal was confirmed.

The headed attempt proves the previously missing current-session browser
secrecy/capture capability. It remains an overall **FAIL / blocked** result
because the same run did not produce the required signed decision or confirmed
explicit revoke. The normal-browser signed-denial run and this distinct headed
browser run cannot be combined into one frozen 14/14 evaluation.

### D-E0006-010 decision-first rerun

The final authorized provider-free fixture used
`/tmp/proof-e0006-five-minute.vy8Ws4`, fixed port `43121`, preparation
`00000000-0000-7000-8000-00000000000b`, request
`01a058d7-2cf6-7a53-aca9-af9cd8880334`, run
`01a058d7-2cae-7aa2-be7a-99fd26f7f9c4`, step
`01a058d7-2cc8-7c63-abdc-898f1030c329`, requester
`01a058d6-ac35-7b70-889a-00814c62be53`, and enrolled Human
`01a058d6-d910-7e23-a2f2-55384af38d35`. It began with exactly one pending
`release.publish::v1` request and no decision or execution.

The Human performed the visual one-use handoff in the headed
`e0006-manual` page and signed exactly one denial before verifier browser
inspection. The Human-visible UI showed exact `preview` /
`2026.08.29-rc1` arguments, blocked reason `approval request was already
denied`, reason `E0006 synthetic verification — deny; no execution
authorized.`, receipt `Decision signed: denied.`, and a displayed resume
command that was explicitly not run.

Credential-free durable inspection established:

- decision `01a058de-c60a-7e72-be94-b13a13d477bd`, outcome `denied`, fixed
  reason, exact request ID, required Human, and decision timestamp
  `2026-08-31T17:29:49.826859514Z` inside the request window;
- request and decision signatures were each 64 bytes, and the decision's
  request digest was
  `1b501c80b9f8260d958060884fffb54599a2e541e2511e8e21a990c17fb6f0ea`.
  The mutation route verifies the signed request and enrolled Human key before
  signing and persistence; the deterministic signature/substitution suite was
  already green;
- exact pending input `preview` / `2026.08.29-rc1`, digest
  `8dbccce517d9d4db2349b31681da8125a918c4e0740f1ff9d868618155cd835c`,
  and matching request/run/step/requester context;
- `execution_proof_id: null`, no execution record, run `waiting_for_input`,
  step `waiting_for_approval`, and only event sequences 0–4 ending at
  `approval_required`; there was no `approval_resumed`, tool success, proof,
  evaluation, or second decision; and
- an exact IPv4 `127.0.0.1:43121` listener and clean unauthenticated index
  headers: no-store, CSP, no-referrer, frame denial, and nosniff.

The independent same-tab browser requirement remained unavailable. Attaching
to named session `e0006-manual` returned the clean loopback URL but exposed a
distinct fresh pre-bootstrap page: tab inventory contained only `New Tab`,
bootstrap text length was zero, the bootstrap panel was visible, the approval
app and session controls were hidden, and only one same-origin resource was
present. Its aggregate surfaces were otherwise clean—empty query, fragment,
referrer, Web Storage, cookies, IndexedDB, service workers, and Cache Storage;
zero remote resources; no global `window.session`. Because it was not the
Human-visible post-decision page, no screenshot was taken and none of those
aggregates are claimed as evidence for the decision session.

After the durable read, the Human clicked End Session and reported the exact
`session ended` state. A post-revoke durable read preserved the denial and null
execution proof. Controlled Ctrl+C removed the listener; the same Terminal A
returned exactly `echo restored`; the named browser was closed and session
inventory was empty. A final durable read again preserved exactly one denial,
zero execution, and the unresumed waiting run. The exact fixture was then
removed and absence confirmed.

This rerun verifies the Human decision, explicit revoke, shutdown, and
fail-closed no-execution product path. It remains overall **FAIL / blocked**:
the independent verifier never obtained the Human-visible post-decision tab,
so the frozen current-session browser aggregate and screenshot requirement is
not established. Browser evidence from the prior fixture cannot be combined
with this run.

### Credential-free cross-agent tab probe

A bounded diagnostic then tested attachment without using a product fixture,
approval authority, credential, provider, external network, or governed
effect. The orchestrator created isolated runtime `/tmp/e0006-tab-runtime`, one
headed named session `e0006-tab-probe`, and one tab initialized at
`about:blank`. That existing tab asynchronously navigated via
`setTimeout(location.assign(...))` to `http://127.0.0.1:43122/` while an
intentional `/hold` fetch remained unresolved.

The verifier independently attached with the same runtime and session name.
Credential-free direct reads from both agents reached the exact live page:

- URL `http://127.0.0.1:43122/`;
- title `E0006 attachment probe`;
- body marker `Credential-free single-tab attachment probe`; and
- visible status `pending` while `/hold` remained unresolved.

Inventory remained exactly one active named session and one tab. The
`agent-browser tab list` display retained stale initialization metadata
`about:blank`, while direct URL/title/body reads returned the current navigated
document. Therefore tab-list URL metadata is non-authoritative after this
asynchronous navigation. The verifier did not navigate, screenshot, close, or
mutate any product state during the probe.

The probe browser/session, loopback listener, isolated runtime directory, and
disposable temp content were fully cleaned up. Independent checks found no
listener on 43122 and no `/tmp/e0006-tab-runtime` directory. The probe explains
the earlier tab-list mismatch but cannot be substituted for the frozen product
journey: it contained no product bootstrap/session, request, Human decision,
execution boundary, or revoke. D-E0006-010 was consumed by the completed
decision-first run, so **FAIL / blocked** remains and another product attempt
requires new explicit owner authorization.

### D-E0006-013 final single-tab run

The final authorization staged fresh provider-free fixture
`/tmp/proof-e0006-final-single.ICHLUn`, preparation
`00000000-0000-7000-8000-00000000000c`, request
`01a058f6-fdd8-7312-a98c-f6aca788c2c4`, run
`01a058f6-fd92-77b1-be95-de7e8e797893`, step
`01a058f6-fdad-7bd0-bcd3-2621d703f535`, and enrolled Human
`01a058f6-8ed4-70e1-96d3-9a16a4542472`. Exactly one pending
`release.publish::v1` request existed with no decision or execution. The clean
server selected default IPv4 loopback port `35953`; planned port `43123`
remained unused.

The Human reported `Local confirmation verified.` The verifier then attached
read-only to isolated runtime `/tmp/e0006-final-runtime`, display `:0`, and
named headed session `e0006-final`, without navigation, a second tab, clicks,
or credential access. Direct reads established one active named session, one
tab, exact URL `http://127.0.0.1:35953/`, and title `Proof approvals`. The tab
inventory retained stale initialization metadata `chrome://newtab/`, which the
credential-free probe had already shown to be non-authoritative.

The current DOM did not expose the Human-expected approval app:

- bootstrap panel visible, bootstrap code text empty, and app/session controls
  hidden;
- one same-origin resource, with clean URL, query, fragment, referrer, storage,
  cookie, IndexedDB, service-worker, Cache Storage, and remote-resource
  aggregates on the exposed tab;
- no detected overlay; and
- status `request rejected. Restart the console to try again.`

The screenshot safety gate required code cleared **and hidden** plus the app
visible. It did not pass, so no screenshot was taken and the exposed-tab
aggregates are not claimed as proof of the Human-visible product page. The
terminal handoff succeeded, but session establishment/ownership is
inconclusive. The tab failed closed; this evidence does not establish a second
browser, compromise, or stolen session.

The Human stopped Terminal A with Ctrl+C and the same terminal reported exactly
`echo restored`. Final durable inspection established exactly one still-pending
request, `decision: null`, `execution_proof_id: null`, run
`waiting_for_input`, step `waiting_for_approval`, and event sequences 0–4
ending at `approval_required`. There was no `approval_resumed`, tool success,
proof, evaluation, provider call, governed effect, or retry.

The named browser/session, process, listeners, isolated runtime, and exact
fixture were fully removed. Independent checks found no listener on 35953 or
43123 and neither `/tmp/e0006-final-runtime` nor the fixture remained.
D-E0006-013 authority is consumed. Overall remains **FAIL / blocked** because
this run did not provide the complete product journey and browser evidence in
one session, and prior-run evidence cannot be combined.

## Independent 14-check result

All checks are required and the policy requires 10,000 basis points. The final
decision-first session completed denial and revoke, but its Human-visible tab
was not exposed through the verifier's named-session attachment. The result is
therefore **FAIL / not scored as passing**, not a partial release score.

| # | Check | Independent evidence status |
|---:|---|---|
| 1 | `clean_launch_surface` | Verified live: real controlling PTY, clean output, exact IPv4 loopback listener, clean argv. |
| 2 | `bootstrap_local_handoff` | Human ceremonies, including D-E0006-013, verified direct visual-to-controlling-TTY entry, successful local confirmation, and same-TTY echo restoration without communicating the code. Rejection/restoration paths also passed deterministically. |
| 3 | `bootstrap_single_use` | Deterministic suite passed; the final ceremony produced exactly one successful session and one decision, with no replay or second exchange. |
| 4 | `bootstrap_expiry_and_race` | Deterministic race/equality coverage passed; the live bootstrap also expired without producing a session. |
| 5 | `session_distinction_and_scope` | Deterministic scope/distinction tests passed; the final ceremony established one scoped live session without extracting its value. |
| 6 | `session_expiry_and_revocation` | Deterministic expiry/revoke coverage passed; the final Human-visible session reached the exact ended-session state before controlled shutdown. |
| 7 | `constant_work_secret_comparison` | Deterministic fixed-length decoding/comparison and generic-error tests passed. |
| 8 | `browser_memory_only` | **Blocked for the final decision session:** named-session attachment exposed only a distinct fresh pre-bootstrap tab, not the Human-visible post-decision tab. A prior fixture passed these aggregates, but cross-run combination is forbidden. |
| 9 | `request_boundary` | Verified by deterministic duplicate/header cases and real foreign-origin no-CORS probe. |
| 10 | `response_security_headers` | Verified live for index, 404, and 405 plus deterministic malformed/error cases. |
| 11 | `pre_signing_failure_boundary` | Deterministic Tower lease/race/drift tests passed; the final live signing mutation produced one exact denial bound to the fresh request and no execution. |
| 12 | `approval_v1_v2_compatibility` | Deterministic exact-context/signing/no-resume tests passed; the final run persisted one exact signed v1 denial, and the run remained unresumed with zero execution. |
| 13 | `process_browser_artifact_secret_scan` | Process/static sentinel tests passed and no credential was extracted or captured. **Final-session browser capture is unavailable** because attachment exposed the wrong tab. |
| 14 | `fail_closed_rollback` | Final run reached explicit session revoke, controlled shutdown, no listener, same-TTY echo restoration, and zero execution/resume; deterministic lost-response/authority-error tests remained green. |

## Ordered rejection-vector reproduction

The complete host suite executed the deterministic harnesses authorized for
destructive/racy vectors. All associated tests passed. This table records the
exact ordered frozen set; it does not convert the blocked browser journey into
a 14/14 result.

| # | Rejection vector | Evidence |
|---:|---|---|
| 1 | `non_linux_or_non_tty_launch` | Process test passed. |
| 2 | `url_query_fragment_output_argv_or_environment_credential` | Process/static tests passed; live launch URL was clean. |
| 3 | `malformed_unknown_or_duplicate_bootstrap_body` | Strict Tower body tests passed. |
| 4 | `bootstrap_terminal_mismatch` | Authority-state test passed. |
| 5 | `bootstrap_terminal_eof_loss_or_restore_failure` | Terminal EOT/parser/restoration tests passed. |
| 6 | `bootstrap_expired_before_confirmation` | Deterministic waiter test passed; live bootstrap expired with no session. |
| 7 | `bootstrap_deadline_equality` | Injectable-clock equality test passed. |
| 8 | `exchange_waits_before_local_confirmation` | Async blocking test passed; live page remained pre-session. |
| 9 | `concurrent_exchange_loser` | Deterministic claimed-winner test passed. |
| 10 | `lost_exchange_response` | Consumed/lost-response test passed. |
| 11 | `replayed_exchange` | Single-use/replay tests passed. |
| 12 | `cross_instance_bootstrap_or_session` | Cross-scope test passed. |
| 13 | `cross_workspace_bootstrap_or_session` | Cross-scope test passed. |
| 14 | `malformed_or_duplicate_session_header` | Protected-route header tests passed. |
| 15 | `session_absolute_deadline_equality` | Injectable-clock expiry test passed. |
| 16 | `session_idle_deadline_equality` | Injectable-clock expiry test passed. |
| 17 | `explicit_revoke_then_stale_page` | Router revoke/stale-session test passed. |
| 18 | `decision_races_revoke` | Actual Tower mutation race tests passed. |
| 19 | `decision_races_expiry` | Actual Tower mutation race tests passed. |
| 20 | `missing_wrong_or_duplicate_host` | Request-boundary tests passed. |
| 21 | `missing_wrong_duplicate_null_or_opaque_origin` | Origin tests passed; live foreign-origin probe returned no CORS permission. |
| 22 | `missing_duplicate_non_json_or_malformed_json_content` | Strict content-type/JSON tests passed. |
| 23 | `unknown_route_or_wrong_method` | Framework-error tests passed; live 404/405 retained baseline headers. |
| 24 | `browser_persistence_cookie_referrer_service_worker_or_console_channel` | Static checks and a prior headed fixture passed, but **same-tab post-decision evidence is unavailable for the final signed-decision session**. |
| 25 | `registry_governance_changes_between_review_and_post` | Fresh-registry pre-key-access test passed. |
| 26 | `approval_request_argument_human_or_signature_substitution` | Exact-context substitution and no-execution tests passed. |

## Assumptions and decisions

- A Human entering the displayed value directly through the controlling TTY is
  part of the security property, not test plumbing. Browser DOM extraction
  would put the code in tool output; screenshots were explicitly forbidden
  while it was visible; clipboard/file/pipe/argv or automated OS relay would
  violate the frozen contract. The verifier stopped the first automated
  attempt rather than weakening the handoff; the follow-up co-located Human
  then completed it without communicating the value.
- A prior headed fixture exposed only clean Navigation API entries, but the
  final decision-first session's Human-visible tab was not available to the
  verifier. That prior history evidence cannot close the current-session gap.
- The synthetic fixture used only checked-in code/data and a local scripted
  provider. No governed operation executed and no provider boundary was
  crossed.

## Risks and residual work

- Gate C is blocked because no single fresh session completed every required
  check. The final decision-first session produced an exact signed denial,
  explicit revoke, controlled shutdown, no listener, restored same-TTY echo,
  and zero execution/resume, but named-session attachment never exposed its
  Human-visible post-decision tab. The verifier therefore lacks the required
  same-session browser aggregate and screenshot evidence. D-E0006-013 then
  confirmed terminal handoff but failed closed before browser session
  establishment/ownership could be established; it produced no decision or
  execution and does not imply a second browser or compromise.
- Same-UID process/browser memory inspection, browser developer tooling, root
  compromise, compromised Human private keys, and local denial of service
  remain the frozen residual limitations.

## Cross-owner requests

Do not repair or relax E0006-02. D-E0006-013 authority is consumed; do not run
another product fixture without new explicit owner authorization. Any
authorized rerun must use one fresh headed named agent-browser session while an
authorized co-located Human visually reads the code and types it directly into
the exact controlling terminal. Complete the
credential-free browser capture, exact synthetic decision, durable validation,
confirmed explicit revoke, and shutdown in that same session before request
expiry. Until then, keep `approval ui` unreleased and use terminal approval
commands as rollback.

Never include credentials, private key contents, or copied runtime databases.
